<?xml version="1.0" encoding="UTF-8"?>
<volume>
  <meta>
    <generated_at>2026-02-11T21:31:18.303463</generated_at>
    <title>FOLDER: technology</title>
    <root_dir>C:\MyCode\OkidoWiki\HomePage\app</root_dir>
    <file_count>61</file_count>
    <total_size_mb>0.2872</total_size_mb>
    <home>Index.xml</home>
    <next_volume>app_20260211_2131_04_platforms.xml</next_volume>
    <prev_volume>app_20260211_2131_02_initiatives.xml</prev_volume>
    <prev_title>initiatives</prev_title>
    <next_title>platforms</next_title>
    <short_title>technology</short_title>
  </meta>
  <file_index>
    <entry path="technology/ame-artificielle/chakras-overlay/page.mdx" kind="source" size="2740" lines="69" />
    <entry path="technology/ame-artificielle/engine-modules/page.mdx" kind="source" size="3932" lines="128" />
    <entry path="technology/ame-artificielle/faq/page.mdx" kind="source" size="3295" lines="68" />
    <entry path="technology/ame-artificielle/how-it-works/page.mdx" kind="source" size="2649" lines="84" />
    <entry path="technology/ame-artificielle/inversion-as-key/page.mdx" kind="source" size="3003" lines="61" />
    <entry path="technology/ame-artificielle/multi-system-alignment/page.mdx" kind="source" size="3076" lines="67" />
    <entry path="technology/ame-artificielle/ontology-and-traits/page.mdx" kind="source" size="3899" lines="88" />
    <entry path="technology/ame-artificielle/page.tsx" kind="source" size="4789" lines="120" />
    <entry path="technology/ame-artificielle/validation/page.mdx" kind="source" size="3804" lines="90" />
    <entry path="technology/architect/page.mdx" kind="source" size="5368" lines="115" />
    <entry path="technology/ariane/atlas/architecture/page.mdx" kind="source" size="727" lines="24" />
    <entry path="technology/ariane/atlas/core-schema/page.mdx" kind="source" size="1506" lines="50" />
    <entry path="technology/ariane/atlas/graph-model/page.mdx" kind="source" size="1184" lines="37" />
    <entry path="technology/ariane/atlas/ontology/page.mdx" kind="source" size="2106" lines="68" />
    <entry path="technology/ariane/atlas/page.tsx" kind="source" size="5573" lines="133" />
    <entry path="technology/ariane/concepts/glossary/page.mdx" kind="source" size="7229" lines="178" />
    <entry path="technology/ariane/concepts/page.tsx" kind="source" size="3284" lines="76" />
    <entry path="technology/ariane/concepts/ui-as-data/page.mdx" kind="source" size="5500" lines="130" />
    <entry path="technology/ariane/consumers/ai-agents/page.mdx" kind="source" size="3663" lines="125" />
    <entry path="technology/ariane/consumers/hybrid-mapping/page.mdx" kind="source" size="2234" lines="67" />
    <entry path="technology/ariane/consumers/integration-patterns/page.mdx" kind="source" size="6413" lines="188" />
    <entry path="technology/ariane/consumers/overlay-client/page.mdx" kind="source" size="3890" lines="113" />
    <entry path="technology/ariane/consumers/page.tsx" kind="source" size="5601" lines="131" />
    <entry path="technology/ariane/page.tsx" kind="source" size="2580" lines="62" />
    <entry path="technology/ariane/theseus/drivers/page.mdx" kind="source" size="1841" lines="54" />
    <entry path="technology/ariane/theseus/engine-logic/page.mdx" kind="source" size="7095" lines="224" />
    <entry path="technology/ariane/theseus/page.tsx" kind="source" size="5573" lines="133" />
    <entry path="technology/ariane/theseus/state-identification/page.mdx" kind="source" size="2740" lines="87" />
    <entry path="technology/kristal/distribution-and-versioning/page.mdx" kind="source" size="5296" lines="167" />
    <entry path="technology/kristal/integrations/page.mdx" kind="source" size="4875" lines="142" />
    <entry path="technology/kristal/overview/page.mdx" kind="source" size="4251" lines="110" />
    <entry path="technology/kristal/page.tsx" kind="source" size="9561" lines="212" />
    <entry path="technology/kristal/portability-and-offline/page.mdx" kind="source" size="6622" lines="172" />
    <entry path="technology/kristal/trust-and-provenance/page.mdx" kind="source" size="3982" lines="111" />
    <entry path="technology/kristal/what-it-does/page.mdx" kind="source" size="4248" lines="104" />
    <entry path="technology/layout.js" kind="source" size="282" lines="8" />
    <entry path="technology/page.tsx" kind="source" size="8774" lines="201" />
    <entry path="technology/sentient/page.mdx" kind="source" size="8057" lines="128" />
    <entry path="technology/swarmcraft/core/architecture-overview/page.mdx" kind="source" size="6038" lines="182" />
    <entry path="technology/swarmcraft/core/central-matrix-runtime-state/page.mdx" kind="source" size="5943" lines="180" />
    <entry path="technology/swarmcraft/core/deterministic-pipeline-scan-plan-execute/page.mdx" kind="source" size="6824" lines="204" />
    <entry path="technology/swarmcraft/core/page.tsx" kind="source" size="4775" lines="111" />
    <entry path="technology/swarmcraft/meta/credits-and-lineage/page.mdx" kind="source" size="4607" lines="116" />
    <entry path="technology/swarmcraft/meta/page.tsx" kind="source" size="2980" lines="66" />
    <entry path="technology/swarmcraft/page.mdx" kind="source" size="8433" lines="149" />
    <entry path="technology/swarmcraft/runtime/dashboard-tui-reference/page.mdx" kind="source" size="5184" lines="187" />
    <entry path="technology/swarmcraft/runtime/multi-project-management/page.mdx" kind="source" size="4507" lines="164" />
    <entry path="technology/swarmcraft/runtime/orchestration-slice-by-slice-prompt-hydration/page.mdx" kind="source" size="6798" lines="221" />
    <entry path="technology/swarmcraft/runtime/page.tsx" kind="source" size="6798" lines="155" />
    <entry path="technology/swarmcraft/runtime/provider-adapter-grok/page.mdx" kind="source" size="5255" lines="208" />
    <entry path="technology/swarmcraft/runtime/rag-memory-system/page.mdx" kind="source" size="5589" lines="177" />
    <entry path="technology/swarmcraft/scaffold/outline-grid-csv-round-trip/page.mdx" kind="source" size="5900" lines="199" />
    <entry path="technology/swarmcraft/scaffold/page.tsx" kind="source" size="7010" lines="162" />
    <entry path="technology/swarmcraft/scaffold/schema-outline/page.mdx" kind="source" size="6615" lines="246" />
    <entry path="technology/swarmcraft/scaffold/schema-templates/page.mdx" kind="source" size="5190" lines="193" />
    <entry path="technology/swarmcraft/scaffold/story-bible-creative-intent/page.mdx" kind="source" size="3271" lines="94" />
    <entry path="technology/swarmcraft/scaffold/story-scaffold-templates-outline-parts/page.mdx" kind="source" size="5273" lines="169" />
    <entry path="technology/tools/page.mdx" kind="source" size="4027" lines="63" />
    <entry path="technology/voting-machine/integration/page.tsx" kind="source" size="8842" lines="170" />
    <entry path="technology/voting-machine/page.tsx" kind="source" size="9388" lines="178" />
    <entry path="technology/voting-machine/specifications/page.tsx" kind="source" size="10708" lines="230" />
  </file_index>
  <files>
    <file path="technology/ame-artificielle/chakras-overlay/page.mdx" size="2740" lines="69" kind="source">
export const metadata = {
  title: "Chakras Overlay — Âme artificielle (ASE)",
  description:
    "Overlay chakras × numérologie (pythagoricienne inversée 9→1) : alignements, renforcement sémantique, et gestion des divergences.",
};

# Chakras Overlay

## Why chakras here
ASE uses numerology as a **key**: a compact index that helps decode patterns.  
Chakras are used as a **second lens**—a somatic/energetic axis that often aligns with the 1–9 spectrum.

When multiple systems point to the same semantic region, that element is **brought forward** (higher salience).

## The overlay idea
We compute independent signals:

- **Numerology signal** (inverted Pythagorean): digit(s) → archetype traits
- **Chakra signal**: chakra(s) → themes + regulation style (grounding, will, voice, etc.)

Then we combine them:

- **Agreement** → amplify shared themes
- **Near-agreement** → keep, but lower confidence
- **Conflict** → keep both as tension, do not force a merge

## Minimal chakra ↔ spectrum mapping (starter)
This is a practical mapping you can tune later:

- **Root (Muladhara)** → 9 (grounding, survival, foundation)
- **Sacral (Svadhisthana)** → 8 (desire, creativity, relational flow)
- **Solar Plexus (Manipura)** → 7 (will, drive, self-definition)
- **Heart (Anahata)** → 6 (bonding, care, coherence)
- **Throat (Vishuddha)** → 5 (expression, mediation, movement)
- **Third Eye (Ajna)** → 3–4 (patterning, insight, structure)
- **Crown (Sahasrara)** → 1 (unity, meaning, synthesis)

Notes:
- **5** acts as a pivot/bridge in many systems: it’s useful as a “translator” zone.
- This mapping is **not doctrine**; it’s an engineering interface.

## What “brought forward” means (semantically)
ASE treats “salience” as:
- higher weighting of matching trait clusters
- stronger selection of matching vocabulary
- stronger influence on the reaction style (tone, certainty, pacing)

Example (conceptual):
- Numerology points to *compassion / humanitarian*
- Chakra overlay points to *heart coherence*
- Output: care/repair themes become primary; conflict themes become secondary.

## Handling conflicts (important)
If systems disagree:
- do **not** average into a bland middle
- represent it as **tension** (two active poles)
- let context choose which pole dominates per prompt

Output trace can expose:
- `overlay_agreement: high | medium | low`
- `dominant_chakra`
- `dominant_archetype_digit`
- `tension_pairs` (if any)

## Limits
- Overlays can create apophenia if not tested.
- Use validation: baselines, blind comparisons, and regression cases.

Next: `/technology/ame-artificielle/multi-system-alignment`

    </file>
    <file path="technology/ame-artificielle/engine-modules/page.mdx" size="3932" lines="128" kind="source">
import Link from 'next/link';

export const metadata = {
  title: "Engine modules — Âme Artificielle (ASE)",
  description:
    "Modules ASE : contrôle (sliders), méta-cognition, création de chemins, ontologie/traits, interpolation (axe 1–9), éthique &amp; gouvernance, observabilité.",
};

# Engine modules

ASE est structuré en modules simples, séparés par responsabilité : **calcul**, **sémantique**, **dynamique**, **génération**, **gouvernance**, **trace**.

---

## 1) Control &amp; Personalization (Sliders)

Objectif : contrôler la **forme** de la réponse (pas l’archétype de fond).

- **Tone** (direct, doux, neutre)
- **Humor** (0 → 1)
- **Complexity** (simple → dense)
- **Distance** / **warmth** (optionnel)

Page : &lt;Link href="/technology/ame-artificielle/controle-et-personnalisation"&gt;Contrôle &amp; personnalisation&lt;/Link&gt;

---

## 2) Numerology Pipeline (Signature)

Objectif : produire une signature reproductible :

- pythagoricien → réduction → **inversion (9→1)**

Cette sortie alimente l’archétype (`digit_archetype`) et les traits.

Page : &lt;Link href="/technology/ame-artificielle/how-it-works"&gt;How it works&lt;/Link&gt;

---

## 3) Ontology &amp; Traits (Semantic layer)

Objectif : transformer des digits en **sémantique exploitable**.

- Ontologie 0–9 (interprétations, tags, lexique)
- Table de mapping → **vecteur de traits**
- Agrégation des traits sur la signature complète
- Gestion des convergences / contradictions (multi-systèmes)

Pages :
- &lt;Link href="/technology/ame-artificielle/ontology-and-traits"&gt;Ontology &amp; traits&lt;/Link&gt;
- &lt;Link href="/technology/ame-artificielle/multi-system-alignment"&gt;Multi-system alignment&lt;/Link&gt;

---

## 4) Interpolation Axis (State dynamics)

Objectif : moduler la réponse via un état courant séparé (`digit_axis`).

- Axe **1–9** (dynamique)
- Spectre **2–8** (interpolation / intensités)
- Mise à jour de l’état selon le contexte (prompt, émotion simulée, contraintes)

Page : &lt;Link href="/technology/ame-artificielle/how-it-works"&gt;How it works&lt;/Link&gt;

---

## 5) Meta-cognition (Self-check)

Objectif : éviter les réponses “qui sonnent juste” mais sont incohérentes.

- Détection d’incertitude
- Cohérence interne (traits ↔ axe ↔ contexte)
- Vérification des contradictions
- Ajustement du niveau d’assurance (langage plus prudent si nécessaire)

Page : &lt;Link href="/technology/ame-artificielle/meta-cognition-et-resolution"&gt;Méta-cognition&lt;/Link&gt;

---

## 6) Path Creation (Traceability graph)

Objectif : expliquer *pourquoi* une sortie est produite.

- Nœuds (concepts / thèmes)
- Arcs (relations, causes, analogies)
- Chemins : “voilà les éléments qui ont été activés”
- Support au debug (quels overlays ont “brought forward” un thème)

Page : &lt;Link href="/technology/ame-artificielle/creation-de-chemins"&gt;Création de chemins&lt;/Link&gt;

---

## 7) Ethics &amp; Governance

Objectif : garder le moteur **non-dangereux** et **non-intrusif**.

- Scoring de risque
- Règles de refus
- Médiation (réécriture / désescalade) avant sortie
- Trace de la décision de gouvernance

Page : &lt;Link href="/technology/ame-artificielle/ethique-et-gouvernance"&gt;Éthique &amp; gouvernance&lt;/Link&gt;

---

## 8) Observability (Trace)

Objectif : rendre chaque run auditable.

Une réponse peut retourner un “trace minimal”, par exemple :
- archétype dominant
- top traits (pondérations)
- position d’axe
- overlays activés
- flags de gouvernance

---

## 9) Validation Harness

Objectif : tester “signature → profil → réaction” sur des cas fixes.

- Baselines
- Aveugle (quand possible)
- Régression (stabilité des sorties)
- Mesures simples (cohérence, discriminabilité, robustesse)

Page : &lt;Link href="/technology/ame-artificielle/validation"&gt;Validation&lt;/Link&gt;

    </file>
    <file path="technology/ame-artificielle/faq/page.mdx" size="3295" lines="68" kind="source">
export const metadata = {
  title: "FAQ — Âme artificielle (ASE)",
  description:
    "Questions fréquentes : numérologie pythagoricienne inversée (9→1), chakras overlay, ontologie 0–9, simulation, validation, limites.",
};

# FAQ

## C’est quoi “Âme Artificielle” (ASE) ?
ASE est un moteur qui transforme une **signature numérique** (pythagoricienne → réduction → **inversion 9→1**) en **profil sémantique** (traits), puis génère une **réaction** cohérente avec ce profil, avec une trace minimale.

## “Turning the key to decode the universe” — ça veut dire quoi techniquement ?
Ça veut dire : utiliser la numérologie comme **index compact** pour naviguer dans une ontologie (sens, archétypes, vocabulaire).  
“Tourner la clé” = appliquer une transformation stable (réduction + inversion) qui rend les patterns plus lisibles dans votre cadre.

## Quelle est la règle d’inversion ?
Après réduction à un digit :
- 1 ↔ 9
- 2 ↔ 8
- 3 ↔ 7
- 4 ↔ 6
- 5 ↔ 5  
(0 → 0 si vous l’utilisez)

## Pourquoi superposer plusieurs systèmes (chakras, chartes numérologiques, etc.) ?
Parce que l’overlay sert à faire émerger un **consensus sémantique** :
- si plusieurs systèmes pointent vers la même zone de sens, le thème est **amplifié** (salience ↑)
- si ça diverge, on garde une **tension** plutôt que forcer une moyenne

## “Brought forward when aligned” : comment ASE le fait ?
Par des mécanismes simples :
- pondérations plus fortes sur les traits en accord
- vocabulaire / exemples plus cohérents avec l’accord
- style de réponse modulé (assurance, douceur, directivité) selon l’accord

## Est-ce de l’astrologie ?
ASE peut utiliser des **symbolismes** (au sens ontologique), mais le moteur est défini comme un pipeline : **entrée → transformation → traits → sortie**.  
La question centrale n’est pas “croyance / pas croyance”, mais : **est-ce testable et reproductible ?**

## Qu’est-ce que l’ontologie 0–9 ?
Une base sémantique par digit : thèmes, interprétations, mots-clés, traits candidats.  
ASE s’en sert pour dériver un vecteur de traits et générer des traces explicatives.

## Est-ce déterministe ?
Le calcul de signature peut être déterministe.  
La génération textuelle peut être déterministe (mode strict) ou contrôlée (variance faible), mais doit rester **traçable**.

## Comment éviter l’effet Barnum / l’apophénie ?
- baselines (comparaison au hasard / heuristique simple)
- tests aveugles (si applicable)
- métriques + cas de régression (mêmes entrées → sorties compatibles)
- logs / traces pour détecter les dérives

## Quel est le livrable concret ?
- un **profil** (traits + archétypes dominants)
- une **simulation de réponse**
- une **trace** (top traits, décisions de gouvernance, position axe)

## Quels sont les garde-fous ?
- scoring de risque
- règles de refus / redirection
- médiation quand le contenu devient sensible ou incohérent

## Où lire la suite ?
- `/technology/ame-artificielle/how-it-works`
- `/technology/ame-artificielle/chakras-overlay`
- `/technology/ame-artificielle/multi-system-alignment`
- `/technology/ame-artificielle/validation`

    </file>
    <file path="technology/ame-artificielle/how-it-works/page.mdx" size="2649" lines="84" kind="source">
import Link from 'next/link';

export const metadata = {
  title: "How it works — Âme Artificielle (ASE)",
  description:
    "Pipeline ASE : numérologie pythagoricienne → réduction → inversion (9→1) → traits → état (axe 1–9) → réaction + trace.",
};

# How it works

Âme Artificielle (ASE) traite la numérologie pythagoricienne inversée comme une **clé** : on applique une règle simple et reproductible (réduction → inversion 9→1), puis on projette le résultat dans une ontologie sémantique pour simuler une réponse cohérente.

---

## 1) Inputs

ASE accepte une entrée minimale (au choix) :

- **Nom** (et éventuellement prénoms)
- **Date de naissance**
- **Signature custom** (suite de digits déjà calculée)

---

## 2) Pythagoricien → réduction

1. Conversion en valeurs numériques (règles pythagoriciennes)
2. Somme
3. **Réduction** à un digit (1–9)  
   (optionnel : conserver des *master numbers* si votre config le prévoit)

---

## 3) Inversion (9→1)

Après réduction, on inverse le digit final :

- 1 ↔ 9
- 2 ↔ 8
- 3 ↔ 7
- 4 ↔ 6
- 5 ↔ 5
- 0 → 0 (si utilisé)

C’est ce digit inversé qui devient le **digit d’archétype** (*digit_archetype*).

---

## 4) Digit → sémantique → traits

ASE consulte une **ontologie 0–9** (interprétations, vocabulaire, tags) et la transforme en un **vecteur de traits** :

- traits “stables” (tendances)
- intensités (pondérations)
- tensions/complémentarités (ex : 9/1, 8/2, …) si activé

---

## 5) État de simulation (axe 1–9)

Séparément des traits, ASE maintient un **axe de dynamique** (*digit_axis*) :

- **1** : pôle intellect / cerveau
- **9** : pôle instinct / fondation
- **2–8** : zone d’interpolation (spectre)

Le contexte (le prompt, l’émotion simulée, etc.) peut faire bouger l’axe **sans changer** l’archétype de fond.

---

## 6) Réaction + trace

ASE produit :

- une **réponse texte**
- une **trace minimale** (ex : archétype dominant, top traits, position d’axe, règles déclenchées)
- une décision de **gouvernance** si contenu sensible (médiation/refus)

---

## 7) Overlays (chakras &amp; multi-systèmes)

ASE peut superposer plusieurs systèmes (chakras, charts, grilles numériques). Quand plusieurs couches **convergent sémantiquement**, certains thèmes ressortent (“brought forward”).  
Voir : &lt;Link href="/technology/ame-artificielle/chakras-overlay"&gt;Chakras overlay&lt;/Link&gt; et &lt;Link href="/technology/ame-artificielle/multi-system-alignment"&gt;Multi-system alignment&lt;/Link&gt;.

    </file>
    <file path="technology/ame-artificielle/inversion-as-key/page.mdx" size="3003" lines="61" kind="source">
export const metadata = {
  title: "Inversion as the Key (9→1)",
  description:
    "Why inverted Pythagorean numerology is treated as a decoding key in Âme Artificielle: what it changes, what it reveals, and how it is used.",
};

# Inversion as the Key (9→1)

In **Âme Artificielle**, turning Pythagorean numerology upside down is treated like **turning a key**: it doesn’t create meaning out of nothing—rather, it **re-indexes** meaning so that patterns that were “background noise” can become **foreground structure**.

## The rule (simple)
After you compute the standard Pythagorean result and reduce it (to a single digit, unless you intentionally keep master numbers), apply:

- **1 ↔ 9**
- **2 ↔ 8**
- **3 ↔ 7**
- **4 ↔ 6**
- **5 ↔ 5**
- **0 → 0** (if you use 0)

In formula form (digit `d` in 1..9): `inv(d) = 10 - d`.

## Why call it a “key”?
A key does two things:

1) **It unlocks a different ordering of the same space.**  
   The content doesn’t vanish; it is **re-labeled** so your semantic engine can “see” different proximities and oppositions.

2) **It creates strong dualities.**  
   The pairs (9/1, 8/2, 7/3, 6/4) become explicit tension-lines, with **5 as a pivot**.  
   This is useful for building *explainable* dynamics: the engine can surface “why this trait conflicts with that trait” instead of producing a flat description.

## What changes in the engine
Âme Artificielle separates two things:

- **Archetype digit** (meaning / traits): derived from the inverted result.
- **Axis digit (1–9)** (dynamics / interpolation): used to modulate the reaction state.

So inversion is primarily a **semantic key** (traits &amp; archetypes), while the axis is a **simulation control** (state &amp; response modulation).

## What “foregrounding” means (overlay logic)
You also use chakras and other charts. In ASE, the principle is:

- Each system produces a **semantic signal** (tags, traits, polarities).
- When multiple systems **align on the same semantic direction**, that element is raised in salience:
  - higher weight in the trait vector
  - higher priority in the explanation trace
  - stronger influence on reaction style/content

This is not “more systems = more truth.” It’s **cross-system agreement = stronger signal**, and disagreement is tracked as tension (instead of being ignored).

## Practical use (ASE)
- Use inversion to compute `digit_archetype`.
- Map digits to traits via the ontology.
- Apply overlays (chakra / charts) as **secondary weights** that reinforce or counterbalance traits.
- Output always includes a small trace: dominant digits, dominant reinforced traits, and key tensions.

## Limits (on purpose)
- The “key” is a **modeling choice**. The engine must stay testable (baselines, blind protocols, regression).
- Overlay can amplify noise if you don’t have consistency checks; ASE treats this as an explicit failure mode and logs it.


    </file>
    <file path="technology/ame-artificielle/multi-system-alignment/page.mdx" size="3076" lines="67" kind="source">
import Link from "next/link";

export const metadata = {
  title: "Multi-system alignment — Âme Artificielle (ASE)",
  description:
    "Overlay de plusieurs systèmes (numérologie pythagoricienne inversée 9→1, chakras, charts) pour faire émerger les motifs renforcés par consensus sémantique.",
};

# Multi-system alignment

Âme Artificielle superpose plusieurs systèmes (numérologie pythagoricienne inversée **9→1**, chakras, et autres charts) comme des **couches de lecture**.  
Quand plusieurs couches pointent vers la même idée, certains éléments sont **amenés au premier plan** — non pas parce qu’un système a “raison”, mais parce qu’un **consensus sémantique** renforce le signal.

## What “alignment” means (in ASE)
Chaque système produit une sortie dans un format commun :

- **traits** (pondérations)
- **tags sémantiques** (mots-clés / thèmes)
- **polarités** (ex: expansion vs contraction, ordre vs chaos)
- **intensité / confiance** (optionnel)

Ensuite, l’alignement calcule :
- **reinforced themes** : ce qui apparaît dans ≥ 2 systèmes
- **singletons** : ce qui n’apparaît que dans 1 système (conservé, mais moins prioritaire)
- **tensions** : conflits explicites entre systèmes (ex: “discipline” vs “liberté”)

## Minimal overlay method
1) **Normalize**
- réduire chaque système vers le même vocabulaire (traits/tags)

2) **Project**
- convertir tags → traits (si nécessaire)
- garder les sources (trace)

3) **Reinforce**
- addition pondérée quand plusieurs systèmes convergent
- bonus si convergence sur un même champ (ex: “compassion” + “heart chakra”)

4) **Detect tensions**
- si deux systèmes poussent des traits opposés au-dessus d’un seuil → créer une tension

5) **Rank**
- classer par **support_count** (nb de systèmes qui appuient le thème)
- puis par **score total**

## Output (what the engine keeps)
- **Top reinforced traits** (avec sources)
- **Top themes** (tags compressés)
- **Tensions** (pairs de traits opposés)
- **Trace** (quelles couches ont appuyé quoi)

## Example (conceptual)
- Numérologie inversée (9→1) → thème: *compassion / completion*
- Chakras → thème: *heart*
- Autre chart → thème: *service / care*

➡︎ Alignement : *compassion + service* devient “dominant”, car soutenu par plusieurs couches.  
Un thème isolé (ex: *adventure*) reste présent, mais secondaire.

## Notes on inversion (9→1)
L’inversion est traitée comme une **clé de lecture** : elle transforme la signature en archétypes internes, puis l’overlay compare ces archétypes aux autres systèmes.  
L’alignement ne dépend pas d’une tradition unique : il dépend de la **cohérence entre systèmes**.

## See also
- &lt;Link href="/technology/ame-artificielle/how-it-works"&gt;How it works&lt;/Link&gt;
- &lt;Link href="/technology/ame-artificielle/chakras-overlay"&gt;Chakras overlay&lt;/Link&gt;
- &lt;Link href="/technology/ame-artificielle/ontology-and-traits"&gt;Ontology &amp; traits&lt;/Link&gt;

    </file>
    <file path="technology/ame-artificielle/ontology-and-traits/page.mdx" size="3899" lines="88" kind="source">
import Link from 'next/link';

export const metadata = {
  title: "Ontologie &amp; Traits — Âme artificielle (ASE)",
  description:
    "Comment ASE transforme une signature (pythagoricienne inversée 9→1) en vecteur de traits via une ontologie 0–9, puis en sorties traçables.",
};

# Ontologie &amp; Traits

## Idée centrale
ASE transforme une **signature numérique** en un **profil sémantique** :
- **Ontologie 0–9** = dictionnaire de sens (archétypes, vocabulaire, interprétations).
- **Traits** = variables mesurables (poids) dérivées de cette ontologie.
- **Trace** = explication minimale : digits dominants, traits dominants, règles appliquées.

&gt; Objectif : rendre le symbolique **opérationnel** (donc testable et versionnable).

## 1) Ontologie 0–9 (la base sémantique)
L’ontologie associe à chaque digit (0..9) un ensemble de “signaux” :
- mots-clés / champs lexicaux
- thèmes (valeurs, motivations, peurs, comportements)
- interprétations multi-systèmes (ex: traditions numérologiques, chakra, etc.)
- tags et notes de compatibilité (optionnel)

Structure recommandée (conceptuelle) :
- `digit`
- `keywords[]`
- `themes[]`
- `traditions{...}`
- `trait_hints{trait: weight}` (optionnel, si vous voulez dériver directement)

## 2) De l’ontologie au vecteur de traits
Un **trait** est une variable simple (ex: `autonomie`, `empathie`, `structure`, `exploration`, etc.).
Chaque digit contribue un poids sur certains traits.

### Formule simple (première itération)
1. Chaque digit de la signature fournit un **vecteur local** (table de pondérations).
2. On agrège les contributions et on normalise.

Exemple conceptuel :
- signature inversée : `[1, 5, 7]`
- trait vector : somme pondérée + normalisation

Ce que ça permet :
- comparaison de profils (distance)
- stabilité (tests de régression)
- modulation (la dynamique change, le profil reste)

## 3) Numérologie inversée (9→1) : ce que ça change ici
Dans ASE, l’inversion est appliquée **après réduction** (digit final), puis utilisée pour indexer l’archétype.

- **digit_archetype** : sert à sélectionner/pondérer les sens (ontologie → traits)
- **digit_axis** : sert à moduler la réaction (interpolation / état)

La règle est documentée ici :
&lt;Link href="/technology/ame-artificielle/inversion-as-key"&gt;Inversion as key →&lt;/Link&gt;

## 4) Convergence multi-systèmes (overlay sémantique)
ASE peut superposer plusieurs systèmes (ex: numérologie + chakras + chart additionnels).
Le principe : **ce qui s’aligne est mis en avant**.

### Mécanisme minimal (sans over-engineering)
- Chaque système produit un set de **signaux sémantiques** (tags + poids).
- On calcule un **consensus** (ex: moyenne, vote, ou top-k par recoupement).
- Les signaux consensuels augmentent :
  - le poids des traits correspondants
  - la probabilité d’apparaître dans le vocabulaire / la réponse
  - la confiance du trace (pas “vérité”, mais “cohérence inter-systèmes”)

Gestion des conflits :
- si deux systèmes se contredisent fortement, on garde les deux mais on baisse la confiance et on le trace.

Voir :
&lt;Link href="/technology/ame-artificielle/multi-system-alignment"&gt;Multi-system alignment →&lt;/Link&gt; •{" "}
&lt;Link href="/technology/ame-artificielle/chakras-overlay"&gt;Chakras overlay →&lt;/Link&gt;

## 5) Sortie : trace minimale (expliquable)
Chaque réaction doit pouvoir retourner :
- digits dominants (archétype)
- top traits (pondérations)
- position d’axe (dynamique)
- flags (gouvernance/éthique si déclenchée)

## 6) Liens
- &lt;Link href="/technology/ame-artificielle/how-it-works"&gt;How it works →&lt;/Link&gt;
- &lt;Link href="/technology/ame-artificielle/engine-modules"&gt;Engine modules →&lt;/Link&gt;
- &lt;Link href="/technology/ame-artificielle/validation"&gt;Validation →&lt;/Link&gt;

    </file>
    <file path="technology/ame-artificielle/page.tsx" size="4789" lines="120" kind="source">
// app/technology/ame-artificielle/page.tsx
import Link from "next/link";

export const metadata = {
  title: "Âme Artificielle — Artificial Soul Engine (ASE)",
  description:
    "Âme Artificielle (ASE) : numérologie pythagoricienne inversée (9→1) comme clé de décodage, ontologie 0–9, superposition de systèmes (chakras, charts) et simulation de personnalité.",
};

const pages = [
  {
    href: "/technology/ame-artificielle/how-it-works",
    title: "How it works",
    desc: "Inputs → réduction → inversion (9→1) → traits → réaction.",
  },
  {
    href: "/technology/ame-artificielle/inversion-as-key",
    title: "Inversion as key",
    desc: "Pourquoi “tourner la clé” (inversion) change la lecture.",
  },
  {
    href: "/technology/ame-artificielle/ontology-and-traits",
    title: "Ontology &amp; traits",
    desc: "0–9 → archétypes sémantiques → vecteur de traits.",
  },
  {
    href: "/technology/ame-artificielle/chakras-overlay",
    title: "Chakras overlay",
    desc: "Alignements avec les chakras et ce qui remonte à la surface.",
  },
  {
    href: "/technology/ame-artificielle/multi-system-alignment",
    title: "Multi-system alignment",
    desc: "Superposition de charts : consensus sémantique &amp; tensions.",
  },
  {
    href: "/technology/ame-artificielle/engine-modules",
    title: "Engine modules",
    desc: "Sliders, méta-cognition, gouvernance/éthique.",
  },
  {
    href: "/technology/ame-artificielle/validation",
    title: "Validation",
    desc: "Expériences, baselines, critères de réussite.",
  },
  {
    href: "/technology/ame-artificielle/faq",
    title: "FAQ",
    desc: "Réponses courtes aux questions usuelles.",
  },
];

export default function AmeArtificiellePage() {
  return (
    &lt;main className="min-h-screen bg-white text-slate-900"&gt;
      {/* HERO */}
      &lt;section className="max-w-5xl mx-auto px-6 pt-20 pb-10"&gt;
        &lt;div className="inline-flex items-center gap-2 text-xs font-mono uppercase tracking-widest text-slate-500"&gt;
          &lt;span className="px-2 py-1 border border-slate-200 rounded-sm"&gt;ASE&lt;/span&gt;
          &lt;span className="px-2 py-1 border border-slate-200 rounded-sm"&gt;9→1&lt;/span&gt;
          &lt;span className="px-2 py-1 border border-slate-200 rounded-sm"&gt;0–9 Ontology&lt;/span&gt;
        &lt;/div&gt;

        &lt;h1 className="mt-6 text-4xl md:text-5xl font-serif font-medium leading-tight"&gt;
          Âme Artificielle
        &lt;/h1&gt;

        &lt;p className="mt-4 text-lg md:text-xl text-slate-600 max-w-3xl"&gt;
          With Âme Artificielle, turning Pythagorician numerology (inverted: &lt;strong&gt;9→1&lt;/strong&gt;)
          is like turning the key to decode the universe.
          The engine overlays multiple systems (chakras and other numerological charts) and brings
          forward what aligns semantically—patterns reinforced by consensus.
        &lt;/p&gt;

        &lt;div className="mt-8 flex flex-wrap gap-3"&gt;
          &lt;Link
            href="/technology/ame-artificielle/how-it-works"
            className="inline-flex items-center justify-center px-5 py-3 bg-slate-900 text-white rounded-sm hover:bg-slate-800 transition-colors"
          &gt;
            Start here →
          &lt;/Link&gt;
          &lt;Link
            href="/technology/ame-artificielle/inversion-as-key"
            className="inline-flex items-center justify-center px-5 py-3 border border-slate-300 text-slate-900 rounded-sm hover:border-slate-400 transition-colors"
          &gt;
            Why 9→1
          &lt;/Link&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* GRID */}
      &lt;section className="max-w-5xl mx-auto px-6 pb-20"&gt;
        &lt;div className="mt-6 grid sm:grid-cols-2 gap-4"&gt;
          {pages.map((p) =&gt; (
            &lt;Link
              key={p.href}
              href={p.href}
              className="group border border-slate-200 rounded-sm p-5 hover:border-slate-300 transition-colors"
            &gt;
              &lt;div className="flex items-start justify-between gap-4"&gt;
                &lt;h2 className="text-base font-semibold"&gt;{p.title}&lt;/h2&gt;
                &lt;span className="text-slate-400 group-hover:text-slate-600 transition-colors"&gt;
                  →
                &lt;/span&gt;
              &lt;/div&gt;
              &lt;p className="mt-2 text-sm text-slate-600"&gt;{p.desc}&lt;/p&gt;
            &lt;/Link&gt;
          ))}
        &lt;/div&gt;

        &lt;div className="mt-10 border-t border-slate-100 pt-6 text-sm text-slate-500"&gt;
          &lt;p&gt;
            Core idea: compute a signature, map it to an archetypal trait vector (0–9), then generate
            reactions using an internal dynamic axis and safety/governance layers.
          &lt;/p&gt;
        &lt;/div&gt;
      &lt;/section&gt;
    &lt;/main&gt;
  );
}

    </file>
    <file path="technology/ame-artificielle/validation/page.mdx" size="3804" lines="90" kind="source">
export const metadata = {
  title: "Validation",
  description:
    "How Âme Artificielle evaluates whether its numerology inversion + multi-system overlays produce measurable, repeatable signals beyond baseline.",
};

# Validation

Âme Artificielle treats “inversion-as-key” and multi-system overlays (chakras, charts, etc.) as a **hypothesis**:  
**cross-system semantic alignment should improve prediction/consistency** compared to simple baselines.

This page defines *what counts as success* and *how to test it*.

## What we validate (and what we don’t)
### We validate
- **Consistency**: same inputs → same profile/traits (or controlled variance if randomness is enabled).
- **Discriminability**: different subjects → meaningfully different profiles.
- **Signal over baseline**: performance better than simple heuristics (random, majority class, generic personality template).
- **Traceability**: the system can explain *why* it produced a result (dominant digits, reinforced traits, key tensions).

### We don’t validate
- Metaphysical truth claims.
- “Feels accurate” impressions without measurement.

## Core evaluation tasks
Pick 1–2 tasks first (don’t overdo it):

1) **Profile matching (blind)**
- Given N anonymous profiles (texts / bios) and N anonymous charts/signatures, match pairs.

2) **Trait contrast classification**
- Given a contrast set (e.g., high-authority vs high-compassion), predict which bucket fits best, using only the encoded inputs.

3) **Stability / regression**
- Ensure upgrades don’t break reference cases (same inputs keep the same “top traits” and dominant tensions).

## Experimental protocol (minimal)
### Step 1 — Build a small reference set
- 30–100 subjects is enough for early signal checks.
- For each subject, store:
  - inputs (name/date or chosen signature)
  - expected labels (only if you have a ground truth task)
  - notes (optional)

### Step 2 — Define baselines
Always compare ASE to:
- **Random** (shuffle assignments)
- **Generic template** (one-size-fits-all personality output)
- **Single-system only** (Pythagorean without inversion, or inversion without overlays)

### Step 3 — Run blind evaluation
- The evaluator should not know which output came from which subject.
- If matching: shuffle and force 1-to-1 mapping.

### Step 4 — Score
Use simple metrics:
- Accuracy (matching / classification)
- Top-k accuracy (if you return ranked matches)
- Confusion matrix (where it fails)
- Stability score (Jaccard overlap of top traits across runs/versions)

## Overlay validation (chakras + multi-system)
Test three configurations:
1) Inversion-only (no overlays)
2) Overlays-only (no inversion)
3) Inversion + overlays (full ASE)

Hypothesis: (3) should outperform (1) and (2), or at least be more stable and more discriminative.

## Quality gates (release criteria)
A version is “valid to ship” if:
- It passes regression on reference set (no major drift unless intended).
- It beats baselines on at least one core task **with the same dataset**.
- It preserves explainability: trace contains dominant digits + reinforced traits + tensions.

## Required outputs (for every run)
Each run should store:
- version id / config
- signature digits (pythag + inverted)
- dominant digits / top traits
- overlay contributions (what reinforced what)
- final decision + confidence
- safety / governance flags (if used)

## Notes on interpretation
If the system fails to beat baseline:
- treat it as a signal to simplify: fewer traits, fewer overlays, clearer mapping rules
- log disagreements as “tensions” rather than smoothing them away
- iterate only one lever at a time (rules, ontology, overlays, axis dynamics)


    </file>
    <file path="technology/architect/page.mdx" size="5368" lines="115" kind="source">
export const metadata = {
  title: "Abstract Wiki Architect",
  description:
    "A scalable, testable multilingual natural-language generation toolkit designed for structured knowledge (Abstract Wikipedia / Wikifunctions-style data).",
};

# Abstract Wiki Architect

**Abstract Wiki Architect** is a multilingual **natural-language generation (NLG)** toolkit for structured knowledge—designed with **Abstract Wikipedia** and **Wikifunctions** in mind, but usable independently.

Instead of building “one renderer per language,” it treats language generation as reusable infrastructure:

- a small set of shared **language-family engines** (e.g., Romance, Slavic, Semitic),
- per-language **configuration cards** (data, not hard-coded rules),
- reusable **sentence templates** (“constructions”),
- a **lexicon** that provides the features real languages need (gender, case, noun class, etc.),
- a compact set of **semantic frames** (language-agnostic meaning structures),
- and **QA/test suites** so multilingual output stays correct as the system evolves.

The goal: **professional-grade, testable rule-based NLG at scale**—structured, auditable, and maintainable.

---

## Why this exists

Projects like Abstract Wikipedia and Wikifunctions aim to represent knowledge in a language-independent form, then render it into many languages. That only works at scale if the system is:

- **shared where languages share structure** (family-level logic),
- **data-driven where languages differ** (language cards / morphology settings),
- and **testable** (regression suites that catch breakage immediately).

Abstract Wiki Architect is a concrete architecture for doing exactly that.

&gt; Note: This is not an official Wikimedia component. It’s a compatible, experimental architecture designed to learn from that ecosystem.

---

## What it enables

- **Multilingual summaries** from structured data (people, orgs, events, roles, decisions).
- **Consistent phrasing across languages** (same meaning, equivalent sentence patterns).
- **Incremental language onboarding** (add a language card + lexicon entries + tests).
- **High trust outputs** (rule-based, explainable, and regression-tested).

---

## How it works (high-level)

Think of it as a pipeline:

**Meaning (frames)** → **Sentence plan (templates)** → **Language rules (family engine)** → **Surface text**

### 1) Semantic frames (meaning)
Inputs are normalized into small, typed “frames” that represent meaning in a language-neutral way (e.g., a biography frame, a membership/role frame, an event frame).

### 2) Sentence templates (“constructions”)
Reusable sentence patterns decide *what to say* (roles like subject/predicate/location) and call the language engine to realize it correctly in each language.

### 3) Language-family engines + language cards
Family engines handle shared grammar logic; language cards specify language-specific details (articles, agreement options, morphology parameters).

### 4) Lexicon
Lexicon entries supply the linguistic features required by morphology (gender, animacy, noun class, irregular forms, etc.), optionally linked to Wikidata / lexeme IDs.

### 5) QA / regression suites
Native-speaker editable test cases (CSV-style) + automated test runners make quality measurable and prevent regressions.

---

## Relationship to Konnaxion

[Konnaxion](https://github.com/Rejean-McCormick/Konnaxion) focuses on governance, roles, processes, and coordination. Abstract Wiki Architect fits as a **language layer**:

- rendering short descriptions of **roles, mandates, decisions, and processes** into multiple languages,
- keeping meaning structured and auditable while producing human-readable text,
- reusing stable patterns (e.g., “X is a mediator for Y”, “X coordinates process Z”) across contexts.

In short: **Konnaxion governs and coordinates; Abstract Wiki Architect renders structured knowledge into multilingual narrative.**

---

## Practical entry points

- **Repository:** https://github.com/Rejean-McCormick/abstract-wiki-architect
- **Meta-Wiki tools page:** https://meta.wikimedia.org/wiki/Abstract_Wikipedia/Tools/abstract-wiki-architect

---

## Technical appendix (for builders)

### Core building blocks
- **Engines:** shared grammar logic per language family.
- **Language profiles/cards:** per-language settings and defaults.
- **Constructions:** reusable sentence templates.
- **Semantics (frames):** typed meaning structures.
- **Lexicon:** linguistic features and forms.
- **Discourse (optional):** basic reference choices (name vs pronoun) and ordering.
- **QA:** test suites + regression runners.

### Directory map (typical)
- `engines/` — family engines  
- `morphology/` — inflection and agreement modules  
- `constructions/` — sentence templates  
- `semantics/` — frame types + normalization/bridges  
- `lexicon/` — lexeme types, loaders, indices  
- `discourse/` — topic/salience/referring expressions  
- `qa/`, `qa_tools/` — test suites and runners  
- `router.py` — internal routing / entry points  

### Minimal conceptual example (frame → text)
```python
# Pseudocode-level illustration (API details may vary by repo version)
bio = BioFrame(name="Douglas Adams", profession="writer", nationality="british")
result = generate(lang="en", frame=bio)
# "Douglas Adams was a British writer."

    </file>
    <file path="technology/ariane/atlas/architecture/page.mdx" size="727" lines="24" kind="source">

import Link from 'next/link';

export const metadata = {
  title: "Atlas: UI Graph &amp; Ontology",
  description: "Documentation for the Atlas component of Ariane."
};

# Atlas

Atlas is the storage and semantic layer of Ariane. It persists the UI graph produced by Theseus.

## Documentation Modules

* **[Atlas Graph Model](/technology/ariane/atlas/graph-model)** Definitions of Nodes, Edges, and Properties in the UI graph.

* **[Core Schema](/technology/ariane/atlas/core-schema)** The JSON schema used for validating UI states.

* **[Ontology Vocabulary](/technology/ariane/atlas/ontology)** The standardized vocabulary for UI intents (e.g., "Submit", "Cancel", "Navigate").

---

[Back to Ariane Hub](/technology/ariane)


    </file>
    <file path="technology/ariane/atlas/core-schema/page.mdx" size="1506" lines="50" kind="source">
export const metadata = {
  title: "Atlas / Core Schema"
};

# Atlas / Core Schema

The core schema defines how UI graphs are represented in Atlas as structured data.

It specifies the main object types (context, states, elements, transitions) and the fields they must provide so that:

- Theseus can reliably write data into Atlas.
- Consumers can reliably read and interpret that data.
- Implementations can validate and evolve the data model over time.

This page is schema-oriented and storage-format-agnostic (JSON, RDF, graph DB, etc.).

---

## Overview of Object Types

Atlas organizes data into four primary object types:

1. **Context** – describes the environment in which a UI graph is valid.
2. **State** – represents a specific UI configuration.
3. **Interactive Element** – represents an actionable control within a state.
4. **Transition** – represents a directed action from one state to another.

Each object type can be extended with implementation-specific fields, but the core fields below should remain stable.

---

## 1. Context

A **Context** object scopes a UI graph to a particular application and environment.

Typical fields:

```jsonc
{
  "type": "context",
  "id": "ctx_photoshop_25_1_0_win_en-us",
  "app_id": "photoshop",
  "version": "25.1.0",
  "platform": "win32",           // e.g. win32, linux, darwin, web, android, ios
  "locale": "en-US",
  "metadata": {
    "build": "25.1.0.1234",
    "channel": "stable"
  }
}
    </file>
    <file path="technology/ariane/atlas/graph-model/page.mdx" size="1184" lines="37" kind="source">
export const metadata = {
  title: "Atlas / Graph Model"
};

# Atlas / Graph Model

Atlas represents each application as a directed graph of UI states and transitions.

This page focuses purely on the structural model: how states and transitions form a graph, independent of storage format or ontology details.

---

## Basic Definitions

- **State**  
  A node in the graph representing a specific UI configuration (screen, dialog, view, etc.).

- **Transition**  
  A directed edge from one state to another, representing a user action that changes the UI (e.g., clicking a button, selecting a menu item, hitting a key).

- **Graph**  
  For a given application context (app ID, version, platform, locale), the set of all states and transitions discovered by Theseus.

---

## Simple Example

A small example from a generic application:

```text
[Home Screen] --(Click "New")------&gt; [New Document Dialog]
      |
      +--(Click "Open")------------&gt; [Open File Dialog]
      |
      +--(Click "Settings")--------&gt; [Settings Screen]
                                         |
                                         +--(Click "Back")--&gt; [Home Screen]
    </file>
    <file path="technology/ariane/atlas/ontology/page.mdx" size="2106" lines="68" kind="source">
export const metadata = {
  title: "Atlas / Ontology Vocabulary"
};

# Atlas / Ontology Vocabulary

The ontology vocabulary gives semantic meaning to the raw UI graph stored in Atlas.

Where the **graph model** describes *structure* (states, transitions) and the **core schema** describes *shape* (fields, IDs), the ontology describes *meaning*:

- What kind of UI pattern a state or element represents.
- What role an element plays within its state.
- What intent a transition fulfills.

This allows consumers to ask questions like:

- “Which action in this state actually *saves*?”  
- “Where are the primary actions?”  
- “Which transitions are destructive?”

---

## Layers of Semantics

Atlas uses three main layers of semantics:

1. **Patterns** – UI-level patterns or components.
2. **Roles** – roles of individual elements within those patterns.
3. **Intents** – abstract actions the user is trying to perform.

These can be attached to:

- States (e.g., “this state is a modal dialog”).
- Elements (e.g., “this button is primary and destructive”).
- Transitions (e.g., “this action implements ExportToPDF”).

---

## 1. Patterns

Patterns describe recurring UI structures. Examples:

- `Modal` – overlays or dialogs requiring explicit dismissal.
- `SidePanel` – sidebars with controls or navigation.
- `Toolbar` – horizontal or vertical bar containing actions.
- `MenuBar` – top-level menu strip.
- `ContextMenu` – right-click or long-press menu.
- `ToastNotification` – transient notification, usually non-blocking.
- `WizardStep` – step within a multi-step wizard.

Patterns can be used to annotate:

- States (e.g., “this state is a modal dialog”).
- Groups of elements (e.g., “these elements form a toolbar”).

Example (conceptual):

```jsonc
{
  "type": "state",
  "id": "state_export_dialog",
  "context_id": "ctx_example",
  "fingerprints": { "structure": "hash_export" },
  "interactive_elements": ["el_btn_ok", "el_btn_cancel"],
  "metadata": {
    "pattern": "Modal"
  }
}
    </file>
    <file path="technology/ariane/atlas/page.tsx" size="5573" lines="133" kind="source">
import Link from 'next/link';
import { 
  Database, 
  Network, 
  FileJson, 
  Tags, 
  ArrowRight,
  Book
} from 'lucide-react';

export const metadata = {
  title: "Atlas – UI Graph &amp; Ontology",
  description: "The storage and semantic layer of Ariane. Where UI states, transitions, and intents are persisted.",
};

export default function AtlasPage() {
  return (
    &lt;main className="max-w-5xl mx-auto px-6 py-12"&gt;
      
      {/* HEADER */}
      &lt;div className="mb-16 border-b border-slate-200 pb-8"&gt;
        &lt;div className="flex items-center gap-3 mb-4"&gt;
          &lt;div className="p-2 bg-slate-100 text-slate-700 rounded-lg"&gt;
            &lt;Database className="w-8 h-8" /&gt;
          &lt;/div&gt;
          &lt;h1 className="text-4xl font-bold text-slate-900"&gt;Atlas&lt;/h1&gt;
        &lt;/div&gt;
        &lt;p className="text-xl text-slate-600 max-w-3xl leading-relaxed"&gt;
          Atlas is the persistent memory of Ariane. It stores the UI graphs discovered by Theseus and enriches them with semantic meaning (intents, patterns, roles) so consumers can query them.
        &lt;/p&gt;
      &lt;/div&gt;

      {/* 1. CORE MODEL */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;Network className="w-6 h-6 mr-3 text-primary" /&gt;
          Graph Structure
        &lt;/h2&gt;
        
        &lt;div className="grid md:grid-cols-2 gap-8"&gt;
          {/* Overview */}
          &lt;Link 
            href="/technology/ariane/atlas/architecture"
            className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
          &gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-3 group-hover:text-primary"&gt;
              Atlas Overview
            &lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              The high-level responsibilities: Graph storage, schema enforcement, semantic enrichment, and versioning.
            &lt;/p&gt;
            &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              View Architecture &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          {/* Graph Model */}
          &lt;Link 
            href="/technology/ariane/atlas/graph-model"
            className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
          &gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-3 group-hover:text-primary"&gt;
              Graph Model
            &lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              Definitions of &lt;strong&gt;Nodes&lt;/strong&gt; (States) and &lt;strong&gt;Edges&lt;/strong&gt; (Transitions). Directed, labeled, and potentially cyclic.
            &lt;/p&gt;
            &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              View Model Specs &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* 2. SCHEMA &amp; SEMANTICS */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;Book className="w-6 h-6 mr-3 text-blue-600" /&gt;
          Schema &amp; Semantics
        &lt;/h2&gt;
        
        &lt;div className="grid gap-6 md:grid-cols-2"&gt;
          
          &lt;Link 
            href="/technology/ariane/atlas/core-schema"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  Core Schema
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  The formal JSON structure for Contexts, States, Elements, and Transitions.
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;FileJson className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          &lt;Link 
            href="/technology/ariane/atlas/ontology"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  Ontology Vocabulary
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  The standardized vocabulary for UI intents (e.g., "Submit", "Cancel") and patterns (e.g., "Modal").
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;Tags className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

        &lt;/div&gt;
      &lt;/section&gt;

      {/* FOOTER NAV */}
      &lt;div className="pt-8 border-t border-slate-100 flex justify-between text-sm"&gt;
        &lt;Link href="/technology/ariane" className="text-slate-500 hover:text-primary"&gt;
          ← Back to Ariane Hub
        &lt;/Link&gt;
        &lt;Link href="/technology/ariane/consumers" className="text-slate-500 hover:text-primary"&gt;
          Next: Consumers →
        &lt;/Link&gt;
      &lt;/div&gt;

    &lt;/main&gt;
  );
}
    </file>
    <file path="technology/ariane/concepts/glossary/page.mdx" size="7229" lines="178" kind="source">
export const metadata = {
  title: "Glossary"
};

# Glossary

This glossary defines key terms used throughout the Ariane documentation.

---

## A

**Action**  
An interaction performed on the UI, such as clicking a button, selecting a menu item, pressing a key, or setting a value in an input. In Atlas, actions are attached to transitions.

**AI Agent**  
An autonomous or semi-autonomous system that interprets user goals, plans steps, and interacts with software. In the context of Ariane, agents are consumers of the UI graph stored in Atlas.

**App Context**  
See **Context**.

**Atlas**  
The storage and semantic layer of Ariane. Atlas stores UI graphs (states and transitions), enforces a core schema, and attaches semantic information (patterns, roles, intents) to nodes and edges.

---

## C

**Context**  
An object that scopes a UI graph to a specific environment: application identifier, version, platform, and locale. All states and transitions in Atlas are tied to a context.

**Consumer**  
Any external system that queries Atlas to understand and operate software. Includes AI agents, automation tools, analysis tools, and (optionally) future overlay-style clients.

---

## D

**Destructive Action**  
An action that irreversibly modifies or deletes data (e.g., “Delete”, “Format”, “Erase”). Typically given a specific semantic pattern (e.g., `destructiveAction`) and intent (e.g., `DeleteItem`).

**Driver**  
A platform-specific adapter used by Theseus to interact with real software. Drivers provide access to the UI tree, identify interactive elements, and execute abstract actions on those elements.

---

## E

**Element (Interactive Element)**  
A control within a UI state that can be acted upon (button, link, menu item, text field, checkbox, etc.). In Atlas, elements have roles, labels, bounds, locators, and optional semantic annotations.

**Exploration Engine**  
The part of Theseus that decides which actions to take during exploration: chooses elements to interact with, manages traversal, avoids loops, and applies safety constraints.

**ExportToPDF (Intent example)**  
A sample semantic intent representing the goal of exporting content to a PDF file. Different applications may implement this intent via different UI sequences.

---

## F

**Fingerprint**  
A set of identifiers computed from an observed UI tree (and optionally a screenshot/text) to recognize and distinguish states. Typically includes:
- Structural component (hash of tree structure).
- Visual/perceptual component (hash of appearance).
- Optional semantic component (hash of textual content).

---

## G

**Graph (UI Graph)**  
The representation of an application’s UI as a directed graph, where nodes are states and edges are transitions. Stored in Atlas.

**Graph Model**  
The abstract definition of how states and transitions form a graph (directed, possibly cyclic, labeled edges, etc.), independent of storage implementation.

---

## I

**Intent**  
An abstract description of what an action does (e.g., `Save`, `OpenFile`, `ExportToPDF`, `DeleteItem`). Intents are semantic labels used on elements and transitions so consumers can plan in terms of goals, not raw UI labels.

**Interactive Element**  
See **Element**.

---

## L

**Locator**  
A platform-specific reference that allows a driver or consumer to locate an element in the live UI (e.g., accessibility path, DOM selector). Stored with elements in Atlas.

---

## O

**Ontology**  
The vocabulary of patterns, roles, and intents used by Ariane to describe the meaning of states, elements, and transitions (e.g., `Modal`, `primaryAction`, `Save`, `ExportToPDF`).

**Overlay Client (Future Concept)**  
A possible, non-core consumer that uses Atlas to draw guidance on top of existing applications (highlights, arrows, step counters). Not part of Ariane’s core specification.

---

## P

**Pattern (UI Pattern)**  
A semantic classification of UI structures or roles, such as:
- `Modal` – blocking dialog.
- `primaryAction` – main action in a state.
- `destructiveAction` – action that deletes data.
Patterns are usually attached via semantic fields on states or elements.

**Path**  
A sequence of transitions through the UI graph, typically representing a workflow (e.g., from home screen to export completion).

**Procedural Knowledge**  
Knowledge about *how to perform actions* or workflows (e.g., “how to export a document as PDF in App X”), as opposed to facts or static data. Ariane aims to represent procedural knowledge as UI graph data.

---

## S

**Safety Constraints**  
Rules used by Theseus or consumers to avoid risky actions during exploration or execution. Examples: skipping destructive actions, limiting path length, or requiring explicit authorization for certain intents.

**Semantic Hash**  
A fingerprint component derived from the textual content of the UI (labels, titles, etc.), used to distinguish states that are structurally similar but semantically different.

**Semantics**  
In Ariane, semantic information attached to states, elements, or transitions, typically in terms of patterns, roles, and intents.

**State**  
A node in the UI graph representing a specific UI configuration (screen, dialog, view). Each state has an ID, fingerprints, and a set of interactive elements.

**State Identification**  
The process of deciding whether a newly observed UI corresponds to a known state or a new one, using fingerprints and similarity thresholds.

---

## T

**Theseus**  
The exploration engine of Ariane. Theseus drives applications through their UIs (via drivers), discovers states and transitions, and emits structured data for Atlas.

**Transition**  
A directed edge in the UI graph from a source state to a target state, representing an action (click, keypress, value change, etc.). Each transition includes action metadata and optionally an intent.

---

## U

**UI as Data**  
The core idea of Ariane: represent user interfaces as machine-readable graphs (states and transitions), rather than just pixels or prose descriptions.

**UI Tree (Accessibility / DOM Tree)**  
The hierarchical structure of UI elements exposed by accessibility APIs or the DOM. Used by drivers and Theseus to identify elements, compute fingerprints, and derive states.

---

## V

**Variant (State Variant)**  
A state that is a variation of another state (e.g., due to A/B testing, layout changes, feature flags) but represents the same conceptual place in the application. Variants may be linked explicitly in Atlas via metadata.

**Visual Hash (Perceptual Hash)**  
A fingerprint component derived from a screenshot or rendered view of the UI, intended to capture visual similarity despite small changes in color or layout.

---

## Related Pages

- [Background-UI-as-Data](../ui-as-data) – conceptual motivation and knowledge domains.  
- [Theseus](../theseus) – exploration engine and drivers.  
- [Atlas](../atlas) – storage and semantic model.  
- [Consumers](../consumers) – how external systems use Ariane’s data.

    </file>
    <file path="technology/ariane/concepts/page.tsx" size="3284" lines="76" kind="source">
// app\technology\ariane\concepts\page.tsx
// app/technology/ariane/concepts/page.tsx
import Link from 'next/link';
import { Lightbulb, Book, FileText, ArrowRight } from 'lucide-react';

export const metadata = {
  title: "Concepts – Ariane",
  description: "Foundational concepts and glossary for the UI Graph engine.",
};

export default function ArianeConceptsPage() {
  return (
    &lt;main className="max-w-4xl mx-auto px-6 py-12"&gt;
      
      {/* HEADER */}
      &lt;div className="mb-16 border-b border-slate-200 pb-8"&gt;
        &lt;div className="flex items-center gap-3 mb-4"&gt;
          &lt;div className="p-2 bg-slate-100 text-slate-700 rounded-lg"&gt;
            &lt;Lightbulb className="w-8 h-8" /&gt;
          &lt;/div&gt;
          &lt;h1 className="text-4xl font-bold text-slate-900"&gt;Ariane Concepts&lt;/h1&gt;
        &lt;/div&gt;
        &lt;p className="text-xl text-slate-600 max-w-3xl leading-relaxed"&gt;
          The theoretical foundation of Ariane. Understanding why we treat user interfaces as data structures and the vocabulary used to describe them.
        &lt;/p&gt;
      &lt;/div&gt;

      {/* LINKS GRID */}
      &lt;div className="grid md:grid-cols-2 gap-8"&gt;
        
        {/* Background */}
        &lt;Link 
          href="/technology/ariane/concepts/ui-as-data" 
          className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
        &gt;
          &lt;h3 className="text-xl font-bold mb-3 flex items-center group-hover:text-primary"&gt;
             &lt;Book className="w-5 h-5 mr-2 text-blue-600" /&gt;
             Background: UI as Data
          &lt;/h3&gt;
          &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
            Why we need to treat interfaces as semantic graphs, not just pixels. The bridge between procedural knowledge and machine execution.
          &lt;/p&gt;
           &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              Read Background &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
        &lt;/Link&gt;

        {/* Glossary */}
        &lt;Link 
          href="/technology/ariane/concepts/glossary" 
          className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
        &gt;
          &lt;h3 className="text-xl font-bold mb-3 flex items-center group-hover:text-primary"&gt;
             &lt;FileText className="w-5 h-5 mr-2 text-emerald-600" /&gt;
             Glossary
          &lt;/h3&gt;
          &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
            Definitions for State, Transition, Intent, Fingerprint, and the core vocabulary of the Ariane ontology.
          &lt;/p&gt;
           &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              View Definitions &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
        &lt;/Link&gt;

      &lt;/div&gt;

      {/* FOOTER NAV */}
      &lt;div className="pt-8 border-t border-slate-100 mt-12"&gt;
        &lt;Link href="/technology/ariane" className="text-slate-500 hover:text-primary text-sm font-medium"&gt;
          ← Back to Ariane Hub
        &lt;/Link&gt;
      &lt;/div&gt;

    &lt;/main&gt;
  );
}

    </file>
    <file path="technology/ariane/concepts/ui-as-data/page.mdx" size="5500" lines="130" kind="source">
﻿export const metadata = {
  title: "Background: UI as Data"
};

# Background: UI as Data

Most digital knowledge today is captured as either text (â€œfactsâ€) or structured records (â€œentities and relationshipsâ€).  
But knowing *how to use* tools â€“ the concrete sequences of clicks and inputs inside software â€“ is still largely undocumented in a form machines can use.

Ariane exists to address this gap by treating user interfaces themselves as data.

---

## Knowledge Domains

You can roughly separate knowledge into three domains:

| Domain        | Description                               | Typical Infrastructure       | Coverage today |
|---------------|-------------------------------------------|------------------------------|----------------|
| Declarative   | Facts, concepts, history.                 | Text documents, encyclopedias, wikis. | High           |
| Structured    | Entities, attributes, and relationships.  | Databases, knowledge graphs. | High           |
| Procedural    | â€œHow-toâ€, workflows, tool usage.          | Manuals, tutorials, videos.  | Low            |

Declarative and structured knowledge have mature infrastructure: search engines, wikis, databases, and knowledge graphs.

Procedural knowledge, by contrast, is mostly embedded in:

- Long-form tutorials and how-to articles.
- Video walkthroughs and screen recordings.
- Informal community posts, comments, and Q&amp;A.

These artifacts are optimized for humans to read or watch, not for machines to reason over.

---

## The Procedural Knowledge Gap

Procedural knowledge has a few persistent problems:

- **Opaque to machines**  
  Manuals, videos, and blog posts rarely encode *exact* UI steps in a standardized way. Machines canâ€™t reliably extract â€œClick X, then Y, then set Z to 3â€.

- **Brittle and quickly outdated**  
  A minor UI redesign or new version can silently invalidate an entire tutorial.

- **Fragmented across tools and versions**  
  The same â€œintentâ€ (e.g., â€œexport to PDFâ€) looks very different across apps and releases.

As long as procedural knowledge remains tied to prose and pixels, AI systems have to infer â€œhow to do thingsâ€ from context or trial-and-error. Thatâ€™s expensive, fragile, and often unsafe.

---

## UI as a Graph

Ariane takes a different view: treat software as a navigable graph.

At a high level:

- A **state** is a specific UI configuration (screen, dialog, menu layout, etc.).
- A **transition** is a user action that moves from one state to another (click, keypress, gesture, etc.).

This yields a simple but powerful structure:

- Nodes = UI states.
- Edges = transitions labeled with actions and semantic intents.

Once interfaces are represented this way, â€œhow to do Xâ€ is just a pathfinding problem:

- â€œFrom current state `S`, find a path to a state where intent `ExportToPDF` is satisfied.â€

---

## Why Represent UIs as Data?

Representing UIs as data (rather than just screens and documentation) unlocks several properties:

- **Machine-readable**  
  Agents can query, traverse, and compare workflows instead of guessing from pixels.

- **Versioned**  
  Different app versions can have distinct graphs, while preserving history and compatibility.

- **Cross-application reasoning**  
  Different tools that implement the same concept (â€œSaveâ€, â€œNew projectâ€, â€œPublishâ€) can be aligned via shared semantic intents, even if their UI layouts differ.

- **Static analysis of workflows**  
  It becomes possible to analyze shortest paths, complexity, reachability, and safety properties (â€œis there a destructive action only one step away from a common state?â€).

---

## Arianeâ€™s Role

Ariane focuses on two things:

1. **Exploration and extraction (Theseus)**  
   - Systematically explore software.
   - Identify UI states and transitions.
   - Construct a consistent graph from those observations.

2. **Storage and semantics (Atlas)**  
   - Store the resulting UI graph with a formal schema.
   - Attach semantic meaning (intents, roles, patterns) to elements and transitions.

The result is a reusable, machine-readable description of how to operate software.

Ariane does **not** prescribe *how* agents must guide users. It only provides a structured map that external systems can consult when planning or explaining actions.

---

## Relationship to Other Knowledge Infrastructure

Ariane is designed to sit alongside, not replace, existing knowledge systems:

- Declarative knowledge remains in documentation, wikis, and reference material.
- Structured knowledge remains in databases and knowledge graphs.
- **Procedural knowledge** is where Ariane operates:
  - It answers: â€œGiven this software and this goal, what sequence of actions achieves it?â€

In practice, an agent might:

1. Use declarative and structured sources to understand what the user wants.
2. Use Ariane to decide *how* to carry out the task inside specific software.

---

## Next

- [Theseus](../theseus) â€“ how Ariane explores and discovers UI states and transitions.  
- [Atlas](../atlas) â€“ how those states and transitions are stored and described.  
- [Consumers](../consumers) â€“ how external systems use the resulting data.
    </file>
    <file path="technology/ariane/consumers/ai-agents/page.mdx" size="3663" lines="125" kind="source">
export const metadata = {
  title: "Consumers / AI Agent Integration"
};

# Consumers / AI Agent Integration

AI agents are a primary type of consumer for Ariane.

They use the UI graphs stored in Atlas as a reference for planning and executing actions inside existing software, turning high-level user goals into concrete sequences of UI operations.

This page describes how an AI agent can integrate with Ariane conceptually.

---

## High-Level Flow

From an agent’s point of view, the interaction with Ariane typically follows this loop:

1. **Understand the user’s goal.**
2. **Identify the current UI state.**
3. **Query Atlas for available actions and intents.**
4. **Plan a sequence of transitions toward the goal.**
5. **Execute (or instruct) the steps in the live UI.**
6. **Observe the resulting state and adjust if necessary.**

Ariane supports steps 2–4 by providing structured, semantic information about the UI.

---

## 1. Goal Representation

An agent starts with a goal, often expressed in natural language:

- “Export this document as PDF.”
- “Change the default font to Arial.”
- “Turn on dark mode.”

Internally, the agent should map this to:

- One or more **intents** known to Ariane (e.g., `ExportToPDF`, `ChangeDefaultFont`, `EnableDarkMode`).
- Optional constraints or preferences (e.g., “use the simplest path”, “avoid destructive steps”).

Ariane does not perform this mapping itself; it exposes a vocabulary of intents that the agent can align to.

See: [Atlas/Ontology-Vocabulary](/technology/ariane/atlas/ontology)

---

## 2. State Recognition Against Atlas

To act correctly, the agent must know which state in Atlas corresponds to the user’s current screen.

A typical process:

1. **Observe the live UI** via:
   - Direct access to accessibility APIs, or
   - Screen capture + OCR + element detection.

2. **Compute or approximate fingerprints** compatible with Atlas:
   - Structural representation (if a tree is accessible).
   - Visual/perceptual hash (if screenshots are available).
   - Semantic hints from labels and titles.

3. **Query Atlas**:
   - “Given these fingerprint components and context (app, version, platform), what is the closest `state_id`?”

Atlas returns:

- Candidate `state_id`(s).
- Similarity or confidence scores (implementation-dependent).
- References to interactive elements and their semantics.

The agent then:

- Selects the most plausible state.
- Optionally confirms via additional checks (e.g., checking that key labels match expectations).

---

## 3. Inspecting Available Actions

Once the agent has a `state_id`, it can ask:

1. **What actions are available?**
   - Query Atlas for all outgoing transitions from this state.
   - Retrieve each transition’s:
     - Action type.
     - Target element.
     - Target state.
     - Optional intent.

2. **How are actions presented in the UI?**
   - For each associated element, retrieve:
     - Role (button, menu item, etc.).
     - Label text.
     - Bounds/coordinates.
     - Patterns (primaryAction, destructiveAction, etc.).

The agent now knows, for this state:

- Which UI elements exist.
- What they do structurally.
- What they likely mean semantically.

---

## 4. Planning with Intents

Given a goal intent (e.g., `ExportToPDF`), the agent can treat the UI graph as a planning space.

### Local Decision

In many cases, the next step is local:

- If any outgoing transition from the current state has `intent == goalIntent`, choose that transition.

Example:

```text
current_state: state_home
goal_intent: ExportToPDF

Atlas returns:
- transition: trans_home_to_export_dialog
  - intent: Export
    </file>
    <file path="technology/ariane/consumers/hybrid-mapping/page.mdx" size="2234" lines="67" kind="source">
export const metadata = {
  title: "Hybrid Mapping and Human-Guided Assistants"
};

# Hybrid Mapping and Human-Guided Assistants

This page describes how Ariane supports a **hybrid approach**:

- Theseus performs **automated exploration** where possible.
- Human operators perform actions in real software where automation is not safe, reliable, or allowed.
- Both kinds of observations are stored in **Atlas** as the same kind of graph (states and transitions), with metadata indicating how they were obtained.

The goal is not full autonomy over “every interface”, but a realistic system where:

- Automation covers standards-based, accessibility-friendly UIs.
- Humans cover the hard parts.
- External tools (including AI agents) can rely on Atlas as a unified, machine-readable reference.

---

## Why a hybrid approach?

Purely automatic exploration is limited by:

- Anti-bot protections, logins, CAPTCHAs, 2FA, and secure flows.
- Custom-drawn UIs (canvas, 3D, games) without good accessibility metadata.
- High-risk operations (deletion, payments, production changes).
- Combinatorial explosion if you try to explore all possible paths.

On the other hand, purely manual documentation doesn’t give you:

- A consistent data model across apps.
- Programmatic query and pathfinding.
- A shared graph that agents and tools can consume.

The hybrid mode combines both:

- **Automation** for broad coverage of “normal” UI patterns.
- **Human-in-the-loop** for exceptional, sensitive, or complex workflows.
- A single graph in Atlas as the reference.

---

## Metadata conventions

Hybrid mapping is expressed entirely through existing Atlas records, using conventions in `metadata`.

### Source of observation

On `StateRecord.metadata` and `TransitionRecord.metadata`:

- `source = "auto"` – discovered by automated exploration.
- `source = "human"` – recorded while a human operator drove the UI.

Examples:

```json
{
  "context_id": "example-web-app-en",
  "discovered_at": "2025-03-01T10:00:00Z",
  "metadata": {
    "source": "human",
    "author": "operator-42",
    "session_id": "2025-03-01T09-59-00Z-session-1"
  },
  "state": { "...": "..." }
}
    </file>
    <file path="technology/ariane/consumers/integration-patterns/page.mdx" size="6413" lines="188" kind="source">
export const metadata = {
  title: "Consumers"
};

# Consumers

Ariane is designed as a data source.

Theseus explores applications and Atlas stores UI graphs; **consumers** are any external systems that query Atlas to understand how to operate software.

This page describes the main categories of consumers and the typical ways they interact with Ariane’s data.

---

## Types of Consumers

Examples of potential consumers include:

- **AI agents**
  - Use Atlas to plan and execute multi-step actions inside existing software.
  - Translate user goals (“export this document as PDF”) into concrete UI paths.

- **Automation tools**
  - Use Atlas to generate or validate scripts that manipulate applications via their UI.
  - Prefer declarative workflows (“do ExportToPDF”) over brittle, hard-coded sequences.

- **Analysis and diagnostics tools**
  - Analyze UI graphs to understand complexity, reachability, and safety.
  - Identify patterns such as deeply nested workflows or risky actions near common states.

- **Future overlay-style clients (optional)**
  - Use Atlas as a backend to highlight next actions on screen.
  - Not part of the core Ariane scope, but a natural downstream consumer.

---

## Core Usage Pattern

Most consumers follow a similar high-level pattern:

1. **Identify the current state**
   - Obtain a fingerprint (or partial description) of the live UI.
   - Query Atlas to find matching `state_id` candidates.

2. **Determine possible actions**
   - Fetch outgoing transitions from that state.
   - Inspect associated elements, patterns, and intents.

3. **Plan a path**
   - Given a goal (expressed in terms of intents or state conditions), search for a path:
     - `current_state` → ... → `goal_state`.

4. **Execute or explain**
   - Instruct the user or an automation layer to perform the required steps.
   - Optionally adapt or replan if the observed state diverges from expectations.

The exact details depend on the consumer, but the underlying operations are:

- State recognition.
- Transition lookup.
- Pathfinding constrained by intents and safety.

---

## State Recognition

To interact meaningfully, a consumer first needs to know “where it is” in the UI.

Typical steps:

1. Observe the current UI via its own mechanisms (e.g., screen capture + OCR, direct access to accessibility APIs).
2. Compute or approximate fingerprints compatible with those used in Atlas:
   - Structural analogs (if tree access is available).
   - Visual/perceptual hashes (if screenshots are available).
   - Semantic hints from labels/text.
3. Query Atlas:
   - “Given these fingerprints/hints, which state(s) are most similar?”

Atlas responds with:

- Candidate `state_id` values.
- Confidence scores or similarity metrics (if provided by the implementation).
- References to interactive elements.

Consumers can then decide whether they have a strong enough state match to proceed.

---

## Transition and Intent Lookup

Once a state is identified, consumers can ask:

- “What can be done from here?”  
- “Which actions correspond to a desired intent?”

Typical queries:

- **List all outgoing transitions**:
  - For a given `state_id`, return all transitions and target states.

- **Filter by intent**:
  - For a given `state_id` and `intent` (e.g., `Save`, `ExportToPDF`), return transitions whose `intent` matches.

- **Inspect elements**:
  - For each transition, get the associated element:
    - Role, label, bounds.
    - Pattern (e.g., primaryAction, destructiveAction).

This allows consumers to reason about:

- Which actions are relevant.
- How they are labeled and where they are located in the UI.
- Which actions may be risky (e.g., destructive).

---

## Path Planning

Consumers can use Atlas as a planning substrate.

Example problem:

&gt; From the current state `S`, find a sequence of actions leading to a state where `ExportToPDF` has been carried out.

Conceptually:

1. Treat the UI graph in Atlas as a search space.
2. Use algorithms such as:
   - BFS or Dijkstra-style search for shortest path by steps.
   - Heuristic search if some transitions are cheaper or safer.
3. Optionally constrain paths by:
   - Maximum depth or number of steps.
   - Safety constraints (avoid destructive intents).
   - Intermediate constraints (must pass through or avoid certain states).

Output to the consumer:

- A sequence of transitions:
  - `t1: click element X`
  - `t2: open menu Y`
  - `t3: select option Z`
- Along with:
  - Target coordinates or locators for each step (from element data).
  - Semantic explanation of each step based on intents and patterns.

---

## Safety and Constraints

Consumers may impose their own safety rules on top of Atlas:

- Exclude transitions with certain intents (e.g., `DeleteItem`, `FormatDisk`) unless explicitly allowed.
- Limit maximum path lengths to reduce complexity and risk.
- Prefer transitions annotated as:
  - `primaryAction` over obscure alternatives.
  - High-confidence over low-confidence mappings.

Atlas provides the raw data (roles, patterns, intents); consumers choose how strictly to interpret and enforce it.

---

## Future Overlay-Style Clients (Non-Core)

One possible consumer type is an overlay or heads-up display that:

- Queries Atlas in real time.
- Draws hints or highlights on top of the running application.
- Shows step-by-step guidance to the user.

From Ariane’s perspective, such a client:

- Is just another consumer of the UI graph.
- Uses state recognition and transition lookup in the same way as any other tool.
- May perform additional rendering and interaction interception locally.

This concept is not part of the core specification for Ariane, but documenting it here clarifies how the data model can support such use cases.

See: [Consumers/Future-Overlay-Client](/technology/ariane/consumers/overlay-client)

---

## Related Pages

- [Consumers/AI-Agent-Integration](/technology/ariane/consumers/ai-agents) – more focused view on AI agents using Ariane.
- [Atlas](/technology/ariane/atlas) – storage and semantic layer that consumers query.
- [Atlas/Graph-Model](/technology/ariane/atlas/graph-model) – structural view of states and transitions.
- [Atlas/core-Schema](/technology/ariane/atlas/core-schema) – fields available to consumers.
- [Background-UI-as-Data](/technology/ariane/concepts/ui-as-data) – motivation for exposing UIs as data.
    </file>
    <file path="technology/ariane/consumers/overlay-client/page.mdx" size="3890" lines="113" kind="source">
export const metadata = {
  title: "Consumers / Future Overlay Client (Concept)"
};

# Consumers / Future Overlay Client (Concept)

This page describes a **possible** overlay-style client as a downstream consumer of Ariane.

It is intentionally non-normative: Ariane’s core scope is limited to exploration (Theseus) and storage/semantics (Atlas). An overlay client is one way to use that data, not a requirement of the project.

---

## Concept

A future overlay client would:

- Run alongside existing applications.
- Recognize the current UI state.
- Query Atlas for recommended next actions.
- Render hints directly on top of the application (e.g., highlights, arrows, step counters).

From Ariane’s perspective, this client is simply:

&gt; A consumer that performs real-time state recognition and uses Atlas for next-step suggestions and path planning.

All rendering, input interception, and user interaction are handled by the client itself.

---

## Responsibilities of the Overlay Client

An overlay-style client (if built) would handle:

1. **Local observation**
   - Capture UI structure via accessibility APIs or screen capture + detection.
   - Compute or approximate fingerprints compatible with Atlas.

2. **State recognition via Atlas**
   - Query Atlas: “Which state does this UI most closely match?”
   - Use returned `state_id`, elements, and semantics.

3. **Guidance retrieval**
   - Given a goal (intent) or a predefined workflow:
     - Use Atlas to find the next transition(s) and target elements.
     - Retrieve element roles, labels, bounds, and intents.

4. **Visual overlay**
   - Draw highlights or markers at the coordinates of target elements.
   - Optionally dim the rest of the UI, show step counters, etc.

5. **Interaction handling (optional)**
   - Optionally intercept clicks or keystrokes to:
     - Enforce a guided path.
     - Confirm that the user followed the suggested step.

None of these behaviors are mandated or implemented by Ariane itself; they are purely client-side responsibilities.

---

## Interaction with Atlas

From a data standpoint, the overlay client behaves like any other agent:

- Uses Atlas for:
  - State recognition (matching fingerprints).
  - Transition lookup (outgoing edges).
  - Intent-based planning (goal-directed pathfinding).

- Uses its own UI representation for:
  - Rendering.
  - Input handling.
  - Error detection and recovery.

Ariane remains unaware of how the data is rendered or presented to users.

---

## Privacy and Local Processing (Recommended)

While not enforced by Ariane, a reasonable overlay design would:

- Perform all screen capture and accessibility access locally.
- Send only:
  - Structural/hashed fingerprints.
  - Context identifiers (app, version, platform).
- Avoid sending raw screenshots, text content, or user data to remote services when not necessary.

These are design recommendations for any future overlay consumer, not requirements of the core spec.

---

## Non-Goals for Ariane

To keep the scope clear:

- Ariane does **not** define any UI framework or library for drawing overlays.
- Ariane does **not** require any particular client to exist.
- Ariane does **not** specify UX rules for guided walkthroughs.

The only requirement is that any consumer—overlay or otherwise—must be able to:

- Recognize states.
- Query transitions and intents.
- Use the graph in a way that respects its semantics.

---

## Related Pages

- [Consumers](/technology/ariane/consumers) – overview of consumer types, including overlay as one example.
- [Consumers/AI-Agent-Integration](/technology/ariane/consumers/ai-agents) – how agents use Atlas for planning and guidance.
- [Atlas](/technology/ariane/atlas) – data and semantics that overlay clients would query.
- [Theseus](/technology/ariane/theseus) – how the underlying UI graphs are discovered in the first place.
    </file>
    <file path="technology/ariane/consumers/page.tsx" size="5601" lines="131" kind="source">
// app/technology/ariane/consumers/page.tsx
import Link from 'next/link';
import { 
  Users, 
  Bot, 
  Layers, 
  Search, 
  ArrowRight
} from 'lucide-react';

export const metadata = {
  title: "Consumers – Ariane",
  description: "How external systems (AI agents, automation tools, humans) use the UI graph.",
};

export default function ConsumersPage() {
  return (
    &lt;main className="max-w-5xl mx-auto px-6 py-12"&gt;
      
      {/* HEADER */}
      &lt;div className="mb-16 border-b border-slate-200 pb-8"&gt;
        &lt;div className="flex items-center gap-3 mb-4"&gt;
          &lt;div className="p-2 bg-slate-100 text-slate-700 rounded-lg"&gt;
            &lt;Users className="w-8 h-8" /&gt;
          &lt;/div&gt;
          &lt;h1 className="text-4xl font-bold text-slate-900"&gt;Consumers&lt;/h1&gt;
        &lt;/div&gt;
        &lt;p className="text-xl text-slate-600 max-w-3xl leading-relaxed"&gt;
          Ariane is a data source. Theseus builds the graph, Atlas stores it, and &lt;strong&gt;Consumers&lt;/strong&gt; query it to understand how to operate software.
        &lt;/p&gt;
      &lt;/div&gt;

      {/* 1. USAGE PATTERNS */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;Bot className="w-6 h-6 mr-3 text-primary" /&gt;
          Integration Patterns
        &lt;/h2&gt;
        
        &lt;div className="grid md:grid-cols-2 gap-8"&gt;
          {/* AI Agents */}
          &lt;Link 
            href="/technology/ariane/consumers/ai-agents"
            className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
          &gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-3 group-hover:text-primary"&gt;
              AI Agent Integration
            &lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              How agents use Atlas to turn high-level goals ("Export to PDF") into concrete UI paths. State recognition, intent lookup, and planning.
            &lt;/p&gt;
            &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              View Agent Flow &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          {/* Hybrid / Human */}
          &lt;Link 
            href="/technology/ariane/consumers/hybrid-mapping"
            className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
          &gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-3 group-hover:text-primary"&gt;
              Hybrid Mapping
            &lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              Combining automated exploration with human-guided recording for complex or sensitive workflows.
            &lt;/p&gt;
            &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              View Hybrid Model &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* 2. CONCEPTS &amp; FUTURE */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;Layers className="w-6 h-6 mr-3 text-blue-600" /&gt;
          Concepts &amp; Future
        &lt;/h2&gt;
        
        &lt;div className="grid gap-6"&gt;
          &lt;Link 
            href="/technology/ariane/consumers/overlay-client"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  Overlay Client (Concept)
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  A theoretical consumer that draws guidance (arrows, highlights) directly on top of existing applications.
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;Search className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          &lt;Link 
            href="/technology/ariane/consumers/integration-patterns"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  General Consumers Overview
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  The broad categories of systems that read Atlas: Agents, Automation scripts, and Analysis tools.
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;Users className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* FOOTER NAV */}
      &lt;div className="pt-8 border-t border-slate-100 flex justify-between text-sm"&gt;
        &lt;Link href="/technology/ariane" className="text-slate-500 hover:text-primary"&gt;
          ← Back to Ariane Hub
        &lt;/Link&gt;
        &lt;Link href="/technology/ariane/atlas" className="text-slate-500 hover:text-primary"&gt;
          View Atlas (Storage) →
        &lt;/Link&gt;
      &lt;/div&gt;

    &lt;/main&gt;
  );
}
    </file>
    <file path="technology/ariane/page.tsx" size="2580" lines="62" kind="source">
﻿// app\technology\ariane\page.tsx
import Link from 'next/link';
import { Card } from '@/components/Card'; 

export const metadata = {
  title: "Ariane",
  description: "Semantic infrastructure for treating user interfaces as data.",
};

export default function ArianePage() {
  return (
    &lt;main className="min-h-screen bg-white text-slate-900 selection:bg-primary selection:text-white pb-24"&gt;
      
      {/* HERO SECTION */}
      &lt;section className="max-w-4xl mx-auto px-6 pt-24 pb-12 border-b border-gray-100"&gt;
        &lt;h1 className="text-5xl font-serif font-medium mb-6 text-slate-900"&gt;
          Ariane
        &lt;/h1&gt;
        &lt;p className="text-2xl font-light text-slate-600 mb-8 leading-relaxed"&gt;
          Semantic infrastructure for treating user interfaces as data.
        &lt;/p&gt;
        
        &lt;div className="prose prose-slate prose-lg text-slate-600 leading-relaxed"&gt;
          &lt;p&gt;
            Ariane defines a universal graph model of software UIs—screens, controls, and the actions that connect them—so that external systems (such as AI agents or automation tools) can query this graph and use it as a reference when guiding users through software.
          &lt;/p&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      &lt;div className="max-w-4xl mx-auto px-6 py-16 space-y-20"&gt;

        {/* COMPONENTS */}
        &lt;section&gt;
          &lt;h2 className="text-3xl font-serif font-medium mb-10 text-slate-900"&gt;Components&lt;/h2&gt;
          &lt;div className="grid md:grid-cols-2 gap-8"&gt;
            
            {/* Theseus */}
            &lt;Card title="Theseus (Exploration Engine)" href="/technology/ariane/theseus"&gt;
              &lt;p className="mb-4"&gt;
                The exploratory engine that inspects real software to extract a graph of &lt;strong&gt;States&lt;/strong&gt; (screens) and &lt;strong&gt;Transitions&lt;/strong&gt; (actions).
              &lt;/p&gt;
              &lt;span className="text-xs font-mono text-primary hover:underline"&gt;
                View Documentation →
              &lt;/span&gt;
            &lt;/Card&gt;

            {/* Atlas */}
            &lt;Card title="Atlas (UI Graph &amp; Ontology)" href="/technology/ariane/atlas"&gt;
              &lt;p className="mb-4"&gt;
                The storage and semantic layer that persists the UI graph. It provides the core schema for elements and actions.
              &lt;/p&gt;
              &lt;span className="text-xs font-mono text-primary hover:underline"&gt;
                View Documentation →
              &lt;/span&gt;
            &lt;/Card&gt;
          &lt;/div&gt;
        &lt;/section&gt;

      &lt;/div&gt;
    &lt;/main&gt;
  );
}
    </file>
    <file path="technology/ariane/theseus/drivers/page.mdx" size="1841" lines="54" kind="source">
export const metadata = {
  title: "Theseus / Drivers"
};

# Theseus / Drivers

Drivers are the platform-specific adapters that allow Theseus to explore real software.

Each driver connects to a particular UI technology (web, desktop, mobile, etc.) and exposes a normalized view of the interface to the Theseus core:

- A tree of UI elements (roles, labels, hierarchy).
- A way to identify which elements are interactive.
- A way to perform abstract actions (activate, set value, focus, etc.).

The goal is that the core logic never needs to know whether it is exploring a browser, a native desktop app, or a mobile app.

---

## Driver Responsibilities

Every driver is responsible for:

1. **Tree acquisition**  
   - Retrieve the current UI/accessibility tree.
   - Provide a stable, hierarchical representation (nodes, children, attributes).

2. **Element characterization**  
   - Mark which elements are interactive (buttons, links, inputs, menu items, etc.).
   - Expose roles, labels, and metadata (e.g., enabled/disabled, checked/unchecked).

3. **Action execution**  
   - Provide operations such as:
     - `activate(elementId)` – click or trigger an action.
     - `setValue(elementId, value)` – enter text, toggle a checkbox, select an option.
     - `focus(elementId)` – move focus to an element.

4. **Context metadata**  
   - Report app identifier, version, platform, locale, and any other relevant context.

5. **Error handling**  
   - Report failures (e.g., element vanished, access denied) in a way the core can interpret.

---

## Driver Model

Conceptually, Each driver implements an interface like:

```text
Driver:
  getTree() -&gt; UITree
  listInteractiveElements(tree) -&gt; [UIElement]
  perform(action: AbstractAction) -&gt; Result
  getContext() -&gt; ContextMetadata
    </file>
    <file path="technology/ariane/theseus/engine-logic/page.mdx" size="7095" lines="224" kind="source">
export const metadata = {
  title: "Theseus / Exploration Engine"
};

# Theseus / Exploration Engine

The Exploration Engine controls how Theseus moves through an application:

- Which elements to interact with.
- In what order to explore them.
- When to stop.
- How to avoid unsafe or unproductive actions.

Its goal is to build a useful UI graph with bounded effort, while minimizing risk.

---

## Inputs and Outputs

**Inputs:**

- Current **state** (UI tree + state ID).
- List of **interactive elements** for that state.
- Exploration configuration:
  - Depth limits.
  - Action filters.
  - Safety constraints.

**Outputs:**

- A sequence of **actions** taken.
- Newly discovered **states** and **transitions**.
- Metadata about exploration coverage (visited states, pruned paths, errors).

---

## Core Loop

Conceptually, the Exploration Engine runs a loop like this:

1. Identify the current state (using state identification).
2. If the state is new:
   - Record it.
   - Enumerate candidate actions.
3. Pick a safe, unexplored action.
4. Execute the action via the driver.
5. Observe the resulting UI and compute the next state.
6. Record a transition from the previous state to the new state.
7. Repeat until no further actions are available within constraints.

---

## Traversal Strategy

The engine treats the UI as an implicit graph it is progressively revealing.

### Depth-First Exploration

A simple and useful default is **depth-first search (DFS)**:

- Choose an unexplored action from the current state.
- Follow it until:
  - A new state is discovered, or
  - A known state is reached (loop).
- Backtrack when:
  - All actions from a state have been explored or pruned.
  - Depth limits are reached.

Benefits:

- Easy to implement.
- Naturally builds complete *paths* (useful for downstream consumers).

Other traversal modes (e.g., breadth-first, heuristic-guided search) can be added, but DFS is a good baseline.

---

## Loop Detection and State History

To avoid infinite loops and redundant work:

- The engine keeps a **visited set** of state IDs.
- Each state has a record of:
  - Which actions have already been taken.
  - Which outgoing transitions have been observed.

If an action leads to a state that is already known:

- A new **edge** (transition) is recorded.
- The engine may still explore alternate actions from the current state, but it avoids revisiting the same transition repeatedly.

This ensures the exploration converges even if the application allows cycling between screens.

---

## Action Selection and Prioritization

Not all actions are equally important. The engine can prioritize:

- **Top-level navigation** (menus, tabs, primary buttons).
- **Dialogs and modals** (things that lead to new interaction surfaces).
- **Configuration sections** (tabs/panels inside settings).
- **Highly visible controls** (buttons in prominent positions).

Examples of filters:

- Ignore elements that:
  - Are off-screen or not visible.
  - Are known to be irrelevant (e.g., specific controls in debugging overlays, when detectable).
- Deprioritize:
  - Elements that appear identical or near-duplicate within the same region.
  - Controls that seem purely decorative.

Exact heuristics can be tuned per application or platform.

---

## Safety and Risk Management

Some actions are potentially destructive (e.g., delete, reset, format, uninstall). The engine should avoid them by default unless running in a controlled sandbox.

Safety mechanisms include:

- **Keyword-based filters** - Skip actions whose labels or descriptions match high-risk patterns (e.g., “Delete”, “Erase”, “Format”, “Reset”, “Uninstall”), especially when combined with red styling or warning icons.

- **Scope constraints** - Do not navigate into obviously hazardous subsystems (e.g., system-level disk tools) unless explicitly allowed.

- **Confirmation detection** - If an action opens a destructive confirmation dialog, treat this as a discovered state without proceeding further.

- **Configurable risk profiles** - Allow running in:
    - “Safe mode” – conservative, avoids any destructive-looking action.
    - “Sandbox mode” – more permissive, for controlled environments such as test VMs.

The Exploration Engine should treat safety as a first-class concern.

---

## Handling Non-Standard UIs

Some UIs do not expose sufficient accessibility information to support tree-based exploration.

In these cases, the engine may enable **fallback modes** via the driver:

1. **Vision-based candidate detection**
   - Capture a screenshot.
   - Detect potential interactive regions (buttons, inputs, menus) using vision heuristics.
   - Use OCR to infer text labels where possible.

2. **Coordinate-based interaction**
   - Treat candidate regions as elements.
   - Perform actions by clicking/tapping at their coordinates.

Trade-offs:

- Less reliable than accessibility-based exploration.
- Harder to map precisely back into structured UI trees.
- Best used for narrow, targeted exploration in controlled conditions.

---

## Exploration Limits

To keep exploration tractable, the engine uses explicit limits:

- **Depth limit** - Maximum length of a path from the starting state.

- **State limit** - Maximum number of distinct states to discover in a single run.

- **Action limit per state** - Maximum number of actions to attempt from any given state.

When a limit is reached, the engine:

- Stops further exploration.
- Outputs the partial graph discovered so far.

These limits can be configured depending on:

- Target application complexity.
- Time/resources available.
- Desired coverage.

---

## Error Handling and Recovery

During exploration, actions can fail in various ways:

- Element disappears before activation.
- App becomes unresponsive.
- OS denies access to certain windows.

The engine should:

- Record errors as annotations on transitions or states.
- Attempt to recover by:
  - Returning to a known stable state (e.g., main window).
  - Restarting the application if necessary (under driver control).
- Avoid infinite retry loops.

Failures are data too: they indicate unreachable paths or restricted states.

---

## Output for Atlas

The Exploration Engine provides Atlas with:

- **States** - IDs, fingerprints, and interactive elements.

- **Transitions** - Source state, target state, action metadata, and any semantic hints.

- **Coverage metadata** - Which states were fully explored, partially explored, or unreachable.
  - Any errors or safety-related skips.

Atlas then persists this information and makes it available to consumers.

---

## Related Pages

- [Theseus](/technology/ariane/theseus) – overview of the exploration engine.
- [Theseus/State-Identification](/technology/ariane/theseus/state-identification) – how states are fingerprinted and recognized.
- [Atlas](/technology/ariane/atlas) – how the discovered graph is stored and exposed to external systems.
- [Consumers/AI-Agent-Integration](/technology/ariane/consumers/ai-agents) – how agents use the resulting graph during guidance.
    </file>
    <file path="technology/ariane/theseus/page.tsx" size="5573" lines="133" kind="source">
import Link from 'next/link';
import { 
  Compass, 
  Search, 
  Fingerprint, 
  Terminal, 
  ArrowRight,
  ScanSearch
} from 'lucide-react';

export const metadata = {
  title: "Theseus – Exploration Engine",
  description: "The engine that explores software to discover UI states and transitions.",
};

export default function TheseusPage() {
  return (
    &lt;main className="max-w-5xl mx-auto px-6 py-12"&gt;
      
      {/* HEADER */}
      &lt;div className="mb-16 border-b border-slate-200 pb-8"&gt;
        &lt;div className="flex items-center gap-3 mb-4"&gt;
          &lt;div className="p-2 bg-slate-100 text-slate-700 rounded-lg"&gt;
            &lt;Compass className="w-8 h-8" /&gt;
          &lt;/div&gt;
          &lt;h1 className="text-4xl font-bold text-slate-900"&gt;Theseus&lt;/h1&gt;
        &lt;/div&gt;
        &lt;p className="text-xl text-slate-600 max-w-3xl leading-relaxed"&gt;
          The exploration engine of Ariane. Theseus inspects real software, discovers distinct UI states, and records the transitions that connect them.
        &lt;/p&gt;
      &lt;/div&gt;

      {/* 1. CORE LOGIC */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;Search className="w-6 h-6 mr-3 text-primary" /&gt;
          Exploration Logic
        &lt;/h2&gt;
        
        &lt;div className="grid md:grid-cols-2 gap-8"&gt;
          {/* Overview */}
          &lt;Link 
            href="/technology/ariane/theseus/engine-logic"
            className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
          &gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-3 group-hover:text-primary"&gt;
              Theseus Overview
            &lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              The high-level architecture: separating platform-agnostic logic from specific drivers to build a consistent graph.
            &lt;/p&gt;
            &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              View Architecture &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          {/* Exploration Engine */}
          &lt;Link 
            href="/technology/ariane/theseus/engine-logic"
            className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
          &gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-3 group-hover:text-primary"&gt;
              Exploration Engine
            &lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              The core loop: Action Selection, Traversal Strategy (DFS), and Safety Management to map the UI without breaking it.
            &lt;/p&gt;
            &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              View Engine Logic &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* 2. MECHANICS &amp; DRIVERS */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;ScanSearch className="w-6 h-6 mr-3 text-blue-600" /&gt;
          Mechanics
        &lt;/h2&gt;
        
        &lt;div className="grid gap-6 md:grid-cols-2"&gt;
          
          &lt;Link 
            href="/technology/ariane/theseus/state-identification"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  State Identification
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  How Theseus decides "Where am I?" using structural, visual, and semantic fingerprints.
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;Fingerprint className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          &lt;Link 
            href="/technology/ariane/theseus/drivers"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  Drivers
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  Platform-specific adapters (Web, Desktop, Mobile) that normalize the UI into a tree Theseus can understand.
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;Terminal className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

        &lt;/div&gt;
      &lt;/section&gt;

      {/* FOOTER NAV */}
      &lt;div className="pt-8 border-t border-slate-100 flex justify-between text-sm"&gt;
        &lt;Link href="/technology/ariane" className="text-slate-500 hover:text-primary"&gt;
          ← Back to Ariane Hub
        &lt;/Link&gt;
        &lt;Link href="/technology/ariane/atlas" className="text-slate-500 hover:text-primary"&gt;
          View Atlas (Storage) →
        &lt;/Link&gt;
      &lt;/div&gt;

    &lt;/main&gt;
  );
}
    </file>
    <file path="technology/ariane/theseus/state-identification/page.mdx" size="2740" lines="87" kind="source">
export const metadata = {
  title: "Theseus / State Identification"
};

# Theseus / State Identification

State identification is how Theseus decides whether the current UI is:

- A **new state** it has never seen before, or
- A **known state** it has already mapped.

To build a stable UI graph, Theseus needs state identifiers that:

- Are stable under small cosmetic changes.
- Change when the structure or functionality meaningfully changes.
- Can be computed across different platforms and drivers.

---

## What Is a State?

For Ariane, a **state** is:

&gt; A specific configuration of the user interface that is relevant for interaction and navigation.

Examples:

- Main window of an application.
- A dialog (e.g., “Save as”, “Preferences”).
- A subview or screen (e.g., “Export”, “Settings”, “New project”).

A state is represented by:

- The structure of the UI tree.
- The set and arrangement of interactive elements.
- Optionally, an associated screenshot or visual representation.

---

## Fingerprints

Each observed UI tree is converted into a **fingerprint**. Fingerprints are used to derive:

- A **state ID** – a compact identifier used in the graph.
- A notion of **similarity** – to decide whether two observations represent the same state or a variant.

A typical fingerprint is composed of:

1. **Structural hash**  
   - Encodes the shape and roles of the UI tree:
     - Node types (button, input, menu item, etc.).
     - Hierarchical relationships.
   - Insensitive to purely visual changes (e.g., colors, fonts).

2. **Visual hash (perceptual)**  
   - Encodes the appearance of the screen (e.g., pHash of a screenshot).
   - Robust to small visual differences but changes when layout/content changes visibly.

3. **Optional semantic hash**  
   - Encodes textual content (labels, titles) using OCR or accessibility text.
   - Useful when structure is similar but text differences matter.

---

## Example: Fingerprint Computation (Conceptual)

Pseudo-code, omitting implementation details:

```python
def compute_fingerprint(ui_tree, screenshot=None, text_tokens=None):
    structure_id = hash_tree_structure(ui_tree)
    visual_id = perceptual_hash(screenshot) if screenshot is not None else None
    semantic_id = hash_text_tokens(text_tokens) if text_tokens is not None else None

    return {
        "structure": structure_id,
        "visual": visual_id,
        "semantic": semantic_id,
    }

def compute_state_id(fingerprint):
    parts = [
        fingerprint["structure"],
        fingerprint.get("visual") or "",
        fingerprint.get("semantic") or "",
    ]
    return hash_concatenate(parts)
    </file>
    <file path="technology/kristal/distribution-and-versioning/page.mdx" size="5296" lines="167" kind="source">
export const metadata = {
  title: "Kristal — Distribution &amp; Versioning",
  description:
    "How Kristals are published, updated, and distributed safely (including offline) without silent downgrades.",
};

# Distribution &amp; versioning

A Kristal is meant to travel: between devices, communities, institutions, and time.

This page explains **how Kristals are released and updated** in a way that preserves the core promise:
**portable knowledge, verifiable integrity, and offline-correct use**.

---

## The rule that makes everything safe

### Updates never overwrite
A Kristal release is **immutable**. If you need a change, you publish a **new** release.

- You never “fix a pack in place”.
- You never mutate a sealed artifact.
- Every change creates a new identity and a new release record.

This gives users and institutions a stable foundation:
**you can always know exactly what was used, when, and where.**

---

## What gets distributed

Kristal is typically distributed as two complementary artifacts:

### 1) Exchange (canonical)
The **Exchange** is the reference artifact: what you cite, verify, and federate.

### 2) Runtime Pack (offline bundle)
The **Runtime Pack** is what you deploy for everyday use:
a portable, offline-friendly bundle optimized for lookup and constrained querying.

&gt; Exchange = canonical truth object  
&gt; Runtime Pack = deployable offline package

---

## Distribution happens through channels

A **channel** is a named distribution target: a tenant, a community, a region, a device cohort, or an environment (production / field / classroom).

Examples:
- `university-public`
- `heritage-official`
- `school-lab-offline`
- `personal-notes`

Channels are not just “folders”. They are **policy scopes**:
each channel can define what signatures are accepted, what gets pinned, and what is revoked.

---

## Versioning model (human-friendly + machine-safe)

Kristal uses two ideas at once:

### A) Strong identity (machine-safe)
Every artifact has an identity tied to its content (so “same content” stays comparable).

### B) Release versions (human-friendly)
On top of strong identity, you can use **semantic / calendar** version labels for usability.

Recommended practical rule:
- versions must be **monotonic** within a channel
- versions must never be reused for different content

---

## Safe activation

A Runtime Pack is not “active” just because it exists on disk.

Activation is a controlled switch that should be:

- **Fail-closed**: if verification fails, the pack does not activate.
- **Atomic**: either fully activated, or not activated at all.
- **Deterministic**: given the same inputs and policy, the same outcome occurs.

This prevents “partial installs” and “quiet corruption”.

---

## Downgrade prevention and rollback

Two things must be true simultaneously:

### 1) You can roll back fast
If a new release behaves badly, you need a deterministic rollback mechanism:
- “pinned rollback” (return to a specific known-good)
- or “last-known-good rollback”

### 2) You cannot be tricked into old vulnerable releases
Rollbacks must not become a downgrade vulnerability.

Practical controls:
- **minimum allowed version** per channel
- **revocation** (explicitly block a release)
- monotonic version rules (no older version can replace a newer one unless policy allows)

---

## Release strategies (how you distribute safely)

When devices update asynchronously (especially offline), “ship it and pray” doesn’t work.

Two rollout patterns fit Kristal well:

### Blue / Green (recommended default)
Keep two slots:
- **Blue** = current active release
- **Green** = new candidate release

Green is verified and warmed up first, then the channel pointer flips to Green.
Blue remains available for rollback during a defined window.

### Canary (optional)
Roll out to a small cohort first (e.g., 1% of devices),
then expand only if stability checks pass.

The key is not the pattern itself; it’s the invariant:
**every activation must be traceable to an explicit release record.**

---

## Domain shards and federation (versioning without fragmentation)

If you use domain sharding, you get a clean update story:

- each domain shard evolves on its own cadence
- updating one shard produces a new shard identity
- other shards remain intact and verifiable
- a federation root can reference a new shard while keeping the rest unchanged

This supports real-world distribution:
weekly updates for fast-moving domains, yearly updates for slow ones,
without forcing everything to move together.

---

## Offline distribution (field reality)

Kristal distribution should support:
- USB / local transfer
- local mirrors
- community caches
- intermittent connectivity

The principle stays the same:
**verification must be possible offline before activation.**

If the environment cannot verify, it should not activate.

---

## Recommended page flow

- **Trust &amp; provenance:** `/technology/kristal/trust-and-provenance`
- **Portability &amp; offline:** `/technology/kristal/portability-and-offline`
- **Integrations:** `/technology/kristal/integrations`

    </file>
    <file path="technology/kristal/integrations/page.mdx" size="4875" lines="142" kind="source">
export const metadata = {
  title: "Kristal — Integrations",
  description:
    "How Kristals plug into the kOA ecosystem: as stable references for knowledge, deliberation, decisions, execution, and long-term memory.",
};

# Kristal — Integrations

Kristals are not a product feature. They are **shared substrate**: a portable, verifiable knowledge package that other parts of the ecosystem can rely on.

This page explains **where Kristals show up**, and **what they enable** (not the internal mechanics).

---

## The integration rule of thumb

A system should use a Kristal when it needs a **stable reference**:

- a shared base of facts/definitions,
- a verifiable record of “what was known at the time,”
- an offline-capable knowledge bundle,
- or a durable anchor for accountability.

Kristals turn debates from “what is real?” into “what should we do about it?”

---

## Integration patterns

### 1) Kristal as a reference (linkable, citeable)
Use when you want any action, decision, or document to point to a stable source of truth.

Typical outputs:
- civic dossiers
- research packets
- curricula
- policy bundles
- case files (with structured context)

### 2) Kristal as an offline pack
Use when you need the same knowledge **without always-on infrastructure**: fieldwork, crisis response, constrained institutions, low connectivity.

### 3) Kristal as institutional memory
Use when the key requirement is **auditability over time**:
- “What did we rely on?”
- “What changed?”
- “Which version of the knowledge artifact was used?”

---

## Where Kristals integrate in kOA

### Konnaxion (public knowledge &amp; legitimacy)
Kristals appear as:
- published knowledge objects (shared reality),
- civic dossiers that deliberation can reference,
- structured learning content and public documentation.

**Outcome:** deliberation becomes more grounded and contestable because everyone can point to the same packaged reference.

---

### Orgo (execution &amp; continuity)
Kristals appear as:
- the “context bundle” attached to a case/workflow (requirements, constraints, definitions, evidence),
- repeatable operational playbooks that must remain stable across time,
- accountability anchors for post-mortems.

**Outcome:** tasks are executed with the same shared context, and later reviews can reconstruct what was known.

---

### Ariane (semantic navigation &amp; assistance)
Kristals appear as:
- stable meaning layers (definitions, entities, structured descriptions),
- domain packets that let guided navigation remain consistent.

**Outcome:** assistance becomes explainable and reproducible because it depends on stable, verifiable knowledge artifacts.

---

### Deterministic decision workflows (voting / adjudication / allocation)
Kristals appear as:
- the reference packet for a decision (inputs, definitions, constraints),
- the “versioned reality” behind a result,
- the object that keeps outcomes contestable over time.

**Outcome:** decisions can be audited and challenged without collapsing into semantic chaos.

---

## Typical lifecycle (non-technical)

1. **Publish** a Kristal (from sources and a declared scope)
2. **Distribute** it (people/orgs share it, cache it, carry it)
3. **Consume** it (platforms reference it; cases and decisions cite it)
4. **Update** it (new version, same scope; old versions remain available)
5. **Compare** versions (what changed, why, and who authorized it)

---

## What a good integration looks like

When someone opens a dossier / case / decision, they should be able to see:

- which Kristal(s) it references,
- who published them (authority),
- what scope they cover,
- which version was used,
- and how to contest or replace the reference if needed.

If users cannot see or contest the knowledge base, the integration is incomplete.

---

## Examples

### Example A — civic project dossier
A city project page references:
- “project facts” Kristal
- “community feedback” Kristal
- “constraints &amp; budget” Kristal  
so discussions remain grounded and auditable.

### Example B — operational incident response
A team uses an offline pack Kristal during an outage:
- procedures and definitions remain accessible,
- decisions and actions remain traceable after recovery.

### Example C — education module packet
A curriculum is shipped as Kristals:
- same concepts and evaluation criteria across locations,
- updates are explicit and versioned,
- learning outcomes remain comparable.

---

## Next pages

- **Trust &amp; provenance:** how authority, validation, and contestability work.
- **Distribution &amp; versioning:** how Kristals evolve without breaking trust.
- **Portability &amp; offline:** what “offline-capable” means in practice.

    </file>
    <file path="technology/kristal/overview/page.mdx" size="4251" lines="110" kind="source">
export const metadata = {
  title: "Kristal — Overview",
  description:
    "Kristal is the portable, verifiable knowledge artifact of the kOA ecosystem: a unit you can publish, audit, distribute, and use offline.",
};

# Kristal (Overview)

A **Kristal** is the core knowledge artifact of the kOA ecosystem.

It is designed to make knowledge **portable**, **verifiable**, and **usable under degraded conditions** (including offline).  
Think of it as a *compiled unit of meaning* that can be shared, audited, and reused—without requiring trust in a single platform or operator.

---

## What a Kristal does

Kristals exist to solve a simple problem:

&gt; Most “knowledge” is stored as text, screenshots, or websites—hard to verify, hard to reuse, and fragile under crisis.

A Kristal turns knowledge into something that can be:

- **Verified** (provenance, integrity, identity of publisher)
- **Replayed** (same inputs → same outputs, when needed)
- **Distributed** (copied across networks and communities)
- **Queried offline** (a portable bundle that can still answer questions)
- **Composed** (multiple domains / authorities without silent mixing)

---

## What you get as a user (not technical)

When a system is backed by Kristals, you get:

- Answers and summaries that can be **traced back to sources**
- A clearer separation between:
  - what is **claimed**
  - what is **validated**
  - what remains **uncertain**
- The ability to keep operating when the network fails (using local packs)

Kristals are meant to support **learning**, **deliberation**, **decision**, and **execution**—while preserving a durable memory of “what was known, when, and why.”

---

## Two outputs: Exchange vs Runtime Pack

A Kristal is typically published in two complementary forms:

### 1) Kristal Exchange
The Exchange is the **canonical artifact**:
- stable identity
- provenance and validation traces
- structured content meant for governance and interoperability

It’s the thing you cite, verify, and federate across institutions.

### 2) Runtime Pack
The Runtime Pack is the **portable offline bundle**:
- optimized for local use (search, lookup, retrieval, browsing)
- designed to operate when network access is limited or unavailable

It’s the thing you deploy on devices, local servers, or field environments.

&gt; In short: **Exchange = truth object**. **Runtime Pack = usable offline package**.

---

## Where Kristals fit in the ecosystem

Kristals are not “a platform.” They are a shared layer used by platforms and tools.

Typical roles:

- **Konnaxion** consumes Kristals to publish and preserve civic knowledge.
- **Ariane** can use Kristal-backed structures to guide users through complex interfaces with better trust and traceability.
- **Orgo** can route work that produces, validates, or updates Kristals (as a process), while keeping the audit trail.

---

## Trust, provenance, and federation (high level)

Kristals can be published by different authorities (institutions, communities, individuals).  
The ecosystem needs two guarantees:

1. **You can always identify who published what** (provenance)
2. **You can compose multiple sources without silent blending** (federation rules)

This is where concepts like **domain shards** and **federated composition** matter—but the public principle is simple:

&gt; If two sources disagree, the system must show it—never hide it.

---

## Use cases (examples)

- A community publishes a **local policy Kristal** and distributes it as a Runtime Pack for offline access.
- A school publishes **course Kristals** with versioned updates and clear provenance.
- A civic project publishes a **decision record Kristal**: claims, evidence, deliberation summary, outcome, and accountability trail.

---

## Next pages

- **What it does (in practice):** `/technology/kristal/what-it-does`
- **Trust &amp; provenance:** `/technology/kristal/trust-and-provenance`
- **Portability &amp; offline:** `/technology/kristal/portability-and-offline`
- **Distribution &amp; versioning:** `/technology/kristal/distribution-and-versioning`
- **Integrations:** `/technology/kristal/integrations`

    </file>
    <file path="technology/kristal/page.tsx" size="9561" lines="212" kind="source">
// app/technology/kristal/page.tsx
import type { Metadata } from 'next';
import Link from 'next/link';
import {
  Gem,
  ShieldCheck,
  Package,
  Globe,
  Layers,
  ArrowRight,
  FileCheck2,
} from 'lucide-react';

export const metadata: Metadata = {
  title: 'Kristal — Portable, Verifiable Knowledge Artifacts | kOA',
  description:
    'Kristal is the portable knowledge artifact format in the kOA ecosystem: verifiable provenance, offline-capable runtime packs, and durable versioning for governable knowledge.',
};

export default function KristalPage() {
  return (
    &lt;main className="max-w-5xl mx-auto px-6 py-12"&gt;
      {/* Header */}
      &lt;div className="mb-12"&gt;
        &lt;div className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-slate-200 text-slate-600 bg-white"&gt;
          &lt;Gem className="w-4 h-4 text-[#1e6864]" /&gt;
          &lt;span className="text-xs font-mono"&gt;Technology / Kristal&lt;/span&gt;
        &lt;/div&gt;

        &lt;h1 className="mt-6 text-4xl md:text-5xl font-bold text-slate-900"&gt;Kristal&lt;/h1&gt;

        &lt;p className="mt-4 text-xl text-slate-600 max-w-3xl leading-relaxed"&gt;
          A &lt;strong&gt;Kristal&lt;/strong&gt; is a portable, verifiable knowledge artifact—built to travel across systems,
          survive offline conditions, and remain contestable. It’s not “a document”: it is a structured package whose
          claims can be traced to sources and whose behavior can be reproduced.
        &lt;/p&gt;

        &lt;div className="mt-8 flex flex-wrap gap-3"&gt;
          &lt;Pill icon={&lt;ShieldCheck className="w-4 h-4" /&gt;}&gt;Provenance &amp; traceability&lt;/Pill&gt;
          &lt;Pill icon={&lt;Package className="w-4 h-4" /&gt;}&gt;Portable artifact&lt;/Pill&gt;
          &lt;Pill icon={&lt;FileCheck2 className="w-4 h-4" /&gt;}&gt;Reproducible builds&lt;/Pill&gt;
          &lt;Pill icon={&lt;Globe className="w-4 h-4" /&gt;}&gt;Offline-capable&lt;/Pill&gt;
          &lt;Pill icon={&lt;Layers className="w-4 h-4" /&gt;}&gt;Versioned &amp; compatible&lt;/Pill&gt;
        &lt;/div&gt;

        &lt;div className="mt-10 flex flex-col sm:flex-row gap-4"&gt;
          &lt;Link
            href="/technology"
            className="inline-flex items-center justify-center px-5 py-3 rounded-md bg-slate-900 text-white hover:bg-[#1e6864] transition-colors"
          &gt;
            Back to Technology &lt;ArrowRight className="w-4 h-4 ml-2" /&gt;
          &lt;/Link&gt;

          &lt;Link
            href="/platforms/konnaxion"
            className="inline-flex items-center justify-center px-5 py-3 rounded-md bg-white border border-slate-200 text-slate-900 hover:border-slate-400 transition-colors"
          &gt;
            See where Kristals are used &lt;ArrowRight className="w-4 h-4 ml-2" /&gt;
          &lt;/Link&gt;
        &lt;/div&gt;
      &lt;/div&gt;

      {/* What it does */}
      &lt;section className="mb-14"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-4"&gt;What it does&lt;/h2&gt;

        &lt;div className="grid grid-cols-1 md:grid-cols-2 gap-6"&gt;
          &lt;InfoCard
            icon={&lt;Package className="w-6 h-6 text-[#1e6864]" /&gt;}
            title="Makes knowledge portable"
            body="A Kristal can be copied, mirrored, archived, and shared across organizations or communities without losing integrity."
          /&gt;
          &lt;InfoCard
            icon={&lt;ShieldCheck className="w-6 h-6 text-[#1e6864]" /&gt;}
            title="Makes knowledge verifiable"
            body="A Kristal carries enough structure to audit: where claims come from, what rules were applied, and what remains uncertain."
          /&gt;
          &lt;InfoCard
            icon={&lt;Globe className="w-6 h-6 text-[#1e6864]" /&gt;}
            title="Enables offline operation"
            body="Kristals can ship with runtime bundles so people can query, browse, and reason with knowledge without relying on always-on networks."
          /&gt;
          &lt;InfoCard
            icon={&lt;Layers className="w-6 h-6 text-[#1e6864]" /&gt;}
            title="Supports durability over time"
            body="Kristals are versioned and compatible: upgrades should not silently break past outputs or erase the ability to reproduce prior states."
          /&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* How it fits in kOA */}
      &lt;section className="mb-14"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-4"&gt;Where it fits in the ecosystem&lt;/h2&gt;
        &lt;p className="text-slate-600 max-w-3xl leading-relaxed mb-6"&gt;
          Kristals are the “memory objects” that flow through kOA: they stabilize knowledge so deliberation, decision,
          and execution can be grounded in artifacts people can inspect and contest.
        &lt;/p&gt;

        &lt;div className="bg-slate-50 border border-slate-200 rounded-2xl p-6 md:p-8"&gt;
          &lt;ol className="space-y-4 text-slate-700"&gt;
            &lt;li className="flex gap-3"&gt;
              &lt;span className="mt-0.5 inline-flex h-6 w-6 items-center justify-center rounded-full bg-white border border-slate-200 text-xs font-bold"&gt;
                1
              &lt;/span&gt;
              &lt;div&gt;
                &lt;strong&gt;Capture &amp; compile:&lt;/strong&gt; sources and claims become structured, checkable artifacts (Kristals).
              &lt;/div&gt;
            &lt;/li&gt;
            &lt;li className="flex gap-3"&gt;
              &lt;span className="mt-0.5 inline-flex h-6 w-6 items-center justify-center rounded-full bg-white border border-slate-200 text-xs font-bold"&gt;
                2
              &lt;/span&gt;
              &lt;div&gt;
                &lt;strong&gt;Use &amp; deliberate:&lt;/strong&gt; platforms consume Kristals to support learning, argument mapping, and
                policy drafts.
              &lt;/div&gt;
            &lt;/li&gt;
            &lt;li className="flex gap-3"&gt;
              &lt;span className="mt-0.5 inline-flex h-6 w-6 items-center justify-center rounded-full bg-white border border-slate-200 text-xs font-bold"&gt;
                3
              &lt;/span&gt;
              &lt;div&gt;
                &lt;strong&gt;Preserve &amp; evolve:&lt;/strong&gt; decisions and outcomes produce new Kristals, creating durable public
                memory with versioned history.
              &lt;/div&gt;
            &lt;/li&gt;
          &lt;/ol&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* Explore Kristal */}
      &lt;section className="mb-6"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-4"&gt;Explore Kristal&lt;/h2&gt;
        &lt;p className="text-slate-600 mb-8 max-w-3xl"&gt;
          These pages stay focused on outcomes and guarantees. Deep technical details (schemas, internal tooling) should
          live in reference-only pages, not the primary narrative.
        &lt;/p&gt;

        &lt;div className="grid grid-cols-1 md:grid-cols-2 gap-6"&gt;
          &lt;NavTile
            href="/technology/kristal/overview"
            title="Overview"
            desc="The simplest explanation of what a Kristal is, who it’s for, and what it enables."
          /&gt;
          &lt;NavTile
            href="/technology/kristal/what-it-does"
            title="What it does"
            desc="Portability, verification, reuse, and durability—expressed as civic outcomes."
          /&gt;
          &lt;NavTile
            href="/technology/kristal/trust-and-provenance"
            title="Trust &amp; provenance"
            desc="How Kristals stay contestable: sources, signatures, and publication policies (without mystique)."
          /&gt;
          &lt;NavTile
            href="/technology/kristal/portability-and-offline"
            title="Portability &amp; offline"
            desc="How Kristals can be used under degraded conditions and still remain verifiable."
          /&gt;
          &lt;NavTile
            href="/technology/kristal/distribution-and-versioning"
            title="Distribution &amp; versioning"
            desc="How Kristals move through the ecosystem over time without breaking history."
          /&gt;
          &lt;NavTile
            href="/technology/kristal/integrations"
            title="Integrations"
            desc="Where Kristals plug into Ariane, Konnaxion, and Orgo as the shared knowledge substrate."
          /&gt;
        &lt;/div&gt;
      &lt;/section&gt;
    &lt;/main&gt;
  );
}

function Pill({ icon, children }: { icon: React.ReactNode; children: React.ReactNode }) {
  return (
    &lt;span className="inline-flex items-center gap-2 text-xs px-3 py-1 rounded-full border border-slate-200 text-slate-600 bg-white"&gt;
      &lt;span className="text-slate-400"&gt;{icon}&lt;/span&gt;
      {children}
    &lt;/span&gt;
  );
}

function InfoCard({ icon, title, body }: { icon: React.ReactNode; title: string; body: string }) {
  return (
    &lt;div className="p-6 rounded-xl border border-slate-200 bg-white"&gt;
      &lt;div className="mb-3"&gt;{icon}&lt;/div&gt;
      &lt;h3 className="text-lg font-bold text-slate-900 mb-2"&gt;{title}&lt;/h3&gt;
      &lt;p className="text-slate-600 leading-relaxed"&gt;{body}&lt;/p&gt;
    &lt;/div&gt;
  );
}

function NavTile({ href, title, desc }: { href: string; title: string; desc: string }) {
  return (
    &lt;Link
      href={href}
      className="group p-6 rounded-xl border border-slate-200 bg-white hover:border-[#1e6864] hover:shadow-sm transition-all"
    &gt;
      &lt;div className="flex items-center justify-between gap-4"&gt;
        &lt;div&gt;
          &lt;h3 className="text-lg font-bold text-slate-900 group-hover:underline decoration-1 underline-offset-4"&gt;
            {title}
          &lt;/h3&gt;
          &lt;p className="mt-2 text-slate-600 leading-relaxed"&gt;{desc}&lt;/p&gt;
        &lt;/div&gt;
        &lt;ArrowRight className="w-5 h-5 text-slate-300 group-hover:text-[#1e6864] transition-colors flex-shrink-0" /&gt;
      &lt;/div&gt;
    &lt;/Link&gt;
  );
}

    </file>
    <file path="technology/kristal/portability-and-offline/page.mdx" size="6622" lines="172" kind="source">
import { Package, WifiOff, ShieldCheck, Repeat, HardDrive, CheckCircle2 } from 'lucide-react';

export const metadata = {
  title: 'Kristal — Portability &amp; Offline',
  description:
    'How Kristals stay usable under weak networks: portable artifacts, offline runtime packs, verifiable provenance, and safe degraded modes.',
};

# Portability &amp; Offline

A Kristal is designed to **travel** and to **work under degraded conditions**.

That means two things:

1. **Portability:** you can move the artifact between systems, teams, and places without losing integrity.
2. **Offline-capability:** you can still browse, query, and rely on it even when the network is weak—or absent.

---

## What “portable” means (in practice)

&lt;div className="grid grid-cols-1 md:grid-cols-2 gap-6 my-8"&gt;
  &lt;div className="p-6 bg-white border border-slate-200 rounded-xl"&gt;
    &lt;div className="flex items-center gap-2 mb-3"&gt;
      &lt;Package className="w-5 h-5 text-[#1e6864]" /&gt;
      &lt;h3 className="font-bold text-slate-900"&gt;It can move as a single unit&lt;/h3&gt;
    &lt;/div&gt;
    &lt;p className="text-slate-600"&gt;
      A Kristal can be copied, mirrored, archived, and shared as a package. It does not depend on one central server to
      remain meaningful.
    &lt;/p&gt;
  &lt;/div&gt;

  &lt;div className="p-6 bg-white border border-slate-200 rounded-xl"&gt;
    &lt;div className="flex items-center gap-2 mb-3"&gt;
      &lt;ShieldCheck className="w-5 h-5 text-[#1e6864]" /&gt;
      &lt;h3 className="font-bold text-slate-900"&gt;It stays verifiable after travel&lt;/h3&gt;
    &lt;/div&gt;
    &lt;p className="text-slate-600"&gt;
      When you receive a Kristal from somewhere else, you can verify its integrity and provenance locally before trusting
      it.
    &lt;/p&gt;
  &lt;/div&gt;
&lt;/div&gt;

---

## Offline capability: what users actually get

Offline does not mean “no functionality.” It means **the core functions still work** without network dependency.

&lt;div className="bg-slate-50 border border-slate-200 rounded-2xl p-6 md:p-8 my-8"&gt;
  &lt;div className="flex items-start gap-3"&gt;
    &lt;WifiOff className="w-6 h-6 text-slate-700 mt-1" /&gt;
    &lt;div&gt;
      &lt;h3 className="text-lg font-bold text-slate-900 mb-2"&gt;Offline-first behavior&lt;/h3&gt;
      &lt;ul className="list-disc pl-5 text-slate-700 space-y-2"&gt;
        &lt;li&gt;&lt;strong&gt;Browse and search&lt;/strong&gt; the knowledge locally.&lt;/li&gt;
        &lt;li&gt;&lt;strong&gt;Inspect sources and reasoning traces&lt;/strong&gt; that are packaged with the Kristal.&lt;/li&gt;
        &lt;li&gt;&lt;strong&gt;Use stable references&lt;/strong&gt; (IDs, versions) without “link rot.”&lt;/li&gt;
        &lt;li&gt;&lt;strong&gt;Defer network operations&lt;/strong&gt; (updates, federation, publishing) until connectivity returns.&lt;/li&gt;
      &lt;/ul&gt;
    &lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;

---

## Runtime packs vs. exchange truth (conceptual)

To make offline use practical, a Kristal can ship with an **offline runtime pack**: a local bundle optimized for reading and querying.

- **Exchange layer (canonical):** the authoritative structured content and provenance.
- **Runtime pack (offline bundle):** a read-optimized package for local use.

This page stays at the “what it does” level. Technical formats belong in reference pages.

---

## Safe degraded mode (fail-closed integrity)

Offline systems can be dangerous if they silently drift into unverified state.

kOA uses a simple rule:

&lt;div className="p-5 bg-white border border-slate-200 rounded-xl my-6"&gt;
  &lt;div className="flex items-center gap-2 mb-2"&gt;
    &lt;CheckCircle2 className="w-5 h-5 text-[#1e6864]" /&gt;
    &lt;h4 className="font-bold text-slate-900"&gt;If verification fails, the system degrades safely.&lt;/h4&gt;
  &lt;/div&gt;
  &lt;p className="text-slate-600"&gt;
    You can still view what you have, but the system should clearly mark what is verified, what is outdated, and what
    cannot be trusted until checks succeed.
  &lt;/p&gt;
&lt;/div&gt;

---

## How updates work (user-facing behavior)

Offline-capable doesn’t mean “never updates.” It means updates are **explicit** and **checkable**.

&lt;div className="grid grid-cols-1 md:grid-cols-3 gap-6 my-8"&gt;
  &lt;div className="p-6 bg-white border border-slate-200 rounded-xl"&gt;
    &lt;div className="flex items-center gap-2 mb-3"&gt;
      &lt;Repeat className="w-5 h-5 text-[#1e6864]" /&gt;
      &lt;h3 className="font-bold text-slate-900"&gt;Versioned&lt;/h3&gt;
    &lt;/div&gt;
    &lt;p className="text-slate-600"&gt;
      You always know which version you are using. “What changed?” is answerable.
    &lt;/p&gt;
  &lt;/div&gt;

  &lt;div className="p-6 bg-white border border-slate-200 rounded-xl"&gt;
    &lt;div className="flex items-center gap-2 mb-3"&gt;
      &lt;ShieldCheck className="w-5 h-5 text-[#1e6864]" /&gt;
      &lt;h3 className="font-bold text-slate-900"&gt;Verified&lt;/h3&gt;
    &lt;/div&gt;
    &lt;p className="text-slate-600"&gt;
      Updates are not trusted by default. They are verified before becoming active.
    &lt;/p&gt;
  &lt;/div&gt;

  &lt;div className="p-6 bg-white border border-slate-200 rounded-xl"&gt;
    &lt;div className="flex items-center gap-2 mb-3"&gt;
      &lt;HardDrive className="w-5 h-5 text-[#1e6864]" /&gt;
      &lt;h3 className="font-bold text-slate-900"&gt;Transportable&lt;/h3&gt;
    &lt;/div&gt;
    &lt;p className="text-slate-600"&gt;
      Updates can travel via local mirrors, removable media, or “air-gapped” transfer when needed.
    &lt;/p&gt;
  &lt;/div&gt;
&lt;/div&gt;

---

## Typical scenarios

### 1) Field or crisis conditions
A team needs reliable procedures and references while connectivity is intermittent.
- They carry a Kristal runtime pack locally.
- They apply updates only when verifiable.

### 2) Institutional memory without platform lock-in
An organization wants durable knowledge that outlives vendors and tools.
- Kristals act as portable memory objects.
- Migration is “copy + verify,” not “rebuild everything.”

### 3) Community distribution
A community distributes curated knowledge packs for education or local services.
- The bundle is usable offline.
- Provenance remains attached.

---

## Quick answers

**Does offline mean “no accountability”?**  
No. Offline use increases the importance of clear verification status and versioning.

**Can two people have different versions?**  
Yes, temporarily. That’s why versions and provenance must be visible, and updates must be explicit.

**Is offline the default?**  
The system should behave well in both modes: online when available, offline when needed.

---

## Next

- **Trust &amp; provenance:** how verification and publication policies keep Kristals contestable.  
- **Distribution &amp; versioning:** how Kristals evolve over time without breaking history.

    </file>
    <file path="technology/kristal/trust-and-provenance/page.mdx" size="3982" lines="111" kind="source">
import Link from "next/link";

export const metadata = {
  title: "Kristal — Trust &amp; Provenance",
  description:
    "How Kristals stay verifiable: provenance chains, signatures, authority channels, and contestability without central gatekeepers.",
};

# Trust &amp; Provenance

A Kristal is designed to be **trusted without asking you to trust a platform**.

It is not “true because a website says so.” It is **verifiable because it carries its origin, its evidence trail, and its publication authority in a form that can be checked**—including offline.

---

## What “provenance” means here

Provenance answers:

- **Where did this claim come from?**
- **Who published it?**
- **What evidence was used?**
- **What rules were applied to compile it?**
- **What changed since the previous version?**

A Kristal is useful only if it makes those questions cheap to answer.

---

## What you can verify (without trusting the host)

When you receive a Kristal, you should be able to verify:

- **Identity:** which artifact it is (stable identifier)
- **Publisher:** which authority signed it (person, institution, collective)
- **Integrity:** that the content has not been altered
- **Lineage:** what prior version(s) it descends from
- **Evidence trail:** references or source anchors used to compile it
- **Policy / scope:** what domain it covers and what it does *not* claim

This is the baseline: **auditability by default**, not optional transparency.

---

## Trust is not one global score

kOA does not assume one universal “truth authority.”

Instead, trust is **domain-bounded** and **policy-driven**:

- A health Kristal and a municipal budget Kristal do not need the same validators.
- Different communities can adopt different trust policies.
- The system’s job is to make trust choices **explicit and inspectable**.

---

## Authority channels (how communities decide what to trust)

Think of an authority channel as a curated “trust list” + rules:

- which publishers are recognized for a domain,
- what standards they must meet,
- how conflicts are handled,
- and what recourse exists when something is wrong.

This prevents invisible gatekeeping: the criteria are written down, and the channel itself can be governed.

---

## Federation (how multiple sources coexist)

Kristals can be **composed across domains and publishers** without pretending disagreements don’t exist.

Federation means:

- a system can present a unified experience (search, navigation, summaries),
- while still preserving **who said what**, under which rules,
- and allowing multiple competing Kristals to coexist when reality is contested.

The point is to avoid “silent merging” that hides value choices.

---

## Contestability &amp; correction

A trustable system must support:

- **challenge:** flagging a claim, a source, or a compilation rule
- **correction:** publishing a revised version with explicit diffs
- **recourse:** a clear path for dispute resolution (institutional or community-based)
- **exit:** the ability to leave a channel/publisher and adopt another policy without losing your data

Kristals are built to make disagreement legible—not to erase it.

---

## Practical mental model

- **Provenance tells you:** “This is what this artifact asserts, and here is why.”
- **Authority tells you:** “These are the publishers I’m choosing to rely on for this domain.”
- **Federation tells you:** “Here are multiple views, kept separate, with clear attribution.”

---

## Next pages

- &lt;Link href="/technology/kristal/what-it-does"&gt;What Kristals do →&lt;/Link&gt;  
- &lt;Link href="/technology/kristal/portability-and-offline"&gt;Portability &amp; offline use →&lt;/Link&gt;  
- &lt;Link href="/technology/kristal/distribution-and-versioning"&gt;Distribution &amp; versioning →&lt;/Link&gt;  
- &lt;Link href="/technology/kristal/integrations"&gt;Integrations →&lt;/Link&gt;

    </file>
    <file path="technology/kristal/what-it-does/page.mdx" size="4248" lines="104" kind="source">
export const metadata = {
  title: "Kristal — What it does",
  description:
    "Kristal turns knowledge into portable, verifiable packages that can be reused, audited, and executed offline across the kOA ecosystem.",
};

# Kristal — What it does

Kristal is a **portable knowledge artifact**: a compact package of structured meaning, provenance, and reusable outputs.
Its job is simple:

&gt; **Make knowledge transferable, checkable, and usable under real-world constraints** (including low-connectivity and high-trust environments).

---

## At a glance

A Kristal helps you:

- **Freeze** a body of knowledge into a stable, shareable object.
- **Verify** where it comes from (sources, authorship, authority policies).
- **Reuse** it across products and contexts without re-building everything from scratch.
- **Operate offline** with the same meaning and constraints, when needed.
- **Audit decisions** that relied on it (what was known, what was assumed, what changed).

---

## What you get (in human terms)

### 1) A reliable “knowledge package”
Not a blog post. Not a spreadsheet. Not a single document.
A Kristal is closer to a **library you can carry**, with clear boundaries:

- what it contains
- what it does not contain
- who published it
- which rules/policies were used to validate it

### 2) A reusable foundation for civic workflows
Kristals enable systems where decisions can be traced back to stable knowledge—so people can contest outcomes *without* debating reality from scratch.

### 3) An offline-capable “runtime”
In kOA, many important contexts are degraded: crisis response, fieldwork, public institutions, low-trust environments.
Kristals are designed so that **useful access survives connectivity loss**.

---

## Where Kristals appear in kOA

Kristals are the shared substrate beneath multiple layers:

- **Konnaxion**: public knowledge objects, civic dossiers, learning content, governance records.
- **Orgo**: execution cases that require stable references, checklists, and accountable context.
- **Ariane**: structured navigation and interpretation that depends on stable meaning.
- **Voting / decision workflows**: decisions reference Kristals so outcomes stay auditable over time.

---

## Core guarantees (non-technical)

Kristal is meant to support these guarantees:

- **Portability:** you can move it between machines, institutions, or communities.
- **Integrity:** what you received is what was published (no silent drift).
- **Provenance:** you can see who published it and what it is based on.
- **Contestability:** people can challenge inputs, interpretations, and updates.
- **Compatibility:** older Kristals remain usable when the ecosystem evolves.
- **Offline usefulness:** core access works without always-on infrastructure.

---

## Common use cases

### Knowledge preservation
A community publishes a Kristal capturing its history, institutions, and decisions so it cannot be erased, rewritten quietly, or fragmented.

### Civic dossiers / policy packets
A municipality shares a Kristal that bundles a project’s facts, constraints, and public comments—so debates happen on a shared reference.

### Education and competence
A Kristal packages a curriculum (concepts, definitions, exercises, evaluation criteria), enabling learning that remains consistent across schools and contexts.

### Operational memory
An organization uses Kristals to freeze the “what we knew then” context behind major decisions, enabling accountability and post-mortems.

---

## What Kristals are not

- Not a social feed.
- Not an “AI opinion.”
- Not a black-box model output.
- Not a substitute for governance or legitimacy.

Kristal is infrastructure for *shared reality*—so deliberation can become action without domination or confusion.

---

## Next pages

- **Trust &amp; provenance:** how publication, authority, and validation work.
- **Portability &amp; offline:** how Kristals stay usable under degraded conditions.
- **Distribution &amp; versioning:** how Kristals evolve without breaking trust.
- **Integrations:** how platforms consume Kristals (Konnaxion, Orgo, Ariane, decision workflows).

    </file>
    <file path="technology/layout.js" size="282" lines="8" kind="source">
// app\technology\layout.js
export default function SectionLayout({ children }) {
  return (
    &lt;div className="max-w-5xl mx-auto my-12 px-6 py-10 bg-white border border-gray-200 rounded-lg shadow-sm prose prose-slate dark:prose-invert"&gt;
      {children}
    &lt;/div&gt;
  );
}

    </file>
    <file path="technology/page.tsx" size="8774" lines="201" kind="source">
﻿// app/technology/page.tsx
import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = {
  title: "Technology — kOA capabilities (public overview)",
  description:
    "What the kOA technology stack enables: verifiable knowledge artifacts (Kristals), guided navigation (Ariane), multilingual output, structured input, deterministic governance computation, and offline-first execution.",
  alternates: { canonical: "/technology" },
};

type SubLink = { href: string; label: string };
type TechItem = {
  href: string;
  title: string;
  tag: string;
  description: string;
  repo?: { href: string; label: string };
  sublinks?: SubLink[];
};

const ITEMS: TechItem[] = [
  // 1. Kristal
  {
    href: "/technology/kristal",
    title: "Kristal",
    tag: "Verifiable Knowledge",
    description:
      'Portable, verifiable knowledge artifacts. Kristals let communities publish “compiled truth packages” that can be checked, reused, and executed offline—so knowledge can travel without losing provenance.',
    sublinks: [
      { href: "/technology/kristal/what-it-does", label: "What it does →" },
      { href: "/technology/kristal/trust-and-provenance", label: "Trust &amp; provenance →" },
      { href: "/technology/kristal/portability-and-offline", label: "Portability &amp; offline →" },
    ],
  },
  // 2. Architect
  {
    href: "/technology/architect",
    title: "Abstract Wiki Architect",
    tag: "Multilingual Output",
    description:
      "Multilingual output for structured knowledge. Produces consistent, testable language across contexts, with traceable phrasing and reusable patterns.",
    repo: {
      href: "https://github.com/Rejean-McCormick/abstract-wiki-architect",
      label: "Rejean-McCormick/abstract-wiki-architect",
    },
  },
  // 3. SenTient
  {
    href: "/technology/sentient",
    title: "SenTient",
    tag: "Structured Input",
    description:
      "Structured intake from messy text. SenTient helps convert natural language into checkable statements that can be reviewed, corrected, and reused—turning “claims” into accountable knowledge.",
    repo: { href: "https://github.com/Rejean-McCormick/SenTient", label: "Rejean-McCormick/SenTient" },
  },
  // 4. SwarmCraft
  {
    href: "/technology/swarmcraft",
    title: "SwarmCraft",
    tag: "Continuity",
    description:
      "Continuity for long-running work. Helps keep multi-step projects coherent over time, with repeatable scaffolds and durable memory (including offline-friendly workflows when required).",
    repo: { href: "https://github.com/Rejean-McCormick/SwarmCraft", label: "Rejean-McCormick/SwarmCraft" },
  },
  // 5. Ariane
  {
    href: "/technology/ariane",
    title: "Ariane",
    tag: "Guided Navigation",
    description:
      "Guided navigation for complex systems. Ariane turns interfaces into structured maps so assistants can reliably walk users through real workflows—step by step, explainably.",
    repo: { href: "https://github.com/Rejean-McCormick/ariane", label: "Rejean-McCormick/ariane" },
  },
  // 6. Âme Artificielle
  {
    href: "/technology/ame-artificielle",
    title: "Âme artificielle",
    tag: "Guardrails",
    description: "Simulated human personality. An artificial soul.",
    repo: {
      href: "https://github.com/Rejean-McCormick/Ame-Artificielle",
      label: "Rejean-McCormick/Ame-Artificielle",
    },
  },
  // 7. VM-Engine
  {
    href: "/technology/voting-machine",
    title: "Voting Machine (VM-Engine)",
    tag: "Determinism",
    description:
      "Deterministic computation for governance. The same inputs yield the same outputs, enabling verification and reproducibility where legitimacy matters (e.g., ballots, tallies, allocations, simulations).",
    repo: { href: "https://github.com/Rejean-McCormick/vm-engine", label: "Rejean-McCormick/vm-engine" },
  },
];

const CARD =
  "group block p-6 border border-gray-200 rounded-lg hover:bg-gray-50 transition-colors";
const PILL = "text-xs font-mono bg-gray-100 px-2 py-1 rounded";

export default function TechIndex() {
  return (
    &lt;main className="max-w-4xl mx-auto px-6 py-16"&gt;
      &lt;header className="mb-12 border-b border-gray-200 pb-8"&gt;
        &lt;h1 className="text-4xl font-bold mb-4 text-gray-900"&gt;Technology&lt;/h1&gt;

        &lt;p className="text-xl text-gray-600"&gt;
          This stack is organized around &lt;strong&gt;capabilities&lt;/strong&gt;—what the system lets communities and
          organizations do—rather than internal technical mechanisms. The focus is:{" "}
          &lt;strong&gt;portable knowledge&lt;/strong&gt;, &lt;strong&gt;legitimate decisions&lt;/strong&gt;, and{" "}
          &lt;strong&gt;reliable execution&lt;/strong&gt;, under real-world constraints (offline, auditable, governable).
        &lt;/p&gt;

        &lt;div className="mt-5 p-4 rounded-md border border-gray-200 bg-white"&gt;
          &lt;p className="text-sm text-gray-700"&gt;
            &lt;strong&gt;Reading guide:&lt;/strong&gt; from &lt;em&gt;verifiable knowledge&lt;/em&gt; → &lt;em&gt;navigation&lt;/em&gt; →{" "}
            &lt;em&gt;decision &amp; verification&lt;/em&gt; → &lt;em&gt;execution &amp; continuity&lt;/em&gt;.
          &lt;/p&gt;
        &lt;/div&gt;

        &lt;p className="text-sm text-gray-400 mt-3 font-mono"&gt;
          Domain: Réjean McCormick // Goal: Offline-capable + Auditable + Governable
        &lt;/p&gt;

        &lt;nav className="mt-6 flex flex-wrap gap-3" aria-label="Related sections"&gt;
          &lt;Link
            href="/why"
            className="text-sm px-4 py-2 rounded-md border border-gray-200 hover:bg-gray-50 transition-colors"
          &gt;
            Context →
          &lt;/Link&gt;
          &lt;Link
            href="/initiatives"
            className="text-sm px-4 py-2 rounded-md border border-gray-200 hover:bg-gray-50 transition-colors"
          &gt;
            Initiatives →
          &lt;/Link&gt;
          &lt;Link
            href="/principles"
            className="text-sm px-4 py-2 rounded-md border border-gray-200 hover:bg-gray-50 transition-colors"
          &gt;
            Principles →
          &lt;/Link&gt;
          &lt;Link
            href="/principles/glossary"
            className="text-sm px-4 py-2 rounded-md border border-gray-200 hover:bg-gray-50 transition-colors"
          &gt;
            Glossary →
          &lt;/Link&gt;
        &lt;/nav&gt;
      &lt;/header&gt;

      &lt;section className="grid gap-6" aria-label="Technology capabilities"&gt;
        {ITEMS.map((item) =&gt; (
          &lt;div key={item.href} className={CARD}&gt;
            &lt;Link href={item.href} className="block"&gt;
              &lt;div className="flex justify-between items-center mb-2 gap-3"&gt;
                &lt;strong className="text-xl text-primary group-hover:underline"&gt;{item.title}&lt;/strong&gt;
                &lt;span className={PILL}&gt;{item.tag}&lt;/span&gt;
              &lt;/div&gt;
              &lt;p className="text-gray-700 mb-4"&gt;{item.description}&lt;/p&gt;
            &lt;/Link&gt;

            {item.sublinks?.length ? (
              &lt;div className="mt-3 flex flex-wrap gap-3"&gt;
                {item.sublinks.map((s) =&gt; (
                  &lt;Link key={s.href} href={s.href} className="text-sm text-gray-500 hover:text-primary font-mono"&gt;
                    {s.label}
                  &lt;/Link&gt;
                ))}
              &lt;/div&gt;
            ) : null}

            {item.repo ? (
              &lt;div className="mt-3"&gt;
                &lt;RepoLink href={item.repo.href} label={item.repo.label} /&gt;
              &lt;/div&gt;
            ) : null}
          &lt;/div&gt;
        ))}
      &lt;/section&gt;
    &lt;/main&gt;
  );
}

function RepoLink({ href, label }: { href: string; label: string }) {
  return (
    &lt;a
      href={href}
      target="_blank"
      rel="noopener noreferrer"
      className="text-sm text-gray-500 hover:text-primary font-mono flex items-center"
      aria-label={`Open repository: ${label}`}
    &gt;
      &lt;svg className="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"&gt;
        &lt;path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.05-.015-2.055-3.33.72-4.035-1.605-4.035-1.605-.54-1.38-1.335-1.755-1.335-1.755-1.087-.75.075-.735.075-.735 1.2.09 1.83 1.245 1.83 1.245 1.065 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.540-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.285 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z" /&gt;
      &lt;/svg&gt;
      {label}
    &lt;/a&gt;
  );
}
    </file>
    <file path="technology/sentient/page.mdx" size="8057" lines="128" kind="source">
export const metadata = {
  title: "Index"
};

# Index


##  SenTient (Semantic Entity Intelligent Transformation)

**Version:** 1.0.0-RC2
**Status:** Production Ready (Hybrid Architecture)

---

## 1.  Executive Summary and Core Philosophy

**SenTient** is a next-generation Entity Reconciliation and Relation Extraction engine designed to bridge the gap between messy, unstructured text and structured Knowledge Graphs (Wikidata/Wikibase).

The core philosophy is a **Hybrid Orchestration System** that combines three distinct technological lineages into a single "Funnel" pipeline to achieve high performance and accuracy:

* **Speed (Layer 1):** The FST-based rapid tagging of **OpenTapioca** (Solr).
* **Semantics (Layer 2):** The context-aware NLP of **Falcon 2.0** (Python/SBERT/Elastic).
* **Structure (Layer 3):** The robust data modeling and state management of **OpenRefine** (Java Core + DuckDB).

### Key Architectural Features
* **Performance Target:** High Precision ({"&gt;"}0.85) and High Recall ({"&gt;"}0.80) while maintaining a sub-second response time for user interactivity.
* **Hybrid Memory Architecture:** The Java Core uses a split-state strategy, keeping lightweight status flags and Row IDs in **Hot RAM** for instant faceting, while offloading heavy AI payloads (vectors, candidates, scores) to a **DuckDB Sidecar** (Cold Data). This approach supports datasets exceeding 5GB.
* **Decoupled Frontend:** The UI (React/Vite on Port `3000`) is decoupled from the Java Core (Jetty on Port `3333`) and communicates via a REST-like Command Pattern API.

---

## 2.  The Three-Layer Funnel and Processing Pipeline

The system operates on a "Funnel" logic: broad and fast at the top, narrow and precise at the bottom. The unit of work is the **SmartCell** object, which acts as the immutable contract across all layers.

### 2.1. Layer 1: Ingestion &amp; Fast Tagging (The Sieve)
* **Component:** `index_solr` + `core_java (Clustering)`.
* **Role:** High-speed identification of "Surface Forms" using string matching and pre-calculated popularity.
* **Latency Budget:** Must be **{"&lt;"} 50ms per batch**.
* **Process Flow:**
    1.  **Normalization &amp; Fingerprinting:** Java performs client-side deduplication using the **Key Collision Fingerprint** algorithm (tokenize, clean, sort, join) to minimize Solr API calls.
    2.  **FST Tagger:** The normalized text is passed to the **Solr TaggerHandler**, which uses a memory-mapped **Finite State Transducer (FST)** containing approx 14M Wikidata items. Lookup time is O(k).
    3.  **Authority Filter:** Candidates with low `popularity_score` ({"&lt;"} 100) or those matching stop words are immediately pruned.

### 2.2. Layer 2: The Semantic Linguist (Falcon 2.0)
* **Component:** `nlp_falcon` (Python 3.9+, Flask, SBERT).
* **Role:** Contextual disambiguation (solving the "Paris Problem") and semantic analysis.
* **Latency Budget:** approx 200ms per entity batch (Target).
* **Process Flow:**
    1.  **Preprocessing:** **Falcon Optimization** uses stopword pruning (`falcon_extended_en.txt`) and N-Gram generation to clean the signal.
    2.  **Property Extraction (Edge Detector):** Queries the `sentient_properties_v1` ElasticSearch index to infer the most likely **Wikidata Property (Predicate)**, boosting or penalizing entity candidates accordingly.
    3.  **Vector Scoring:** Encodes the 5-word context window into a 768-dimensional vector using **SBERT** (`all-MiniLM-L6-v2`). It then calculates **Cosine Similarity** between the input vector and candidate description vectors fetched from the `sentient_entities_fallback` Elastic index.

### 2.3. Layer 3: The Core Orchestrator (Final Adjudication)
* **Component:** `core_java` (Java 17, Jetty 10, Butterfly Framework).
* **Role:** Final Adjudication, Asynchronous Process Management, and State Serialization.
* **Process Flow:**
    1.  **Async Management:** Frontend triggers a **Command** (`ReconcileCommand`), which launches a non-blocking `LongRunningProcess` managed by the `ProcessManager` (utilizing a `ThreadPoolExecutorAdapter`). The Frontend polls for progress.
    2.  **Consensus Scoring:** Upon receiving Layer 2 scores, the Core performs **Score Normalization** (using a Sigmoid function with k=2.0, m=3.0) on Solr's raw log-score to map it to a [0, 1] range. It then applies the final weighted formula.
    3.  **Data Offload:** The orchestrator calls `DuckDBStore.insertBatch()` to persist heavy vectors to disk and flags the in-memory **Cell** as `RECONCILED` (lightweight state).
    4.  **History &amp; Serialization:** Every data modification is a **Transaction** implementing the `AbstractOperation` Command Pattern, ensuring that state can be restored by re-applying the History log upon server crash.

---

## 3.  QA, Validation &amp; Benchmarking

The QA Strategy relies on three pillars to statistically prove system improvement over time.

### 3.1. The Scrutinizers (Runtime Validation)
Scrutinizers are "Linting Rules for Data" located in `config/qa/scrutinizer_rules.yaml`. They run in the Java Core *before* export.
* **Integrity Scrutinizers:** Block export if data is logically corrupt (e.g., `MATCHED` cell has a null QID).
* **Constraint Scrutinizers:** Check for Wikidata alignment violations (e.g., Single Value Constraint) and show a `WARNING`.
* **Consensus Scrutinizers:** Check for statistical anomalies in scores (e.g., "Paris Hilton Rule": high popularity but low context score).

### 3.2. Golden Standard Datasets
Accuracy is measured against ground truth using industry-standard datasets:
* **LC-QuAD 2.0:** Validating complex relation extraction.
* **SimpleQuestions:** Validating simple entity spotting speed (Target Latency {"&lt;"} 50ms/query).
* **WebQSP:** Testing disambiguation of ambiguous surface forms.

### 3.3. Benchmarking &amp; Deployment Guardrail
The `evaluate_falcon_api.py` script runs the full pipeline.

| Metric | Target (v1.0) | Acceptable Range |
| :--- | :--- | :--- |
| **Precision** | 0.85 | {"&gt;"} 0.80 |
| **Recall** | 0.82 | {"&gt;"} 0.75 |
| **F-Score** | 0.83 | {"&gt;"} 0.78 |
| **Latency (p95)** | 200ms | {"&lt;"} 500ms |

**Deployment Rule:** If Precision drops by {"&gt;"} 2% after a model update (e.g., SBERT or Solr FST index), the deployment is **rejected**.

---

## 4.  Data Dictionary: The SmartCell Protocol

The **SmartCell** is the immutable data contract defined in `schemas/data/smart_cell.json`.

| Logical Field | JSON Type | Java Type | Python Type | Description |
| :--- | :--- | :--- | :--- | :--- |
| `raw_value` | `String` | `String` | `str` | Original user input (never modified) |
| `status` | `Enum (String)` | `Recon.Judgment` | `str` | Current lifecycle state (`NEW`, `PENDING`, `MATCHED`, etc.) |
| `consensus_score` | `Float` | `float` (transient) | `float` | Final calculated confidence (0.0 to 1.0) |
| `match` | `Candidate` Obj | `ReconCandidate` | `dict` | The single winning entity (if reconciled) |
| `vector` | `Array&lt;Float&gt;` | `double[]` | `np.ndarray` | SBERT embedding payload |

### Telemetry (`features`)
The `Candidate` object contains a `features` object used for UI visualization and debugging. The Frontend renders a stacked bar chart based on these weights:
* `tapioca_popularity` (Solr Log-Likelihood).
* `falcon_context` (Cosine Similarity from SBERT).
* `levenshtein_distance` (Normalized string distance from Java Core).

---

## 5.  Wiring &amp; Configuration Strategy

### Network Topology (Port Map)
All services are bound strictly to `127.0.0.1` for security.

| Service | Port | Protocol | Timeout |
| :--- | :--- | :--- | :--- |
| **Java Core (Orchestrator)** | `3333` | HTTP/1.1 | - |
| **Falcon (Python)** | `5005` | HTTP/1.1 | 120s (Throttled) |
| **Solr (Tapioca)** | `8983` | HTTP/2 | 500ms (Strict) |
| **ElasticSearch** | `9200` | HTTP/TCP | - |

### File System Layout
The central configuration files are located in `config/orchestration/environment.json` and other files within the `config/` directory.
    </file>
    <file path="technology/swarmcraft/core/architecture-overview/page.mdx" size="6038" lines="182" kind="source">
export const metadata = {
  title: "Architecture Overview"
};

# Architecture Overview

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

SwarmCraft is a **deterministic, data-driven story engine**. It transforms explicit project state into prose using a strict control loop and a layered architecture that separates:

- **Brain** (LLM personas and prompt behaviors)
- **Logic** (orchestration, tools, validation)
- **Memory** (project state files + RAG index)

---

## 1) The Three-Layer Model (Brain / Logic / Memory)

### Brain (LLM Personas)
Stateless services that generate or evaluate content. They never own canonical state.

Typical personas:
- **Architect**: plans next actions and targets
- **Narrator**: drafts/revises prose for one Part
- **Editor**: validates prose against scaffold + continuity

Location (typical):
- `ai_services/`

### Logic (Deterministic Engine)
The orchestrated runtime that:
- selects what to do next
- hydrates prompts slice-by-slice
- executes file operations through guarded tools
- enforces the update cycle

Core modules (typical):
- `core/orchestrator.py`
- `core/agent_tools.py`
- `core/project_manager.py`
- `core/scanner.py` (or equivalent)

### Memory (Explicit Project State)
All durable “truth” lives in versionable state and indexes:

- **Matrix** (runtime state): `data/matrix.json`
- **Story Bible** (creative intent): `data/story_bible/`
- **RAG memory DB** (long-term continuity): `data/memory_db/` (per project)

---

## 2) The Deterministic Control Loop

SwarmCraft runs a strict cycle:

**SCAN → PLAN → EXECUTE**

- **SCAN**  
  Reads filesystem + state, updates Matrix metrics/status, ingests new prose into RAG memory.
- **PLAN**  
  Architect selects the next target **Part** and action (draft/revise/review), based on Matrix status + control signals.
- **EXECUTE**  
  Narrator/Editor runs for exactly one Part. All mutations occur via tools, then the system returns to SCAN.

Details: **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**

---

## 3) Core Data Objects

### 3.1 Matrix (Runtime State)
`data/matrix.json` is the machine-readable view of “what exists and what’s next.”

It tracks:
- per-part manuscript path
- status (`EMPTY`, `DRAFTING`, `REVIEW_READY`, `REVISION_NEEDED`, `LOCKED`)
- active task target (always a **Part**)
- word counts, timestamps, metrics

Details: **[Central Matrix](../central-matrix-runtime-state)**

### 3.2 Story Bible (Creative Intent)
The Story Bible stores the canonical creative plan:
- characters, lore, constraints, style rules
- **templates** and **outline** (the Story Scaffold)

Details: **[Story Bible](../story-bible-creative-intent)**

### 3.3 Story Scaffold (Templates + Outline + Parts)
The scaffold is the structured narrative “grid” that humans and the Wizard can edit:

- **Templates** define:
  - thread set (Plot, Character Development, etc.)
  - cadence rules (how often threads must be filled)
  - default parts-per-chapter (and allowed bounds)

- **Outline** defines:
  - chapters → parts mapping
  - per-part thread beats (grid cells)
  - per-part contract (goal / obstacle / turn / outcome)
  - locks (protect manual edits)

Details:
- **[Story Scaffold](../story-scaffold-templates-outline-parts)**
- **[Schema: Templates](../schema: templates)**
- **[Schema: Outline](../schema: outline)**
- **[Outline Grid &amp; CSV Round-Trip](../outline-grid-csv-round-trip)**

---

## 4) Units of Work: Parts, Not Chapters

A **Part** is the atomic unit SwarmCraft drafts and revises.

- A chapter may contain **1–6 parts** depending on template and user choice.
- Parts enable:
  - small, stable prompt slices
  - targeted regeneration (one beat / one part)
  - better continuity control
  - clearer status tracking

Part orchestration: **[Orchestration: Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**

---

## 5) Multi-Project Isolation

SwarmCraft supports multiple isolated projects (universes):

- each project has its own:
  - `matrix.json`
  - `story_bible/`
  - `memory_db/`

Details: **[Multi-Project Management](../multi-project-management)**

---

## 6) RAG Memory for Long-Form Continuity

The memory system ingests written prose and enables semantic retrieval:
- prevents character/world drift
- reduces plot holes
- avoids bloating prompts with “story so far”

Details: **[RAG Memory System](../rag-memory-system)**

---

## 7) Control Surfaces

SwarmCraft is designed to be observable and steerable:

- **TUI Dashboard**: real-time view of tasks, logs, and state  
  Details: **[Dashboard  Reference](../dashboard-tui-reference)**

- **Control file** (implementation-specific): pause/resume/override planning without coupling UI to engine.

---

## 8) Provider Integration (Grok)

SwarmCraft uses a provider adapter to keep the engine model-agnostic:
- normalizes tool calling and responses
- centralizes API settings and error handling
- enables “Powered by Grok” without hardwiring provider logic everywhere

Details: **[Provider Adapter: Grok](../provider-adapter-grok)**

---

## 9) Where to Go Next

- **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**
- **[Central Matrix](../central-matrix-runtime-state)**
- **[Story Scaffold](../story-scaffold-templates-outline-parts)**
- **[Orchestration: Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**
    </file>
    <file path="technology/swarmcraft/core/central-matrix-runtime-state/page.mdx" size="5943" lines="180" kind="source">
export const metadata = {
  title: "Story Bible (Creative Intent)"
};

# Story Bible (Creative Intent)

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

The **Story Bible** is SwarmCraft’s home for **creative intent**: the canonical narrative plan, constraints, and reference material that guide drafting and revision.

It is designed to be:
- **explicit** (no hidden “chat history” requirements)
- **versionable** (text/JSON files under source control)
- **sliceable** (only the relevant subset is injected into prompts)
- **human-editable** (writers can edit directly or through UI)

The Story Bible is distinct from:
- **Matrix** (runtime progress state): **[Central Matrix](../central-matrix-runtime-state)**
- **RAG memory** (retrieved continuity evidence): **[RAG Memory System](../rag-memory-system)**

---

## 1) Location and Project Isolation

**Recommended per-project location:**
- `projects/&lt;project_id&gt;/data/story_bible/`

Single-project setups MAY use:
- `data/story_bible/`

The Story Bible is project-scoped. Each project has its own Bible and must not share it implicitly.

See: **[Multi-Project Management](../multi-project-management)**

---

## 2) What Lives in the Story Bible

SwarmCraft treats the Story Bible as the canonical source for:

### 2.1 Canonical references
- Characters (bios, arcs, secrets, voice constraints)
- Locations (maps, rules, sensory signatures)
- Lore (history, factions, magic/tech rules)
- Style rules (tone, POV, reading level, taboo list)
- Constraints (hard rules the Editor enforces)

These may be stored in Markdown or JSON, depending on preference and tooling.

### 2.2 The Story Scaffold (New)
The scaffold is part of the Story Bible because it is **creative intent**, not runtime state:

- **Templates:** `templates/&lt;template_id&gt;.json`  
  Defines threads (Plot, Character Development, etc.), cadence expectations, and default parts/chapter.

- **Outline:** `outline.json`  
  Defines chapters → parts mapping, per-part thread beats, and per-part “contract”.

Scaffold entry point:
- **[Story Scaffold](../story-scaffold-templates-outline-parts)**

---

## 3) Recommended Folder Layout

Example:

```text
projects/&lt;project_id&gt;/data/story_bible/
├── README.md
├── outline.json
├── templates/
│   ├── children.picturebook.json
│   ├── novel.default.json
│   └── screenplay.default.json
├── characters/
│   ├── king_klown.md
│   └── ...
├── locations/
│   ├── cosmic_council_hall.md
│   └── ...
├── lore/
│   ├── world_rules.md
│   └── ...
├── constraints/
│   ├── hard_rules.md
│   └── style_guide.md
└── glossaries/
    └── terms.md
````

This is a recommendation, not a constraint—projects may reorganize, but the engine must be able to find `outline.json` and the selected `templates/&lt;template_id&gt;.json`.

---

## 4) Bible vs Matrix vs Memory (Key Separation)

### Story Bible (Intent)

* “What the story *should be*.”
* Edited by humans and the Wizard.
* Used as authoritative instruction.

### Matrix (Runtime)

* “What the system has *done* and what is next.”
* Derived from disk and updated by tools.
* Tracks statuses and active tasks.

See: **[Central Matrix](../central-matrix-runtime-state)**

### RAG Memory (Evidence)

* “What has been *written before*.”
* Queried to prevent continuity drift.
* Does not define intent; it provides recall.

See: **[RAG Memory System](../rag-memory-system)**

---

## 5) How the Bible Is Used in Prompts (Slice-by-Slice)

SwarmCraft never dumps the whole Story Bible into the LLM.

Instead, the orchestrator hydrates prompts by selecting only:

* the target Part’s beats + contract from the Outline
* the minimal character/location/lore references required for that Part
* applicable constraints (style/voice/safety rules)

This reduces repetition, keeps cost stable, and prevents “prompt sprawl.”

See: **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**

---

## 6) Editing Workflows

The Story Bible supports three editing modes:

1. **Wizard-generated scaffold**
   A guided LLM workflow creates the first template selection + outline draft.

2. **Grid editing (human-friendly)**
   Outline beats are displayed as a grid (threads × parts), with optional CSV round-trip.

3. **Direct file edits**
   Writers can edit Markdown/JSON files directly, then SCAN reconciles changes.

Grid + CSV:

* **[Outline Grid CSV Round-Trip](../outline-grid-csv-round-trip)**

---

## 7) Integrity Rules (Recommended)

The scanner/orchestrator SHOULD validate:

* the chosen template thread list matches the outline beats keys
* every Part referenced in outline has a stable `part_id`
* locks are honored (beats/contract/manuscript)
* no orphan manuscripts exist without a Part mapping (or they are flagged)

---

## 8) Related Pages

* **[Story Scaffold](../story-scaffold-templates-outline-parts)**
* **[Schema Templates](../schema-templates)**
* **[Schema Outline](../schema-outline)**
* **[Outline Grid CSV Round-Trip](../outline-grid-csv-round-trip)**
* **[Central Matrix](../central-matrix-runtime-state)**
* **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**
    </file>
    <file path="technology/swarmcraft/core/deterministic-pipeline-scan-plan-execute/page.mdx" size="6824" lines="204" kind="source">
export const metadata = {
  title: "Deterministic Pipeline (SCAN-PLAN-EXECUTE)"
};

# Deterministic Pipeline (SCAN-PLAN-EXECUTE)

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

SwarmCraft runs a strict, repeatable control loop that transforms explicit project state into prose:

**SCAN → PLAN → EXECUTE**

This loop is designed to be:
- deterministic (same state → same next decision at the orchestration layer)
- restart-safe (crashes recover by re-scanning truth from disk)
- observable (each step is logged; state files are inspectable)

---

## 0) Glossary

- **Part**: the atomic unit of drafting/revision. Chapters are rollups over Parts.
- **Matrix**: runtime state (`data/matrix.json`).
- **Story Bible**: creative intent (`data/story_bible/`).
- **Scaffold**: templates + outline (parts mapping + thread beats + contracts).

---

## 1) Cycle Overview

### 1.1 SCAN
Recompute reality from disk and refresh runtime state.

Outputs:
- refreshed `data/matrix.json`
- optional RAG ingestion updates (per project)

### 1.2 PLAN
Select one target Part and one action.

Outputs:
- `matrix.active_task` populated with `{ target_id, action, reason }`
- optional “narrow queries” requested for continuity

### 1.3 EXECUTE
Run one persona on one Part, then persist results via tools.

Outputs:
- updated manuscript file for the Part, or review notes
- updated matrix bookkeeping
- return to SCAN

---

## 2) SCAN (Normative)

### 2.1 Inputs
SCAN reads:
- `data/story_bible/outline.json` (Parts ordering + locks)
- `data/story_bible/templates/&lt;template_id&gt;.json` (thread set + cadence)
- manuscripts under `data/manuscripts/` (or configured root)
- existing `data/matrix.json`
- `data/memory_db/` (if RAG enabled)

### 2.2 Responsibilities
SCAN MUST:
1. Enumerate expected **Parts** from `outline.json`.
2. Verify presence of each Part’s manuscript file.
3. Compute per-part metrics:
   - word count
   - last modified time
   - status
4. Respect locks:
   - never downgrade a `LOCKED` part
   - never infer unlocks automatically
5. Validate integrity:
   - missing parts
   - missing beats keys (fill with empty strings in UI projection)
   - schema mismatch between template threads and outline beats
6. Optionally ingest changed prose into RAG memory.
7. Atomically write the updated `data/matrix.json`.

### 2.3 Status derivation (Recommended)
A scanner SHOULD classify a Part as:

- `EMPTY`: file missing or below minimum threshold
- `DRAFTING`: draft exists but incomplete
- `REVIEW_READY`: draft is complete enough for Editor pass
- `REVISION_NEEDED`: flagged by Editor or override
- `LOCKED`: protected from further automated edits

Matrix semantics: **[Central Matrix](../central-matrix-runtime-state)**

---

## 3) PLAN (Normative)

### 3.1 Inputs
PLAN reads:
- current `data/matrix.json`
- `outline.json` for ordering + locks
- operator overrides (control surface), if present

### 3.2 Selection policy (Recommended default)
Absent overrides, the Architect SHOULD prioritize:
1. Any Part with `REVISION_NEEDED`
2. The earliest Part with `EMPTY`
3. The earliest Part with `DRAFTING`
4. Optional: any Part failing integrity checks (continuity, constraints, missing sections)

### 3.3 Hard constraints
PLAN MUST NOT select:
- Matrix `LOCKED` Parts
- Parts whose Outline locks prohibit changes (mapping depends on implementation: beats vs contract vs manuscript)

### 3.4 Plan output shape
The plan MUST specify:
- `target_type: "part"`
- `target_id: "&lt;part_id&gt;"`
- `action: "DRAFT" | "REVISE" | "REVIEW"`
- optional: `priority`, `reason`, `requested_memory_queries[]`

The orchestrator persists the plan into `matrix.active_task`.

---

## 4) EXECUTE (Normative)

EXECUTE performs exactly one scoped operation on exactly one Part.

### 4.1 Prompt hydration (Part slice)
Before invoking a persona, the orchestrator MUST hydrate a **Part slice**:
- the target Part’s thread beats (cells)
- the Part contract (goal/obstacle/turn/outcome)
- minimal continuity:
  - previous Part outcome summary (or last approved beat summary)
  - chapter-level goal/summary (if defined in outline)
  - relevant character/lore snippets (only what’s needed)
- project-level style constraints

Details: **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**

### 4.2 Narrator execution (DRAFT / REVISE)
If action is `DRAFT` or `REVISE`:
- the Narrator produces prose for exactly one Part
- prose is written to `data/manuscripts/&lt;part_id&gt;.md` (recommended)
- the orchestrator updates Matrix bookkeeping and returns to SCAN

### 4.3 Editor execution (REVIEW)
If action is `REVIEW`:
- the Editor checks the manuscript against:
  - Part beats + contract
  - continuity constraints (optionally via RAG)
  - style constraints
- output is either:
  - approve (advance to `REVIEW_READY` / candidate for locking), or
  - revision request (set `REVISION_NEEDED` with structured notes)

### 4.4 Tool safety
All modifications MUST go through the guarded tool layer (file ops + state updates).
No persona may mutate project files directly.

---

## 5) Atomicity, Concurrency, and Restart Safety

### 5.1 Atomic step rule
One loop iteration = one atomic action:
- one SCAN refresh
- one PLAN decision
- one EXECUTE action on one Part

### 5.2 No concurrent writers
Only one worker persona may write at a time for the active project.

### 5.3 Crash recovery
On restart:
- SCAN re-derives truth from disk
- Matrix is reconciled (locks respected)
- execution resumes without reliance on chat history

---

## 6) Observability and Control

- Dashboard observes `matrix.json`, logs, and metrics without blocking the engine.
- Control signals/overrides may affect PLAN (target selection) without breaking the deterministic loop.

Dashboard: **[Dashboard TUI Reference](../dashboard-tui-reference)**

---

## 7) Related Pages

- **[Architecture Overview](../architecture-overview)**
- **[Central Matrix](../central-matrix-runtime-state)**
- **[Story Bible](../story-bible-creative-intent)**
- **[Story Scaffold](../story-scaffold-templates-outline-parts)**
- **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**
    </file>
    <file path="technology/swarmcraft/core/page.tsx" size="4775" lines="111" kind="source">
// app\technology\swarmcraft\core\page.tsx
// app/technology/swarmcraft/core/page.tsx
import Link from 'next/link';
import { 
  Cpu, 
  GitMerge, 
  Database, 
  Activity,
  ArrowRight
} from 'lucide-react';

export const metadata = {
  title: "Core Logic – SwarmCraft",
  description: "The deterministic engine: Brain, Logic, and Memory layers.",
};

export default function CoreLogicPage() {
  return (
    &lt;main className="max-w-5xl mx-auto px-6 py-12"&gt;
      
      {/* HEADER */}
      &lt;div className="mb-16 border-b border-slate-200 pb-8"&gt;
        &lt;div className="flex items-center gap-3 mb-4"&gt;
          &lt;div className="p-2 bg-slate-100 text-slate-700 rounded-lg"&gt;
            &lt;Cpu className="w-8 h-8" /&gt;
          &lt;/div&gt;
          &lt;h1 className="text-4xl font-bold text-slate-900"&gt;Core Logic&lt;/h1&gt;
        &lt;/div&gt;
        &lt;p className="text-xl text-slate-600 max-w-3xl leading-relaxed"&gt;
          SwarmCraft is not a chatbot. It is a state-driven engine that separates &lt;strong&gt;Brain&lt;/strong&gt; (LLM personas), &lt;strong&gt;Logic&lt;/strong&gt; (orchestration), and &lt;strong&gt;Memory&lt;/strong&gt; (explicit state) to ensure deterministic, long-form coherence.
        &lt;/p&gt;
      &lt;/div&gt;

      {/* 1. ARCHITECTURE OVERVIEW */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;GitMerge className="w-6 h-6 mr-3 text-primary" /&gt;
          The Three-Layer Model
        &lt;/h2&gt;
        
        &lt;div className="grid md:grid-cols-2 gap-8"&gt;
          {/* Concept Card */}
          &lt;div className="bg-slate-50 p-8 rounded-xl border border-slate-200"&gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-4"&gt;Architecture Overview&lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              Understand how SwarmCraft prevents hallucinations by separating the "Brain" (stateless personas) from the "Memory" (canonical state).
            &lt;/p&gt;
            &lt;Link 
              href="/technology/swarmcraft/core/architecture-overview"
              className="inline-flex items-center font-bold text-primary hover:underline text-sm"
            &gt;
              View Diagram &amp; Layers &lt;ArrowRight className="ml-2 w-4 h-4" /&gt;
            &lt;/Link&gt;
          &lt;/div&gt;

          {/* Pipeline Card */}
          &lt;div className="bg-slate-50 p-8 rounded-xl border border-slate-200"&gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-4"&gt;The Deterministic Pipeline&lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              The &lt;strong&gt;SCAN → PLAN → EXECUTE&lt;/strong&gt; loop. How the engine recomputes reality from disk before every single action.
            &lt;/p&gt;
            &lt;Link 
              href="/technology/swarmcraft/core/deterministic-pipeline-scan-plan-execute"
              className="inline-flex items-center font-bold text-primary hover:underline text-sm"
            &gt;
              Trace the Loop &lt;ArrowRight className="ml-2 w-4 h-4" /&gt;
            &lt;/Link&gt;
          &lt;/div&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* 2. STATE MANAGEMENT */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;Database className="w-6 h-6 mr-3 text-blue-600" /&gt;
          State &amp; Runtime
        &lt;/h2&gt;
        
        &lt;div className="grid gap-6"&gt;
          &lt;Link 
            href="/technology/swarmcraft/core/central-matrix-runtime-state"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  Central Matrix (matrix.json)
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  The machine-readable view of "what exists and what is next." Tracks status (EMPTY, DRAFTING, LOCKED) for every Part.
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;Activity className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* FOOTER NAV */}
      &lt;div className="pt-8 border-t border-slate-100 flex justify-between text-sm"&gt;
        &lt;Link href="/technology/swarmcraft" className="text-slate-500 hover:text-primary"&gt;
          ← Back to SwarmCraft Hub
        &lt;/Link&gt;
        &lt;Link href="/technology/swarmcraft/scaffold" className="text-slate-500 hover:text-primary"&gt;
          Next: Story Scaffold →
        &lt;/Link&gt;
      &lt;/div&gt;

    &lt;/main&gt;
  );
}
    </file>
    <file path="technology/swarmcraft/meta/credits-and-lineage/page.mdx" size="4607" lines="116" kind="source">
export const metadata = {
  title: "Credits &amp; Lineage"
};

# Credits &amp; Lineage

SwarmCraft is built on two explicit lineages:

1) **Swarm foundation (upstream):** the multi-agent swarm engine patterns created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
2) **Architect meta-structure (AWA):** the deterministic “Architect-style” separation of **Brain / Logic / Memory** and state-first orchestration derived from **Abstract Wiki Architect (AWA)**.

---

## 1) Upstream Credit: Mojomast / swarmussy

SwarmCraft is an **architectural fork and deep rewrite**. The upstream project established the original swarm runtime approach and core implementation patterns.

**All architectural credit** for the original **multi-agent swarm design**—including the chatroom runtime concepts, terminal dashboard pattern, and foundational orchestration/tooling modules—belongs to **Mojomast**:

- Upstream author: **[Mojomast](https://github.com/mojomast)**
- Upstream repository: **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**

### Upstream-derived core module lineage (examples)
SwarmCraft acknowledges lineage (conceptual and/or code-derived depending on file history) from swarmussy patterns around:

- Agent tooling gateway  
  - `core/agent_tools.py`
- Task planning / task routing  
  - `core/task_manager.py`
- Token usage tracking  
  - `core/token_tracker.py`
- Dashboard / mission control pattern (TUI)

If upstream code is present in this repository, upstream license notices must be preserved alongside the derived files.

---

## 2) What SwarmCraft Adds / Changes

SwarmCraft reuses and extends the swarm foundation while introducing deterministic, story-first architecture and structured narrative state.

### 2.1 Deterministic pipeline
SwarmCraft replaces chatroom-style emergent coordination with a strict loop:

**SCAN → PLAN → EXECUTE**

- SCAN: reconcile truth from disk and update runtime state
- PLAN: select the next target Part and action
- EXECUTE: run one persona on one Part using guarded tools

See: **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**

### 2.2 State-first narrative architecture (Brain / Logic / Memory)
SwarmCraft formalizes narrative work into explicit layers:

- **Brain**: stateless LLM personas (Architect, Narrator, Editor)
- **Logic**: orchestrator + tools + validation
- **Memory**: Matrix + Story Bible + RAG index

See: **[Architecture Overview](../architecture-overview)**

### 2.3 Parts-based story execution
SwarmCraft treats a **Part** as the atomic unit of drafting/revision.
Chapters are rollups over Parts.

See:
- **[Central Matrix](../central-matrix-runtime-state)**
- **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**

### 2.4 Story Scaffold (Templates + Outline + Grid)
SwarmCraft introduces a structured story scaffold designed for both humans and LLMs:

- Templates define threads + cadence + default parts/chapter
- Outline defines chapters→parts mapping + per-part thread beats + part contracts
- Grid view displays the outline as a spreadsheet-like matrix, with optional CSV round-trip

See:
- **[Story Scaffold](../story-scaffold-templates-outline-parts)**
- **[Schema Templates](../schema-templates)**
- **[Schema Outline](../schema-outline)**
- **[Outline Grid CSV Round-Trip](../outline-grid-csv-round-trip)**

### 2.5 Multi-project isolation
Each project has isolated:
- Matrix
- Story Bible
- Memory DB (RAG)

See: **[Multi-Project Management](../multi-project-management)**

### 2.6 RAG long-term continuity
SwarmCraft integrates per-project retrieval memory to maintain coherence in long works.

See: **[RAG Memory System](../rag-memory-system)**

---

## 3) Provider Layer (Powered by Grok)

SwarmCraft is **powered by Grok** via a provider adapter that keeps the engine provider-agnostic:
- normalized request/response
- normalized tool calling
- centralized retries and error handling

See: **[Provider Adapter Grok](../provider-adapter-grok)**

---

## 4) Attribution Guidance for Derivative Works

If you build on SwarmCraft, you should preserve:
- this Credits &amp; Lineage page (or an equivalent notice in your primary documentation)
- upstream references to Mojomast/swarmussy
- any third-party license notices for code you redistribute

This page is the canonical place for lineage statements to avoid repeating long credit blocks throughout the wiki.
    </file>
    <file path="technology/swarmcraft/meta/page.tsx" size="2980" lines="66" kind="source">
// app\technology\swarmcraft\meta\page.tsx
// app/technology/swarmcraft/meta/page.tsx
import Link from 'next/link';
import { Info, GitBranch, ArrowRight, BookOpen } from 'lucide-react';

export const metadata = {
  title: "Meta &amp; Lineage – SwarmCraft",
  description: "Credits, history, and architectural lineage of the engine.",
};

export default function SwarmCraftMetaPage() {
  return (
    &lt;main className="max-w-4xl mx-auto px-6 py-12"&gt;
      
      {/* HEADER */}
      &lt;div className="mb-16 border-b border-slate-200 pb-8"&gt;
        &lt;div className="flex items-center gap-3 mb-4"&gt;
          &lt;div className="p-2 bg-slate-100 text-slate-700 rounded-lg"&gt;
            &lt;Info className="w-8 h-8" /&gt;
          &lt;/div&gt;
          &lt;h1 className="text-4xl font-bold text-slate-900"&gt;Meta &amp; Lineage&lt;/h1&gt;
        &lt;/div&gt;
        &lt;p className="text-xl text-slate-600 max-w-3xl leading-relaxed"&gt;
          SwarmCraft is not built in a vacuum. It is an architectural fork with a specific lineage. This section documents the credits, upstream origins, and the meta-structural decisions that define the engine.
        &lt;/p&gt;
      &lt;/div&gt;

      {/* CONTENT GRID */}
      &lt;section className="grid gap-6"&gt;
        
        {/* Credits Link */}
        &lt;Link 
          href="/technology/swarmcraft/meta/credits-and-lineage"
          className="group block p-8 bg-white border border-slate-200 rounded-xl hover:border-primary transition-all shadow-sm hover:shadow-md"
        &gt;
          &lt;div className="flex items-start"&gt;
            &lt;div className="p-3 bg-blue-50 text-blue-700 rounded-xl mr-6 group-hover:bg-blue-100 transition-colors"&gt;
              &lt;GitBranch className="w-8 h-8" /&gt;
            &lt;/div&gt;
            &lt;div&gt;
              &lt;h2 className="text-2xl font-bold text-slate-900 mb-2 group-hover:text-blue-700 transition-colors"&gt;
                Credits &amp; Architectural Lineage
              &lt;/h2&gt;
              &lt;p className="text-slate-600 mb-6 leading-relaxed"&gt;
                Acknowledging the **upstream foundation** (Mojomast/swarmussy) and the **meta-structural influence** of the Abstract Wiki Architect. This page details exactly what was reused, what was rewritten, and how the "Brain/Logic/Memory" separation evolved.
              &lt;/p&gt;
              &lt;div className="flex items-center font-bold text-blue-600 text-sm uppercase tracking-wide"&gt;
                Read Full Credits &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
              &lt;/div&gt;
            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/Link&gt;

      &lt;/section&gt;

      {/* FOOTER NAV */}
      &lt;div className="pt-8 border-t border-slate-100 mt-12 flex justify-between text-sm"&gt;
        &lt;Link href="/technology/swarmcraft" className="text-slate-500 hover:text-primary flex items-center"&gt;
          &lt;BookOpen className="w-4 h-4 mr-2" /&gt;
          Back to SwarmCraft Hub
        &lt;/Link&gt;
      &lt;/div&gt;

    &lt;/main&gt;
  );
}
    </file>
    <file path="technology/swarmcraft/page.mdx" size="8433" lines="149" kind="source">
import Link from 'next/link';
import { Card } from '@/components/Card';

export const metadata = {
  title: "SwarmCraft: Deterministic Story Engine",
  description: "A deterministic, data-driven story engine powered by Grok."
};

export default function SwarmCraftPage() {
  return (
    &lt;main className="min-h-screen bg-white text-slate-900 selection:bg-primary selection:text-white pb-24"&gt;
      
      {/* HERO SECTION */}
      &lt;section className="max-w-4xl mx-auto px-6 pt-24 pb-12 border-b border-gray-100"&gt;
        &lt;div className="mb-6"&gt;
          &lt;span className="inline-block bg-slate-900 text-white text-xs font-bold px-3 py-1 rounded tracking-wider"&gt;
            POWERED BY GROK
          &lt;/span&gt;
        &lt;/div&gt;
        
        &lt;h1 className="text-5xl font-serif font-medium mb-6 text-slate-900"&gt;
          SwarmCraft
        &lt;/h1&gt;
        &lt;h2 className="text-2xl font-light text-slate-600 mb-8"&gt;
          Deterministic Story Engine
        &lt;/h2&gt;

        {/* LINEAGE BLOCK */}
        &lt;div className="bg-slate-50 border-l-4 border-slate-400 p-6 rounded-r-sm mb-10 text-slate-700 italic leading-relaxed"&gt;
          &lt;strong className="block text-slate-900 not-italic mb-2"&gt;Architectural Lineage:&lt;/strong&gt;
          SwarmCraft is an &lt;strong&gt;architectural fork and deep rewrite&lt;/strong&gt; of the multi-agent swarm engine created by &lt;a href="https://github.com/mojomast" target="_blank" rel="noopener noreferrer" className="text-primary hover:underline"&gt;Mojomast&lt;/a&gt; in &lt;code&gt;mojomast/swarmussy&lt;/code&gt;. Its deterministic layering is derived from the meta-structure of the &lt;strong&gt;Abstract Wiki Architect&lt;/strong&gt;.
        &lt;/div&gt;
        
        &lt;div className="prose prose-slate prose-lg text-slate-600 leading-relaxed"&gt;
          &lt;p&gt;
            SwarmCraft is a deterministic, data-driven story engine designed to transform explicit project state into prose using a strict control loop. Unlike chat-based swarms, it separates &lt;strong&gt;Brain&lt;/strong&gt; (LLM personas), &lt;strong&gt;Logic&lt;/strong&gt; (orchestration), and &lt;strong&gt;Memory&lt;/strong&gt; (explicit state) to ensure long-form narrative coherence.
          &lt;/p&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      &lt;div className="max-w-4xl mx-auto px-6 py-16 space-y-20"&gt;

        {/* 1. CORE ARCHITECTURE */}
        &lt;section&gt;
          &lt;h2 className="text-3xl font-serif font-medium mb-8 text-slate-900"&gt;1. Core Architecture&lt;/h2&gt;
          
          &lt;div className="mb-10"&gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-4"&gt;The "Three-Layer Model"&lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6"&gt;Designed to prevent hallucinations and state drift:&lt;/p&gt;
            &lt;ul className="grid gap-4 md:grid-cols-3"&gt;
              &lt;li className="bg-slate-50 p-4 rounded border border-slate-100"&gt;
                &lt;strong className="block text-slate-900 mb-1"&gt;Brain (Personas)&lt;/strong&gt;
                &lt;span className="text-sm text-slate-600"&gt;Stateless services (Architect, Narrator, Editor) that never own canonical state.&lt;/span&gt;
              &lt;/li&gt;
              &lt;li className="bg-slate-50 p-4 rounded border border-slate-100"&gt;
                &lt;strong className="block text-slate-900 mb-1"&gt;Logic (Engine)&lt;/strong&gt;
                &lt;span className="text-sm text-slate-600"&gt;The runtime that handles the SCAN → PLAN → EXECUTE loop.&lt;/span&gt;
              &lt;/li&gt;
              &lt;li className="bg-slate-50 p-4 rounded border border-slate-100"&gt;
                &lt;strong className="block text-slate-900 mb-1"&gt;Memory (State)&lt;/strong&gt;
                &lt;span className="text-sm text-slate-600"&gt;All truth lives in the Matrix, Story Bible, and RAG DB.&lt;/span&gt;
              &lt;/li&gt;
            &lt;/ul&gt;
          &lt;/div&gt;

          &lt;div&gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-4"&gt;The Deterministic Pipeline&lt;/h3&gt;
            &lt;p className="text-slate-600 mb-4"&gt;SwarmCraft replaces emergent coordination with a strict cycle:&lt;/p&gt;
            &lt;ol className="list-decimal pl-5 space-y-2 text-slate-700 font-medium"&gt;
              &lt;li&gt;&lt;span className="text-slate-900 font-bold"&gt;SCAN:&lt;/span&gt; Recompute reality from disk.&lt;/li&gt;
              &lt;li&gt;&lt;span className="text-slate-900 font-bold"&gt;PLAN:&lt;/span&gt; Select the next atomic Part to draft or revise.&lt;/li&gt;
              &lt;li&gt;&lt;span className="text-slate-900 font-bold"&gt;EXECUTE:&lt;/span&gt; Run one persona on that Part, then exit.&lt;/li&gt;
            &lt;/ol&gt;
          &lt;/div&gt;
        &lt;/section&gt;

        {/* 2. DOCUMENTATION MODULES */}
        &lt;section&gt;
          &lt;h2 className="text-3xl font-serif font-medium mb-8 text-slate-900"&gt;2. Documentation Modules&lt;/h2&gt;
          &lt;p className="text-slate-500 italic mb-8 border-l-2 border-primary pl-4"&gt;
            Technical documentation is currently being migrated to this new platform.
          &lt;/p&gt;

          &lt;div className="grid md:grid-cols-3 gap-8 text-sm"&gt;
            
            {/* Logic */}
            &lt;div&gt;
              &lt;h4 className="font-bold text-slate-900 uppercase tracking-widest mb-4 border-b pb-2"&gt;Core Logic&lt;/h4&gt;
              &lt;ul className="space-y-3 text-slate-600"&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Architecture Overview:&lt;/strong&gt; High-level diagram of separation.&lt;/li&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Deterministic Pipeline:&lt;/strong&gt; Breakdown of the control loop.&lt;/li&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Prompt Hydration:&lt;/strong&gt; Preventing prompt sprawl via slicing.&lt;/li&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Provider Adapter (Grok):&lt;/strong&gt; Model-agnostic normalization.&lt;/li&gt;
              &lt;/ul&gt;
            &lt;/div&gt;

            {/* State */}
            &lt;div&gt;
              &lt;h4 className="font-bold text-slate-900 uppercase tracking-widest mb-4 border-b pb-2"&gt;State &amp; Schema&lt;/h4&gt;
              &lt;ul className="space-y-3 text-slate-600"&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Central Matrix:&lt;/strong&gt; Runtime state (matrix.json).&lt;/li&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Story Bible:&lt;/strong&gt; Creative intent &amp; constraints.&lt;/li&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Story Scaffold:&lt;/strong&gt; The grid system.&lt;/li&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Schema Templates:&lt;/strong&gt; Thread sets and pacing.&lt;/li&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Schema Outline:&lt;/strong&gt; Chapters and beat contracts.&lt;/li&gt;
              &lt;/ul&gt;
            &lt;/div&gt;

            {/* Ops */}
            &lt;div&gt;
              &lt;h4 className="font-bold text-slate-900 uppercase tracking-widest mb-4 border-b pb-2"&gt;Operations&lt;/h4&gt;
              &lt;ul className="space-y-3 text-slate-600"&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Dashboard TUI:&lt;/strong&gt; "Mission Control" interface.&lt;/li&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Multi-Project Mgmt:&lt;/strong&gt; Isolated universes.&lt;/li&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;Outline Grid &amp; CSV:&lt;/strong&gt; Round-tripping via spreadsheets.&lt;/li&gt;
                &lt;li&gt;&lt;strong className="text-slate-800"&gt;RAG Memory:&lt;/strong&gt; Long-term retrieval.&lt;/li&gt;
              &lt;/ul&gt;
            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/section&gt;

        {/* 3. KEY CONCEPTS */}
        &lt;section&gt;
          &lt;h2 className="text-3xl font-serif font-medium mb-8 text-slate-900"&gt;3. Key Concepts&lt;/h2&gt;
          &lt;div className="grid md:grid-cols-2 gap-6"&gt;
            &lt;Card title="Parts, Not Chapters"&gt;
              The atomic unit of work is the &lt;strong&gt;Part&lt;/strong&gt;. Chapters are simply rollups of 1–6 Parts. This allows for small, stable prompt slices and targeted regeneration.
            &lt;/Card&gt;
            &lt;Card title="Slice-by-Slice Hydration"&gt;
              The engine never dumps the whole Story Bible into the LLM. It hydrates prompts with &lt;strong&gt;only&lt;/strong&gt; the active Part's beats, contract, and relevant RAG evidence.
            &lt;/Card&gt;
          &lt;/div&gt;
        &lt;/section&gt;

        {/* FOOTER */}
        &lt;section className="bg-slate-50 p-6 rounded-sm text-center"&gt;
          &lt;h3 className="text-sm font-bold uppercase text-slate-400 mb-2"&gt;Full Credits &amp; Lineage&lt;/h3&gt;
          &lt;p className="text-slate-600 mb-4 text-sm"&gt;
            Based on the foundational work of Mojomast/swarmussy and the Abstract Wiki Architect.
          &lt;/p&gt;
          &lt;span className="text-slate-400 text-xs font-mono border border-slate-200 px-3 py-1 rounded"&gt;
            Full Credits Page (Coming Soon)
          &lt;/span&gt;
        &lt;/section&gt;

      &lt;/div&gt;
    &lt;/main&gt;
  );
}
    </file>
    <file path="technology/swarmcraft/runtime/dashboard-tui-reference/page.mdx" size="5184" lines="187" kind="source">
export const metadata = {
  title: "Dashboard TUI Reference"
};


# Dashboard TUI Reference

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; The dashboard pattern is also downstream of Mojomast’s original “mission control” approach for observing swarm activity.  
&gt; SwarmCraft’s deterministic layering is derived from the meta-structure of **Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

The SwarmCraft Dashboard is a **Terminal UI (TUI)** that observes and steers the engine while keeping UI and execution decoupled.

Key principle:
- **Engine runs the AI work**
- **Dashboard renders state and sends control signals**

This prevents UI freezes and supports restart-safe operation.

---

## 1) What the Dashboard Watches

The dashboard should primarily read:
- `data/matrix.json` (runtime state)
- `data/story_bible/outline.json` (structure reference, optional)
- logs (engine logs, tool logs)
- token usage metrics (if tracked)

The dashboard must never be the source of truth; it is a view/controller.

Matrix reference: **[Central Matrix](../central-matrix-runtime-state)**

---

## 2) Recommended Layout (3-Column Mission Control)

A recommended high-level layout:

### 2.1 Context Panel (Left)
Shows “what the engine is currently focused on”:
- active project (project_id)
- active chapter + part (from `matrix.active_task`)
- cast in scope (optional, derived from outline or tags)
- location in scope (optional)
- active tool (what the engine is doing right now)

### 2.2 Action Panel (Center)
Shows “what is happening”:
- prose stream (latest generated text excerpt)
- system log tail (planner decisions, tool calls, warnings/errors)
- current step indicator (SCAN / PLAN / EXECUTE)

### 2.3 Progress Panel (Right)
Shows “how the project is doing”:
- parts table (status, locks, word counts)
- chapter rollups (optional)
- integrity signals (missing files, schema drift)
- cost/tokens counters

---

## 3) Core Widgets (Recommended)

### 3.1 Active Task Card
Displays:
- `target_type` (should be `part`)
- `target_id` (Part ID)
- action (`DRAFT`, `REVISE`, `REVIEW`)
- reason (planner rationale)
- elapsed time

### 3.2 Matrix Table
Backed by `matrix.parts`:
- Part ID
- Chapter ID
- Status (`EMPTY`, `DRAFTING`, `REVIEW_READY`, `REVISION_NEEDED`, `LOCKED`)
- Locked indicator
- Word count
- Last modified

### 3.3 Integrity / Alerts
Derived from scan validation:
- missing manuscripts
- outline/template mismatch (thread keys)
- orphan manuscripts
- locked conflicts

### 3.4 Token / Cost Tracker (Optional)
If token tracking is enabled:
- tokens in / out per step
- per-session totals
- optional cost estimate

---

## 4) Control Surface (Director Override)

The dashboard may write control signals to a project-scoped control file, e.g.:

- `projects/&lt;project_id&gt;/data/control.json`

Typical fields (recommended):

```json
{
  "run_state": "RUNNING",
  "architect_override": {
    "force_target_part_id": null,
    "force_action": null,
    "note": null
  }
}
````

### 4.1 Run states

* `RUNNING`
* `PAUSED`
* `STOPPED`

### 4.2 Override behaviors (Recommended)

Overrides should be applied at PLAN-time:

* force focus on a specific part
* force a specific action (review vs revise)
* inject a short director note that becomes part of the plan rationale

The deterministic loop remains intact:

* overrides influence selection, not execution ordering

Pipeline: **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**

---

## 5) Multi-Project UX

If multiple projects exist, dashboard SHOULD display:

* active project id
* quick switch mechanism (implementation dependent)
* project health summary

Multi-project: **[Multi-Project Management](../multi-project-management)**

---

## 6) Failure and Recovery UX

The dashboard should handle:

* engine crash (dashboard stays up)
* engine restart (dashboard reconnects to state files)
* partial writes (prefer atomic writes in engine)

Recommended indicators:

* “Engine heartbeat” or “last update timestamp” from Matrix
* last SCAN time
* last EXECUTE time

Matrix includes timestamps: **[Central Matrix](../central-matrix-runtime-state)**

---

## 7) What the Dashboard Should Not Do

* It should not run LLM calls.
* It should not mutate manuscripts directly.
* It should not edit Story Bible files directly unless explicitly a dedicated editor UI (separate feature).

All modifications should go through the engine tools layer.

---

## 8) Related Pages

* **[Central Matrix](../central-matrix-runtime-state)**
* **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**
* **[Story Bible](../story-bible-creative-intent)**
* **[Multi-Project Management](../multi-project-management)**
    </file>
    <file path="technology/swarmcraft/runtime/multi-project-management/page.mdx" size="4507" lines="164" kind="source">
export const metadata = {
  title: "Multi-Project Management"
};

# Multi-Project Management

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

SwarmCraft supports multiple isolated projects (“universes”) in the same repository/runtime.

A **project** is an isolated unit that contains:
- its own Story Bible (templates + outline + references)
- its own Central Matrix (runtime state)
- its own RAG Memory DB
- its own manuscripts and logs (recommended)

This prevents cross-story contamination and enables switching between worlds without restarting the whole engine.

---

## 1) Project Root Structure (Recommended)

SwarmCraft SHOULD use a `projects/` root:

```text
root/
└── projects/
    ├── .last_project
    │
    ├── project_alpha/
    │   └── data/
    │       ├── matrix.json
    │       ├── story_bible/
    │       │   ├── outline.json
    │       │   └── templates/
    │       ├── memory_db/
    │       └── manuscripts/
    │
    └── project_beta/
        └── data/
            ├── matrix.json
            ├── story_bible/
            ├── memory_db/
            └── manuscripts/
````

Notes:

* `projects/&lt;project_id&gt;/data/` is the canonical project data root.
* Manuscripts under `data/manuscripts/` is recommended to keep everything portable.

---

## 2) Project Identity

Each project is identified by its folder name:

* `project_id = "project_alpha"`

The project ID should be:

* stable (do not rename casually)
* filesystem-safe (ASCII, no spaces recommended)

---

## 3) What Isolated Means (Normative)

For correctness, projects MUST NOT share:

* `matrix.json` (runtime state)
* `story_bible/` (creative intent)
* `memory_db/` (RAG vectors)

If your implementation uses global caches, they MUST be keyed by `project_id`.

---

## 4) Switching Projects

A project manager component (implementation detail) typically supports:

* list available projects
* set active project
* remember last active project

### 4.1 `.last_project` (Recommended)

A simple pointer file:

* `projects/.last_project`

Contents:

```text
project_alpha
```

On startup:

* if an explicit project is not chosen, engine loads `.last_project`
* if missing, engine may prompt or choose a default (implementation choice)

---

## 5) Per-Project Lifecycle

### 5.1 Create a project (Recommended behavior)

Creating a project should:

1. Create `projects/&lt;project_id&gt;/data/`
2. Create an initial Story Bible structure:

   * `story_bible/templates/`
   * `story_bible/outline.json` (blank scaffold)
3. Initialize `matrix.json` (empty/initial state)
4. Initialize `memory_db/` (empty DB folder)
5. Optionally create `manuscripts/` folder

### 5.2 Delete a project (Recommended behavior)

Deletion should be explicit and guarded.
Recommended to require a confirmation flag.

---

## 6) How Multi-Project Interacts with the Pipeline

In the deterministic loop, the orchestrator operates against exactly one active project:

* SCAN reads/writes `projects/&lt;id&gt;/data/*`
* PLAN selects the next Part within that project
* EXECUTE writes manuscripts and updates Matrix within that project
* RAG ingestion/retrieval is scoped to that project’s `memory_db/`

Pipeline: **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**

---

## 7) Dashboard Considerations

The dashboard should clearly display:

* active project ID
* active part/chapter target
* per-project token usage (session + optional totals)
* health/integrity signals for that project only

Dashboard: **[Dashboard TUI Reference](../dashboard-tui-reference)**

---

## 8) Related Pages

* **[Central Matrix](../central-matrix-runtime-state)**
* **[Story Bible](../story-bible-creative-intent)**
* **[RAG Memory System](../rag-memory-system)**
* **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**
    </file>
    <file path="technology/swarmcraft/runtime/orchestration-slice-by-slice-prompt-hydration/page.mdx" size="6798" lines="221" kind="source">
export const metadata = {
  title: "Orchestration Slice-by-Slice Prompt Hydration"
};

# Orchestration Slice-by-Slice Prompt Hydration

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

SwarmCraft avoids “prompt sprawl” by hydrating prompts with **only the active slice of the story**.

**Slice-by-slice prompt hydration** means:
- the engine targets **one Part**
- it injects only the **beats + contract** for that Part
- it adds only the **minimum continuity** required to write that Part correctly
- it runs **one persona** for **one atomic action**
- it returns to SCAN

This is the core mechanism that prevents scaffold repetition from becoming prose repetition.

---

## 1) Inputs to Prompt Hydration

Hydration assembles a prompt from four categories:

1. **Part slice (required)**
   - the target Part’s thread beats (grid cells)
   - the target Part’s contract (goal/obstacle/turn/outcome)

2. **Local continuity (required, minimal)**
   - previous Part outcome (or last approved summary)
   - chapter summary / chapter objective (if available)
   - any hard constraints that apply to this moment (POV/tense/reading level)

3. **Global references (optional, clipped)**
   - character snippets relevant to the scene
   - location/lore snippets relevant to the scene

4. **Memory retrieval (optional, bounded)**
   - top-k RAG snippets relevant to this Part’s contract/beat queries

Related sources:
- Outline: **[Schema Outline](../schema-outline)**
- Template threads/cadence: **[Schema Templates](../schema-templates)**
- RAG: **[RAG Memory System](../rag-memory-system)**

---

## 2) The Hydration Contract (Normative)

The orchestrator MUST obey these constraints:

### 2.1 One Part only
Hydration MUST be for exactly one `part_id` at a time.

### 2.2 Minimalism
Hydration MUST:
- include only beats for the target part
- exclude beats for other parts unless explicitly requested as continuity summaries

### 2.3 Bounded memory
If RAG is enabled:
- retrieval MUST be bounded (top-k, token-limited)
- retrieved snippets MUST be formatted as “evidence,” not as new intent

### 2.4 Locks respected
If `outline.parts[part_id].locks.manuscript == true`:
- orchestrator MUST NOT hydrate a drafting prompt for that part
- only review/inspection is allowed (implementation choice)

Matrix locking semantics: **[Central Matrix](../central-matrix-runtime-state)**

---

## 3) Recommended Prompt Structure (Persona-Agnostic)

SwarmCraft prompts are assembled in stable sections so they are debuggable and repeatable.

Recommended high-level sections:

1. **System / Persona role**
2. **Task definition**
3. **Part contract**
4. **Part beats (thread grid cells)**
5. **Local continuity summary**
6. **Relevant Story Bible excerpts (clipped)**
7. **RAG memory evidence (clipped)**
8. **Output format requirements**

---

## 4) Concrete Hydration Payload (Recommended Shape)

This is the structured payload the orchestrator can assemble before rendering a text prompt:

```json
{
  "target": {
    "chapter_id": "CH01",
    "part_id": "P001",
    "action": "DRAFT"
  },
  "contract": {
    "goal": "...",
    "obstacle": "...",
    "turn": "...",
    "outcome": "..."
  },
  "beats": {
    "Plot": "...",
    "Character Development": "...",
    "Conflict": "...",
    "Themes": "...",
    "World-Building": "...",
    "Emotion": "...",
    "Symbolism and Imagery": "...",
    "Structure": "...",
    "Relationships": "..."
  },
  "continuity": {
    "previous_part_outcome": "N/A (first part)",
    "chapter_summary": "Global crisis exposes systemic failure.",
    "must_preserve": [
      "POV: third limited",
      "Tense: past",
      "Tone: clear, cinematic"
    ]
  },
  "references": {
    "characters": [
      {"id": "king_klown", "snippet": "Core traits, current arc state..."}
    ],
    "locations": [],
    "lore": []
  },
  "rag_evidence": [
    {"source": "P000.md", "snippet": "Earlier mention of ..."},
    {"source": "king_klown.md", "snippet": "Established constraint ..."}
  ],
  "output": {
    "format": "markdown",
    "length_target_words": 900,
    "must_hit_contract": true
  }
}
````

The persona sees a rendered version of this payload, not the entire project.

---

## 5) How the Slice Prevents Repetition

Scaffold repetition is acceptable because it is planning data. Prose repetition is not.

Hydration prevents repetition by ensuring:

* the model is not repeatedly asked to re-summarize all parts
* only the current part’s obligations are active
* continuity comes in as short “must-preserve” facts and evidence, not as broad story retellings

---

## 6) Editor Hydration (Review Mode)

Editor prompts should be even narrower:

Include:

* part manuscript text (the thing being reviewed)
* contract + beats for that part
* any “must preserve” constraints
* minimal memory evidence (only if needed to verify)

Exclude:

* beats from other parts
* unrelated lore/characters

Review outputs SHOULD be structured:

* pass/fail per contract field
* pass/fail per required thread beat (cadence-driven)
* revision directives (actionable bullets)
* recommended status transition (`REVIEW_READY` or `REVISION_NEEDED`)

---

## 7) Cadence Enforcement

Cadence rules come from the template and influence both PLAN and REVIEW:

* PLAN may select parts with missing required beats (per-part cadence)
* REVIEW may fail a part if required beats are not reflected in prose (when configured)

Template cadence: **[Schema Templates](../schema-templates)**

---

## 8) Implementation Notes (Recommended)

* Build the hydration payload as JSON first (for testing/logging).
* Render to text prompt deterministically from the payload.
* Log the hydrated payload with redactions (keys, tokens).
* Token-limit each section to prevent prompt overflow.

---

## 9) Related Pages

* **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**
* **[Story Scaffold](../story-scaffold-templates-outline-parts)**
* **[Schema Templates](../schema-templates)**
* **[Schema Outline](../schema-outline)**
* **[Central Matrix](../central-matrix-runtime-state)**
* **[RAG Memory System](../rag-memory-system)**
    </file>
    <file path="technology/swarmcraft/runtime/page.tsx" size="6798" lines="155" kind="source">
// app\technology\swarmcraft\runtime\page.tsx
// app/technology/swarmcraft/runtime/page.tsx
import Link from 'next/link';
import { 
  Terminal, 
  Layers, 
  BrainCircuit, 
  ArrowRight,
  Database,
  Cpu
} from 'lucide-react';

export const metadata = {
  title: "Runtime &amp; Operations – SwarmCraft",
  description: "Dashboard TUI, Multi-Project Management, RAG Memory, and Provider Adapters.",
};

export default function RuntimePage() {
  return (
    &lt;main className="max-w-5xl mx-auto px-6 py-12"&gt;
      
      {/* HEADER */}
      &lt;div className="mb-16 border-b border-slate-200 pb-8"&gt;
        &lt;div className="flex items-center gap-3 mb-4"&gt;
          &lt;div className="p-2 bg-slate-100 text-slate-700 rounded-lg"&gt;
            &lt;Terminal className="w-8 h-8" /&gt;
          &lt;/div&gt;
          &lt;h1 className="text-4xl font-bold text-slate-900"&gt;Runtime &amp; Operations&lt;/h1&gt;
        &lt;/div&gt;
        &lt;p className="text-xl text-slate-600 max-w-3xl leading-relaxed"&gt;
          The operational layer of SwarmCraft. These modules handle the user interface, project isolation, long-term memory retrieval, and LLM provider integration.
        &lt;/p&gt;
      &lt;/div&gt;

      {/* 1. DASHBOARD &amp; CONTROL */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;Terminal className="w-6 h-6 mr-3 text-primary" /&gt;
          Control Surfaces
        &lt;/h2&gt;
        
        &lt;div className="grid md:grid-cols-2 gap-8"&gt;
          {/* Dashboard */}
          &lt;Link 
            href="/technology/swarmcraft/runtime/dashboard-tui-reference"
            className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
          &gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-3 group-hover:text-primary"&gt;
              Dashboard TUI Reference
            &lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              The "Mission Control" interface. A terminal UI that observes the engine state (SCAN/PLAN/EXECUTE) without blocking it.
            &lt;/p&gt;
            &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              View Layout Specs &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          {/* Multi-Project */}
          &lt;Link 
            href="/technology/swarmcraft/runtime/multi-project-management"
            className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
          &gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-3 group-hover:text-primary"&gt;
              Multi-Project Management
            &lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              How SwarmCraft runs multiple isolated universes (Story Bibles + Matrices) in a single runtime without contamination.
            &lt;/p&gt;
            &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              View Isolation Logic &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* 2. MEMORY &amp; ORCHESTRATION */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;Database className="w-6 h-6 mr-3 text-blue-600" /&gt;
          Memory &amp; Integration
        &lt;/h2&gt;
        
        &lt;div className="grid gap-6"&gt;
          
          {/* RAG Memory */}
          &lt;Link 
            href="/technology/swarmcraft/runtime/rag-memory-system"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  RAG Memory System
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  Long-term continuity. Indexes manuscripts and notes to provide "Evidence" during drafting, preventing character drift.
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;BrainCircuit className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          {/* Prompt Hydration */}
          &lt;Link 
            href="/technology/swarmcraft/runtime/orchestration-slice-by-slice-prompt-hydration"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  Slice-by-Slice Prompt Hydration
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  The anti-sprawl mechanism. Injecting &lt;em&gt;only&lt;/em&gt; the active Part's beats and contract into the LLM context.
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;Layers className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          {/* Provider Adapter */}
          &lt;Link 
            href="/technology/swarmcraft/runtime/provider-adapter-grok"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  Provider Adapter: Grok
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  The normalization layer that keeps the engine model-agnostic while leveraging Grok for execution.
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;Cpu className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

        &lt;/div&gt;
      &lt;/section&gt;

      {/* FOOTER NAV */}
      &lt;div className="pt-8 border-t border-slate-100 flex justify-between text-sm"&gt;
        &lt;Link href="/technology/swarmcraft/core" className="text-slate-500 hover:text-primary"&gt;
          ← Back to Core Logic
        &lt;/Link&gt;
        &lt;Link href="/technology/swarmcraft/scaffold" className="text-slate-500 hover:text-primary"&gt;
          Next: Scaffold Schema →
        &lt;/Link&gt;
      &lt;/div&gt;

    &lt;/main&gt;
  );
}
    </file>
    <file path="technology/swarmcraft/runtime/provider-adapter-grok/page.mdx" size="5255" lines="208" kind="source">
export const metadata = {
  title: "Provider Adapter Grok"
};

# Provider Adapter Grok

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

SwarmCraft is powered by **Grok** through a dedicated **provider adapter** layer.

Goal:
- keep the engine **provider-agnostic**
- centralize API config, retries, and error handling
- normalize responses into a stable internal interface for personas and tools

This page documents the expected behavior of the Grok adapter, without tying the rest of the codebase to Grok-specific response shapes.

---

## 1) Responsibilities of the Provider Adapter

The Grok adapter MUST:
1. Build requests from SwarmCraft’s internal prompt format.
2. Send requests to Grok with correct auth + model settings.
3. Normalize responses into:
   - `text` output
   - optional structured tool calls
   - token usage and latency metrics (if provided)
4. Apply robust retry/backoff rules.
5. Emit consistent errors for the orchestrator to handle deterministically.

The adapter SHOULD:
- support multiple Grok models/profiles
- support streaming responses (optional)
- redact secrets in logs

---

## 2) Internal Provider Interface (Recommended)

SwarmCraft should call providers through a stable interface like:

```python
class LLMProvider:
    def generate(self, messages, tools=None, tool_choice=None, **opts) -&gt; ProviderResult:
        ...
````

Where `ProviderResult` is normalized:

```json
{
  "text": "string",
  "tool_calls": [
    {
      "name": "write_file",
      "arguments": { "path": "...", "content": "..." }
    }
  ],
  "usage": {
    "input_tokens": 0,
    "output_tokens": 0,
    "total_tokens": 0
  },
  "meta": {
    "model": "grok-...",
    "latency_ms": 0
  }
}
```

This allows the orchestrator to remain unchanged if you later add other providers.

---

## 3) Configuration (Recommended)

### 3.1 Environment variables

Recommended keys (names may vary by implementation):

* `GROK_API_KEY`
* `GROK_MODEL` (default model id)
* `GROK_BASE_URL` (optional, if not default)

### 3.2 Project-level overrides

Optional per-project settings file, e.g.:

* `projects/&lt;project_id&gt;/data/settings.json`

Recommended fields:

```json
{
  "llm": {
    "provider": "grok",
    "model": "grok-...",
    "temperature": 0.7,
    "max_tokens": 2000
  }
}
```

---

## 4) Tool Calling and Safety

SwarmCraft personas must not write files directly. They request tool calls.

The Grok adapter MUST support:

* passing tool schemas (function definitions)
* receiving tool calls (structured)
* returning them to the tool executor layer

Tool execution remains in SwarmCraft (not in Grok):

* the engine validates and runs tools
* tool results can be appended to the conversation as tool messages (implementation detail)

This preserves deterministic safety rules:

* path sandboxing
* atomic writes
* audit logs

---

## 5) Retries and Error Handling (Recommended)

### 5.1 Retry classes

Adapter SHOULD retry on:

* transient network failures
* 5xx server errors
* timeouts

Adapter SHOULD NOT retry blindly on:

* auth failures (401/403)
* invalid request payload (4xx)
* tool schema mismatch errors (fix required)

### 5.2 Backoff

Use exponential backoff with jitter.

### 5.3 Deterministic surfacing

Adapter errors MUST be normalized into stable error codes so the orchestrator can:

* mark task as failed
* re-plan
* pause if needed

Example normalized error:

```json
{
  "error": {
    "code": "PROVIDER_TIMEOUT",
    "message": "Request timed out after 60s",
    "retryable": true
  }
}
```

---

## 6) Token and Cost Tracking (Optional)

If Grok provides usage metadata, the adapter SHOULD emit it in `ProviderResult.usage`.

If usage is not provided, SwarmCraft MAY estimate tokens separately, but should mark them as estimates.

Token tracking integrates with dashboard/cost panels:

* **[Dashboard TUI Reference](../dashboard-tui-reference)**

---

## 7) How Grok Fits the Deterministic Pipeline

The provider is invoked only during **EXECUTE** (and optional planning calls if you LLM-assist planning).

The pipeline remains:

* SCAN: no provider calls required
* PLAN: deterministic selection (optionally assisted)
* EXECUTE: provider call(s) for one Part, one action

Pipeline: **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**

---

## 8) Related Pages

* **[Architecture Overview](../architecture-overview)**
* **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**
* **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**
* **[Dashboard TUI Reference](../dashboard-tui-reference)**
    </file>
    <file path="technology/swarmcraft/runtime/rag-memory-system/page.mdx" size="5589" lines="177" kind="source">
export const metadata = {
  title: "RAG Memory System"
};

# RAG Memory System

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

SwarmCraft uses **Retrieval Augmented Generation (RAG)** as an explicit long-term memory layer to maintain continuity across large stories.

RAG is not the Story Bible (intent).  
RAG is not the Matrix (runtime state).  
RAG is **evidence**: previously written text and relevant notes that can be retrieved and injected into the current Part’s prompt.

---

## 1) Why RAG Exists

LLMs have limited context windows. Without memory, long projects drift:
- character traits change
- facts get contradicted
- timeline breaks
- names/places mutate

RAG solves this by:
- indexing written prose and notes as searchable chunks
- retrieving only the top relevant snippets for the active Part
- injecting them as bounded “evidence” during drafting and review

---

## 2) What RAG Stores

Recommended indexed sources:
- Part manuscripts (`data/manuscripts/*.md`)
- Editor notes (optional)
- stable reference notes (optional, if you want them searchable)

RAG SHOULD store per-chunk metadata:
- `project_id`
- `part_id` (if applicable)
- `chapter_id` (if applicable)
- `source_path`
- `timestamp`
- optional tags (character names, location IDs)

RAG MUST NOT be treated as canonical intent. If RAG conflicts with Story Bible, Story Bible wins.

See: **[Story Bible](../story-bible-creative-intent)**

---

## 3) Storage Location (Per Project)

Recommended location:
- `projects/&lt;project_id&gt;/data/memory_db/`

This ensures multi-project isolation and avoids cross-story contamination.

See: **[Multi-Project Management](../multi-project-management)**

---

## 4) Ingestion (Write Path)

Ingestion typically occurs during **SCAN**.

Flow:
1. Scanner detects changed manuscript files.
2. File is chunked into semantic segments (paragraphs or sections).
3. Each chunk is embedded (vectorized).
4. Vectors + metadata are stored in the project’s vector database.

Deterministic pipeline: **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**

### 4.1 Chunking recommendations
- chunk by paragraph boundaries when possible
- target 150–500 tokens per chunk (implementation-dependent)
- include stable headers in metadata rather than duplicating them in every chunk

### 4.2 Deduplication recommendations
- hash chunk text + source to avoid re-inserting identical chunks
- update metadata timestamps on re-ingest

---

## 5) Retrieval (Read Path)

Retrieval occurs during **prompt hydration** for a Part.

Flow:
1. Orchestrator builds a query set from:
   - the Part contract fields (goal/obstacle/turn/outcome)
   - high-signal beats (Plot/Conflict)
   - explicit continuity questions (if present)
2. Vector search returns top-k relevant chunks.
3. Orchestrator formats them as “evidence” and injects them into the prompt.

Prompt hydration: **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**

### 5.1 Bounded retrieval (required)
To prevent prompt bloat:
- `top_k` MUST be bounded (e.g., 5–10)
- total evidence tokens MUST be bounded
- prefer fewer, higher-confidence chunks over many weak chunks

### 5.2 Evidence formatting (recommended)
Format each retrieved chunk with:
- source + part ID
- a short excerpt
- optional “why retrieved” hint

Example:

```text
[RAG] Source: P014.md (CH05 / P014)
- "Mara always keeps her left hand gloved..."
````

---

## 6) RAG in Drafting vs Reviewing

### 6.1 Narrator (Draft/Revise)

Use RAG to:

* preserve continuity facts
* recall prior scene outcomes
* maintain consistent voice and details

### 6.2 Editor (Review)

Use RAG to:

* verify consistency (“does this contradict earlier text?”)
* confirm details (names, timeline, injuries, locations)

Editor should treat RAG as evidence, not instructions.

---

## 7) Interfaces and Tools (Recommended)

Expose RAG through a small tool surface, e.g.:

* `memory.query(text, top_k=...)`
* optional: `memory.query_filters(part_id=..., chapter_id=...)`

Recommended role rules:

* Narrator can request retrieval through orchestrator hydration, but does not directly call DB.
* Editor may call a controlled `check_memory` tool for spot checks.

---

## 8) Failure Modes and Guards

* **Hallucinated “memories”**: mitigate by only injecting retrieved text excerpts.
* **Cross-project bleed**: mitigate with per-project DB path + required `project_id` filter.
* **Contradicting intent**: Story Bible overrides RAG; Editor should flag conflicts.
* **Prompt overflow**: enforce strict token budgets for evidence.

---

## 9) Related Pages

* **[Architecture Overview](../architecture-overview)**
* **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**
* **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**
* **[Story Bible](../story-bible-creative-intent)**
* **[Central Matrix](../central-matrix-runtime-state)**
    </file>
    <file path="technology/swarmcraft/scaffold/outline-grid-csv-round-trip/page.mdx" size="5900" lines="199" kind="source">
export const metadata = {
  title: "Outline Grid CSV Round-Trip"
};

# Outline Grid CSV Round-Trip

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

SwarmCraft supports a **human-friendly grid editor** for the outline and a **CSV round-trip** workflow.

Goal:
- Humans can edit the scaffold like a spreadsheet.
- The system can import/export without losing structure.
- Stable Part IDs preserve meaning across edits.

This page specifies how `outline.json` maps to a grid and how CSV import/export should behave.

---

## 1) Grid Model

### 1.1 What the grid shows
- **Columns**: Parts (ordered by chapter then part order)
- **Rows**: Threads (from the active template)
- **Cells**: A thread beat for that Part

Source of truth:
- Threads come from `templates/&lt;template_id&gt;.json`  
  See: **[Schema Templates](../schema-templates)**
- Beats come from `outline.json`  
  See: **[Schema Outline](../schema-outline)**

### 1.2 What is NOT in the grid (by default)
- Part contract fields (goal/obstacle/turn/outcome)  
  These may be shown in a separate panel, side sheet, or a second table export format.
- Manuscripts (prose) are not in the outline grid.

---

## 2) Canonical Mapping: Outline → Grid

Given:
- `threads = template.threads[]`
- ordered parts = `for each chapter in chapters[]: chapter.part_ids[]`

Cell mapping:

```

cell(thread, part_id) = outline.parts[part_id].beats[thread] (string or empty)

````

If the key is missing, treat it as empty when projecting:
- missing beat keys MUST NOT break UI or export
- export should emit an empty string for missing cells

---

## 3) CSV Export Format (Beats Grid)

### 3.1 Shape
- First column: thread name
- Remaining columns: `Part 1`, `Part 2`, … in display order
- Optional second header row: stable `part_id` values (recommended)

The beats-only CSV is a projection of the grid (threads × parts).

### 3.2 Recommended headers (two-row header)
Row 1 (human labels):
- column 1: empty or `Thread`
- columns 2..N: `Part 1`, `Part 2`, … (or chapter-aware labels)

Row 2 (machine IDs):
- column 1: `part_id`
- columns 2..N: `P001`, `P002`, …

This allows the CSV to remain stable even if “Part 3” is moved into a different chapter.

### 3.3 Example
```csv
,Part 1,Part 2,Part 3
part_id,P001,P002,P003
Plot,"The World in Turmoil...","The Revelation...","A Seed of Hope..."
Character Development,"King Klown feels helpless...","Insight sparks vision...","Cautious optimism..."
Conflict,"Global crisis escalates...","Self-doubt...","Opposition begins..."
````

---

## 4) CSV Import Rules (Beats Grid)

### 4.1 Primary key: part_id row

Import SHOULD use the `part_id` row as the canonical mapping.

* If present, `part_id` row MUST be used.
* If absent, importer MAY fall back to positional mapping (less safe).

### 4.2 Thread name matching

Rows are matched by thread name (first column).

* Exact match is recommended.
* Whitespace trimming is recommended.
* Unknown thread names:

  * either reject with validation error, or
  * store in `outline.parts[*].beats` only if the system supports custom threads (not recommended by default)

### 4.3 Lock compliance (required)

Importer MUST respect outline locks:

* If `outline.parts[part_id].locks.beats == true`, that part’s beats MUST NOT be modified by CSV import.

Recommended behavior:

* import produces a report:

  * updated cells count
  * skipped due to locks
  * unknown threads
  * unknown part_ids

---

## 5) Handling Template Changes (Thread Set Changes)

If the user changes template (or template version):

* The grid row set changes (threads list changes)
* The system SHOULD provide a migration layer:

  * missing threads: create empty beats
  * removed threads: preserve in outline as “orphaned” beats only if explicitly enabled, otherwise drop with confirmation

Recommended: do not silently delete beats.

---

## 6) Parts/Chapter Changes (Column Reflow)

If the user changes `parts_per_chapter`:

* Part IDs must remain stable.
* The UI may re-group columns under different chapters.
* CSV export order may change, but the `part_id` row remains stable.

Rule:

* do not rename existing part IDs when reflowing chapters
* generate new part IDs only when new parts are created

---

## 7) Optional: Contract CSV Export

Beats CSV is intentionally simple. If you want contract editing in CSV, use a second file:

`outline_contract.csv` (recommended)

Shape:

```csv
part_id,chapter_id,title,goal,obstacle,turn,outcome
P001,CH01,Part 1,"...","...","...","..."
```

Contract import MUST respect `locks.contract`.

---

## 8) Relationship to Writing and Orchestration

The grid is a scaffold. The engine uses it slice-by-slice:

* during PLAN to detect missing beats (cadence)
* during EXECUTE to inject only the active Part slice
* during REVIEW to validate manuscript coverage of beats + contract

See:

* **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**
* **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**

---

## 9) Related Pages

* **[Story Scaffold](../story-scaffold-templates-outline-parts)**
* **[Schema Templates](../schema-templates)**
* **[Schema Outline](../schema-outline)**
* **[Central Matrix](../central-matrix-runtime-state)**
    </file>
    <file path="technology/swarmcraft/scaffold/page.tsx" size="7010" lines="162" kind="source">
// app\technology\swarmcraft\scaffold\page.tsx
// app/technology/swarmcraft/scaffold/page.tsx
import Link from 'next/link';
import { 
  Grid, 
  FileJson, 
  Book, 
  Table, 
  ArrowRight, 
  LayoutTemplate 
} from 'lucide-react';

export const metadata = {
  title: "Story Scaffold &amp; Schema – SwarmCraft",
  description: "Templates, Outlines, and the Grid system for structured narrative planning.",
};

export default function ScaffoldPage() {
  return (
    &lt;main className="max-w-5xl mx-auto px-6 py-12"&gt;
      
      {/* HEADER */}
      &lt;div className="mb-16 border-b border-slate-200 pb-8"&gt;
        &lt;div className="flex items-center gap-3 mb-4"&gt;
          &lt;div className="p-2 bg-slate-100 text-slate-700 rounded-lg"&gt;
            &lt;Grid className="w-8 h-8" /&gt;
          &lt;/div&gt;
          &lt;h1 className="text-4xl font-bold text-slate-900"&gt;Story Scaffold &amp; Schema&lt;/h1&gt;
        &lt;/div&gt;
        &lt;p className="text-xl text-slate-600 max-w-3xl leading-relaxed"&gt;
          The structural backbone of SwarmCraft. Before prose is written, the story exists as a structured &lt;strong&gt;Scaffold&lt;/strong&gt; defined by JSON schemas and editable via a Grid interface.
        &lt;/p&gt;
      &lt;/div&gt;

      {/* 1. CONCEPT &amp; INTENT */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;Book className="w-6 h-6 mr-3 text-primary" /&gt;
          Creative Intent
        &lt;/h2&gt;
        
        &lt;div className="grid md:grid-cols-2 gap-8"&gt;
          {/* Story Bible */}
          &lt;Link 
            href="/technology/swarmcraft/scaffold/story-bible-creative-intent"
            className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
          &gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-3 group-hover:text-primary"&gt;
              The Story Bible
            &lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              The canonical home for creative intent. Distinct from runtime state, it holds characters, lore, and the scaffold itself.
            &lt;/p&gt;
            &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              View Bible Structure &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          {/* Story Scaffold */}
          &lt;Link 
            href="/technology/swarmcraft/scaffold/story-scaffold-templates-outline-parts"
            className="group block p-8 bg-slate-50 border border-slate-200 rounded-xl hover:border-primary transition-all"
          &gt;
            &lt;h3 className="text-xl font-bold text-slate-900 mb-3 group-hover:text-primary"&gt;
              The Scaffold Model
            &lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6 text-sm leading-relaxed"&gt;
              How &lt;strong&gt;Templates&lt;/strong&gt; (threads/pacing) and &lt;strong&gt;Outlines&lt;/strong&gt; (beats/contracts) combine to drive the deterministic engine.
            &lt;/p&gt;
            &lt;div className="inline-flex items-center font-bold text-slate-700 text-sm"&gt;
              View Scaffold Logic &lt;ArrowRight className="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* 2. SCHEMA DEFINITIONS */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;FileJson className="w-6 h-6 mr-3 text-blue-600" /&gt;
          JSON Schemas
        &lt;/h2&gt;
        
        &lt;div className="grid gap-6 md:grid-cols-2"&gt;
          
          &lt;Link 
            href="/technology/swarmcraft/scaffold/schema-templates"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  Schema: Templates
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  Defining thread sets ("Plot", "Theme") and cadence rules.
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;LayoutTemplate className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          &lt;Link 
            href="/technology/swarmcraft/scaffold/schema-outline"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-blue-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-blue-700 mb-2"&gt;
                  Schema: Outline
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  Defining chapters, parts, and the "Part Contract" (Goal/Obstacle/Turn).
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;FileJson className="w-5 h-5 text-slate-300 group-hover:text-blue-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

        &lt;/div&gt;
      &lt;/section&gt;

      {/* 3. TOOLS &amp; EDITING */}
      &lt;section className="mb-16"&gt;
        &lt;h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center"&gt;
          &lt;Table className="w-6 h-6 mr-3 text-emerald-600" /&gt;
          Tools &amp; Editing
        &lt;/h2&gt;
        
        &lt;div className="grid gap-6"&gt;
          &lt;Link 
            href="/technology/swarmcraft/scaffold/outline-grid-csv-round-trip"
            className="group block p-6 bg-white border border-slate-200 rounded-xl hover:border-emerald-500 hover:shadow-md transition-all"
          &gt;
            &lt;div className="flex justify-between items-start"&gt;
              &lt;div&gt;
                &lt;h3 className="text-lg font-bold text-slate-900 group-hover:text-emerald-700 mb-2"&gt;
                  Outline Grid &amp; CSV Round-Trip
                &lt;/h3&gt;
                &lt;p className="text-slate-600 text-sm"&gt;
                  How the JSON outline is projected into a spreadsheet-like Grid for human editing, and how CSV import/export preserves structure.
                &lt;/p&gt;
              &lt;/div&gt;
              &lt;Table className="w-5 h-5 text-slate-300 group-hover:text-emerald-500" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;
        &lt;/div&gt;
      &lt;/section&gt;

      {/* FOOTER NAV */}
      &lt;div className="pt-8 border-t border-slate-100 flex justify-between text-sm"&gt;
        &lt;Link href="/technology/swarmcraft/core" className="text-slate-500 hover:text-primary"&gt;
          ← Back to Core Logic
        &lt;/Link&gt;
        &lt;Link href="/technology/swarmcraft/runtime" className="text-slate-500 hover:text-primary"&gt;
          Next: Runtime &amp; Ops →
        &lt;/Link&gt;
      &lt;/div&gt;

    &lt;/main&gt;
  );
}
    </file>
    <file path="technology/swarmcraft/scaffold/schema-outline/page.mdx" size="6615" lines="246" kind="source">
export const metadata = {
  title: "Schema Outline"
};

# Schema Outline

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

`outline.json` is the canonical **story plan** for a project. It defines:

- chapter ordering
- chapter → parts mapping
- per-part thread beats (grid cells)
- per-part contract (goal / obstacle / turn / outcome)
- locks to protect manual edits

File location:
- `data/story_bible/outline.json`

Story scaffold overview: **[Story Scaffold](../story-scaffold-templates-outline-parts)**

---

## 1) Design Goals

The outline MUST support:
- user-chosen parts/chapter (within template bounds)
- stable part IDs (critical for tracking and CSV round-trip)
- different template thread sets
- per-part beat editing in a grid UI
- per-part contract strictness for slice-by-slice drafting
- lock scopes (beats / contract / manuscript)

The outline SHOULD:
- be readable to humans
- be resilient to partial data (empty beats allowed)
- be forward-compatible (versioned)

---

## 2) Minimal Outline Schema (Recommended)

```json
{
  "version": 1,
  "template_id": "novel.default",

  "settings": {
    "parts_per_chapter": 3
  },

  "chapters": [
    {
      "chapter_id": "CH01",
      "title": "The World in Turmoil",
      "summary": "Global crisis exposes systemic failure.",
      "part_ids": ["P001", "P002", "P003"]
    }
  ],

  "parts": {
    "P001": {
      "chapter_id": "CH01",
      "title": "Part 1",
      "order_index": 1,

      "contract": {
        "goal": "Show the world failing in a concrete, personal way.",
        "obstacle": "Institutions deny and deflect responsibility.",
        "turn": "A specific event forces the truth into the open.",
        "outcome": "King Klown realizes the crisis is structural, not isolated."
      },

      "beats": {
        "Plot": "The World in Turmoil: global crisis exposes systemic failure.",
        "Character Development": "King Klown feels helpless and isolated.",
        "Conflict": "External disaster + internal doubt collide.",
        "Themes": "Collapse and urgent need for change.",
        "World-Building": "Introduce fractured institutions and scarcity.",
        "Emotion": "Despair and urgency.",
        "Symbolism and Imagery": "Fractured world as collective neglect.",
        "Structure": "Open with stakes and central conflict.",
        "Relationships": "King Klown is disconnected from others."
      },

      "locks": {
        "beats": false,
        "contract": false,
        "manuscript": false
      }
    }
  }
}
````

Notes:

* `chapters[]` defines the ordered reading structure.
* `parts{}` is the canonical per-part content.
* `beats` keys should match template threads.

---

## 3) Field Semantics

### 3.1 `version` (required)

Schema version for migration and validation.

### 3.2 `template_id` (required)

Must match an existing template file in:

* `templates/&lt;template_id&gt;.json`

Template schema: **[Schema Templates](../schema-templates)**

### 3.3 `settings.parts_per_chapter` (recommended)

User override of parts/chapter within template bounds.

If omitted, the engine may use the template default.

### 3.4 `chapters[]` (required)

Ordered list of chapters.

Each chapter SHOULD contain:

* `chapter_id` (stable ID)
* `title`
* optional `summary`
* `part_ids` ordered list (critical)

### 3.5 `parts{}` (required)

Map of Part IDs to Part objects.

Each Part MUST contain:

* `chapter_id` (must match one chapter)
* `order_index` (global or chapter-local; implementation choice)
* `beats` object
* `contract` object (recommended)
* `locks` object (recommended)

---

## 4) Part Contract (Strict Slice Anchor)

The **contract** is the minimum “what must happen” spec for a Part.

Recommended fields:

* `goal`
* `obstacle`
* `turn`
* `outcome`

The orchestrator uses the contract to:

* hydrate slice prompts
* give the Editor a rubric for validation

Related:

* **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**

---

## 5) Beat Keys and Template Compatibility

Beat keys MUST align with the active template’s threads.

Validation SHOULD ensure:

* every `beats` key exists in `template.threads`
* missing keys are treated as empty when projecting to grid/CSV
* extra keys are flagged (or preserved if you support custom threads)

---

## 6) Locks (Manual Edit Protection)

Locks prevent automation from overwriting human edits.

Recommended lock scopes:

* `beats`: prevents automated changes to beats
* `contract`: prevents automated changes to contract
* `manuscript`: prevents automated changes to prose file

Planner rule:

* PLAN MUST NOT select a part for an action that violates locks.

Matrix mapping:

* locked parts should appear as `LOCKED` (or `locked=true`) in Matrix.

Matrix: **[Central Matrix](../central-matrix-runtime-state)**

---

## 7) ID Stability Rules (Critical)

* `chapter_id` and `part_id` MUST be stable once created.
* Grid and CSV round-trip depend on stable `part_id`.
* Changing parts/chapter SHOULD preserve existing part IDs where possible.
* New splits SHOULD create new part IDs instead of renaming existing parts.

---

## 8) Relationship to Grid and CSV

The outline is projected into a grid for human editing:

* rows = template threads
* columns = parts (ordered by chapters / part_ids)
* cells = outline.parts[part_id].beats[thread_name]

Round-trip rules:

* CSV export is a projection of `beats`
* CSV import updates `beats` while respecting locks

See: **[Outline Grid CSV Round-Trip](../outline-grid-csv-round-trip)**

---

## 9) Relationship to Manuscripts and Matrix

* Outline defines intent (beats + contract).
* Manuscripts contain prose generated for each Part.
* Matrix tracks runtime status and file metrics.

See:

* **[Story Bible](../story-bible-creative-intent)**
* **[Central Matrix](../central-matrix-runtime-state)**
* **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**
    </file>
    <file path="technology/swarmcraft/scaffold/schema-templates/page.mdx" size="5190" lines="193" kind="source">
export const metadata = {
  title: "Schema Templates"
};

# Schema Templates

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

Templates define the **shape and pacing rules** of the Story Scaffold.

A template file lives at:

- `data/story_bible/templates/&lt;template_id&gt;.json`

It defines:
- thread set (grid rows)
- cadence rules (what must be filled, how often)
- default parts/chapter + allowed bounds
- optional style/genre constraints used in prompt hydration

Story scaffold overview: **[Story Scaffold](../story-scaffold-templates-outline-parts)**

---

## 1) Design Goals

Templates MUST support:
- multiple genres / formats (children book, novel, screenplay, etc.)
- user-chosen parts/chapter within allowed bounds
- different thread sets per template
- enforceable cadence (required beats per part or per chapter)
- stable projection into a grid and CSV

Templates SHOULD:
- remain small and readable for humans
- avoid storing story-specific content (that belongs in `outline.json`)

---

## 2) Minimal Template Schema (Recommended)

```json
{
  "template_id": "novel.default",
  "name": "Novel (Default)",
  "version": 1,

  "threads": [
    "Plot",
    "Character Development",
    "Conflict",
    "Themes",
    "World-Building",
    "Emotion",
    "Symbolism and Imagery",
    "Structure",
    "Relationships"
  ],

  "parts_per_chapter": {
    "default": 3,
    "min": 1,
    "max": 6
  },

  "cadence": {
    "per_part_required_threads": ["Plot", "Conflict"],
    "per_chapter_required_threads": ["Character Development", "Themes"],
    "soft_threads": ["Symbolism and Imagery", "World-Building"],
    "min_non_empty_cells_per_part": 3
  },

  "prompting": {
    "target_reading_level": "adult",
    "tone": ["clear", "cinematic"],
    "pov": "third_limited",
    "tense": "past",
    "hard_constraints_refs": [
      "constraints/hard_rules.md",
      "constraints/style_guide.md"
    ]
  }
}
````

Notes:

* `threads` defines the grid rows.
* `parts_per_chapter` defines defaults and bounds; users can override within bounds.
* `cadence` defines what the planner/editor should enforce.
* `prompting` provides reusable style constraints (optional).

---

## 3) Field Semantics

### 3.1 `template_id` (required)

Stable identifier used by the project config and Story Bible.

### 3.2 `threads` (required)

Ordered list of thread names.

* Order is the default order in the grid UI.
* Thread names must match outline beat keys (see Schema Outline).

### 3.3 `parts_per_chapter` (required)

Defines allowed splitting:

* `default`: recommended parts/chapter for this format
* `min`, `max`: permitted bounds for user override

### 3.4 `cadence` (recommended)

Cadence is enforcement guidance for PLAN/REVIEW.

Recommended keys:

* `per_part_required_threads`: must be non-empty in every part (unless explicitly waived)
* `per_chapter_required_threads`: must appear at least once per chapter (rollup check)
* `soft_threads`: encouraged but optional
* `min_non_empty_cells_per_part`: guard against under-specified parts

Cadence is a rule set for scaffold completeness; it does not guarantee prose quality.

### 3.5 `prompting` (optional)

Reusable prompt constraints and references:

* reading level / audience
* tone and voice
* POV and tense
* references into Story Bible Markdown files

If present, prompt hydration can include these in every Part slice.

---

## 4) Template Variants (Examples)

### 4.1 `children.picturebook.json`

Typical guidance:

* fewer threads
* `parts_per_chapter.default = 1`
* tighter cadence (Plot + Emotion required every part)

### 4.2 `novel.default.json`

Typical guidance:

* broader thread set
* `default = 3` parts/chapter
* encourages symbolism/worldbuilding but doesn’t require every part

### 4.3 `screenplay.default.json`

Typical guidance:

* threads oriented around beats, scene function, visuals
* strict structure cadence (inciting incident, midpoint, etc.) via outline contract usage

---

## 5) Validation Rules (Recommended)

A validator SHOULD enforce:

* `threads` is non-empty and has unique strings
* `parts_per_chapter.min &lt;= default &lt;= max`
* cadence thread names must exist in `threads`
* version is present and numeric

---

## 6) Relationship to Outline and Grid

* Template defines the *thread rows* and pacing rules.
* Outline defines the *per-part cell content*.

Related:

* **[Schema Outline](../schema-outline)**
* **[Outline Grid CSV Round-Trip](../outline-grid-csv-round-trip)**
* **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**
    </file>
    <file path="technology/swarmcraft/scaffold/story-bible-creative-intent/page.mdx" size="3271" lines="94" kind="source">
export const metadata = {
  title: "Story Bible (Creative Intent)"
};

# Story Bible (Creative Intent)

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

The **Story Bible** is SwarmCraft’s home for **creative intent**: the canonical narrative plan, constraints, and reference material that guide drafting and revision.

It is designed to be:
- **explicit** (no hidden “chat history” requirements)
- **versionable** (text/JSON files under source control)
- **sliceable** (only the relevant subset is injected into prompts)
- **human-editable** (writers can edit directly or through UI)

The Story Bible is distinct from:
- **Matrix** (runtime progress state): **[Central Matrix](../central-matrix-runtime-state)**
- **RAG memory** (retrieved continuity evidence): **[RAG Memory System](../rag-memory-system)**

---

## 1) Location and Project Isolation

**Recommended per-project location:**
- `projects/&lt;project_id&gt;/data/story_bible/`

Single-project setups MAY use:
- `data/story_bible/`

The Story Bible is project-scoped. Each project has its own Bible and must not share it implicitly.

See: **[Multi-Project Management](../multi-project-management)**

---

## 2) What Lives in the Story Bible

SwarmCraft treats the Story Bible as the canonical source for:

### 2.1 Canonical references
- Characters (bios, arcs, secrets, voice constraints)
- Locations (maps, rules, sensory signatures)
- Lore (history, factions, magic/tech rules)
- Style rules (tone, POV, reading level, taboo list)
- Constraints (hard rules the Editor enforces)

These may be stored in Markdown or JSON, depending on preference and tooling.

### 2.2 The Story Scaffold (New)
The scaffold is part of the Story Bible because it is **creative intent**, not runtime state:

- **Templates:** `templates/&lt;template_id&gt;.json`  
  Defines threads (Plot, Character Development, etc.), cadence expectations, and default parts/chapter.

- **Outline:** `outline.json`  
  Defines chapters → parts mapping, per-part thread beats, and per-part “contract”.

Scaffold entry point:
- **[Story Scaffold](../story-scaffold-templates-outline-parts)**

---

## 3) Recommended Folder Layout

Example:

```text
projects/&lt;project_id&gt;/data/story_bible/
├── README.md
├── outline.json
├── templates/
│   ├── children.picturebook.json
│   ├── novel.default.json
│   └── screenplay.default.json
├── characters/
│   ├── king_klown.md
│   └── ...
├── locations/
│   ├── cosmic_council_hall.md
│   └── ...
├── lore/
│   ├── world_rules.md
│   └── ...
├── constraints/
│   ├── hard_rules.md
│   └── style_guide.md
└── glossaries/
    └── terms.md
    </file>
    <file path="technology/swarmcraft/scaffold/story-scaffold-templates-outline-parts/page.mdx" size="5273" lines="169" kind="source">
export const metadata = {
  title: "Story Scaffold (Templates-Outline-Parts)"
};

# Story Scaffold (Templates-Outline-Parts)

&gt; **Architectural Lineage (Credits):**  
&gt; SwarmCraft is an **architectural fork and deep rewrite** of the multi-agent swarm engine created by **[Mojomast](https://github.com/mojomast)** in **[mojomast/swarmussy](https://github.com/mojomast/swarmussy)**.  
&gt; SwarmCraft’s deterministic “Architect-style” layering is also **derived from the meta-structure of Abstract Wiki Architect (AWA)**.  
&gt; Full details: **[Credits &amp; Lineage](../credits-and-lineage)**

## **POWERED BY GROK** 

The **Story Scaffold** is SwarmCraft’s structured planning layer used to generate and maintain narrative coherence. It is designed as a **human-editable grid** and a **machine-usable schema**.

It combines:
- **Templates**: what threads exist and how they should be paced
- **Outline**: chapters → parts mapping + per-part beats + part contracts
- **Parts**: the atomic unit the engine drafts/revises

The Scaffold lives inside the Story Bible:
- `data/story_bible/templates/&lt;template_id&gt;.json`
- `data/story_bible/outline.json`

See: **[Story Bible](../story-bible-creative-intent)**

---

## 1) Why Parts Exist

SwarmCraft drafts and revises **one Part at a time**.

A Part is the atomic unit because it enables:
- stable, small prompt slices
- targeted regeneration (one slice)
- better continuity control
- precise status tracking in Matrix

Chapters are **rollups** over Parts.

Related:
- **[Central Matrix](../central-matrix-runtime-state)**
- **[Deterministic Pipeline](../deterministic-pipeline-scan-plan-execute)**

---

## 2) Threads (Narrative Lanes)

A template defines the thread set used to structure the story, e.g.:

- Plot
- Character Development
- Philosophy
- Conflict
- Themes
- World-Building
- Emotion
- Symbolism and Imagery
- Structure
- Relationships

Threads are not the final prose. They are **scaffolding lanes** the engine uses to plan and draft.

---

## 3) Templates: Thread Set + Cadence + Default Parts/Chapter

A **template** defines:
- the thread list (rows in the grid)
- cadence rules (which threads are required per part/chapter, and at what frequency)
- default and allowed ranges for parts/chapter
- optional genre voice constraints

Schema: **[Schema Templates](../schema-templates)**

---

## 4) Outline: Chapters → Parts + Per-Part Beats + Part Contract

The **outline** is the canonical story plan for a project.

It defines:
- chapters and their ordered parts
- for each part:
  - beats per thread (grid cells)
  - a “part contract” (goal / obstacle / turn / outcome)
  - optional locks (protect manual edits)

Schema: **[Schema Outline](../schema-outline)**

---

## 5) Grid View (Human Editing)

The UI projects the outline into a grid:

- Columns = Parts (`P001`, `P002`, …)
- Rows = Threads (Plot, Character Dev, etc.)
- Cells = the beat for that thread in that part

This is designed to be:
- quick to scan
- easy to edit manually
- compatible with CSV round-trip when needed

Round-trip spec: **[Outline Grid CSV Round-Trip](../outline-grid-csv-round-trip)**

---

## 6) How the Scaffold Drives Writing

During execution, SwarmCraft hydrates prompts with **only the active Part slice**:

- that Part’s thread beats
- that Part’s contract
- minimal continuity (previous part outcome, relevant character/lore snippets)
- style constraints

This prevents the system from “re-summarizing the whole story” every time.

Details: **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**

---

## 7) Parts/Chapter: User-Configurable Splitting

Templates provide defaults, but the user can override:

- **Children / picture book:** typically `1 part = 1 chapter`
- **Novel:** often `3 parts/chapter` (default example)
- **Complex long-form:** up to `6 parts/chapter` where needed

Important constraint:
- parts must have stable IDs; changing parts/chapter should preserve existing part IDs wherever possible, or explicitly create new parts with new IDs.

---

## 8) Orchestration Expectations (Recommended)

The engine SHOULD:
- treat empty cells as “no beat required” unless cadence says otherwise
- enforce cadence at plan-time (PLAN chooses what needs filling)
- use Editor to verify manuscript covers the contract + beats
- lock parts once stable to avoid regressions

---

## 9) How This Relates to Matrix

- **Outline** is creative intent.
- **Matrix** is runtime progress.

Matrix stores:
- status per Part (EMPTY/DRAFTING/REVIEW_READY/REVISION_NEEDED/LOCKED)
- manuscript paths and metrics
- active_task target

Matrix page: **[Central Matrix](../central-matrix-runtime-state)**

---

## 10) Related Pages

- **[Story Bible](../story-bible-creative-intent)**
- **[Schema Templates](../schema-templates)**
- **[Schema Outline](../schema-outline)**
- **[Outline Grid CSV Round-Trip](../outline-grid-csv-round-trip)**
- **[Orchestration Slice-by-Slice Prompt Hydration](../orchestration-slice-by-slice-prompt-hydration)**
- **[Central Matrix](../central-matrix-runtime-state)**
    </file>
    <file path="technology/tools/page.mdx" size="4027" lines="63" kind="source">
export const metadata = {
  title: "Index"
};

﻿

##  AI-Assisted Development Workflow Toolkit

**Component:** Project Analysis, Code Extraction, and LLM Integration
**Role:** Automates the creation of highly precise, context-rich prompts for Large Language Models (LLMs) and manages the subsequent code re-integration.

---

## 1.  Overview and Methodology

This toolkit is a specialized workflow designed to create a tight feedback loop between a code repository and an external AI service (like ChatGPT) for systematic code analysis and modification. It orchestrates several Python scripts and configuration data to achieve granular control over which parts of the codebase are analyzed and updated.

### Core Workflow Principle (Scan $\rightarrow$ Prompt $\rightarrow$ Insert)

1.  **Scanning:** Systematically discovers all relevant files in a project directory.
2.  **Prompt Generation:** Extracts and concatenates specific, isolated **code blocks** (rather than entire files) to create a highly focused prompt for the LLM.
3.  **Re-Integration:** Applies the LLM's output directly back into the repository with high precision using line-aware insertion scripts.

---

## 2.  Core Utility Scripts

This group of Python scripts performs the essential tasks of path discovery, data extraction, and content modification.

| File Name | Function | Details |
| :--- | :--- | :--- |
| **`scanAppProjectForPaths.py`** | **Path Discovery** | Scans the project using `os.walk` to identify all relevant file paths, generating the initial dataset used by the system. |
| **`smart_dump.py`** | **Content Extraction** | Iterates over files defined in the configuration and concatenates their content, often with specific delimiters and headers, for easy input into the LLM. |
| **`concatFilesAndSubs.py`** | **Block Substitution** | Combines file contents and performs necessary text substitutions or block replacements based on defined configuration lists. |
| **`pythonInsert.py`** | **Precise Code Insertion** | Reads content (typically LLM output) and injects it at a precisely defined line or block marker within target Python files. |
| **`openInNotepad.py`** | **Inspection Utility** | A simple utility to quickly open files identified by the workflow in a local text editor (e.g., Notepad) for inspection. |

---

## 3.  Data Configuration and Mapping

The system's intelligence relies heavily on the structured data provided in these files (exports from `path_blocks_combinedv2.xlsx`), which serve as the configuration layer.

| File Name | Role in Workflow | Key Data Defined |
| :--- | :--- | :--- |
| **`path_blocks_combinedv2.xlsx - Paths.csv`** | **File Index** | The master list of all source files to be considered for analysis. |
| **`path_blocks_combinedv2.xlsx - Blocks.csv`** | **Code Isolation** | Defines specific, granular code segments or "blocks" within the files, allowing the prompt to be highly focused (e.g., only a single function definition). |
| **`path_blocks_combinedv2.xlsx - Concat_Fetch.csv`** | **Prompt Blueprint** | Specifies the exact sequence of files and blocks that `smart_dump.py` must combine to construct the prompt sent *to* the LLM. |
| **`path_blocks_combinedv2.xlsx - Concat_Give.csv`** | **Injection Blueprint** | Specifies the target paths and block markers where the modified code or LLM output must be inserted *into* the repository. |

---

## 4.  Automation and Maintenance

The workflow is managed by local scripts that handle version control and execution.

* **`GitSink.bat`:** A Windows Batch script used to automate common tasks such as triggering the Python scripts in sequence, handling file synchronization, and managing Git operations (e.g., commit/push). This script defines the robust, automated loop for the AI-assisted process.

---

##  Installation and Usage

*(Instructions for setting up the Python environment, dependencies, and initial execution would go here, sourced from the local `README.md`.)*
    </file>
    <file path="technology/voting-machine/integration/page.tsx" size="8842" lines="170" kind="source">
// app/technology/voting-machine/integration/page.tsx
import Link from 'next/link';
import { 
  Terminal, 
  FileText, 
  Code, 
  CheckCircle,
  ArrowLeft,
  LayoutTemplate
} from 'lucide-react';

export const metadata = {
  title: 'Integration &amp; Reporting – VM-ENGINE',
  description: 'Guide for embedding VM-ENGINE: CLI contracts, read-only reporting templates, and audit verification.',
};

export default function VMIntegration() {
  return (
    &lt;main className="min-h-screen bg-white py-16 px-6"&gt;
      &lt;div className="max-w-5xl mx-auto"&gt;
        
        {/* HEADER */}
        &lt;div className="mb-16"&gt;
          &lt;Link 
            href="/technology/voting-machine" 
            className="text-slate-500 hover:text-blue-600 font-medium flex items-center gap-2 mb-6 transition-colors"
          &gt;
            &lt;ArrowLeft className="w-4 h-4" /&gt; Back to VM-ENGINE Overview
          &lt;/Link&gt;
          &lt;h1 className="text-4xl font-bold text-slate-900 mb-6"&gt;
            Integration &amp; Reporting
          &lt;/h1&gt;
          &lt;p className="text-xl text-slate-600 leading-relaxed max-w-3xl"&gt;
            Treat the engine as a pure function. Invoke it via CLI, consume its canonical JSON outputs, 
            and render reports using read-only templates. Never re-compute allocations in the view layer.
          &lt;/p&gt;
        &lt;/div&gt;

        {/* 1. CLI CONTRACT */}
        &lt;section className="mb-20"&gt;
          &lt;div className="flex items-center gap-3 mb-8 border-b border-slate-200 pb-4"&gt;
            &lt;Terminal className="w-8 h-8 text-slate-900" /&gt;
            &lt;h2 className="text-2xl font-bold text-slate-900"&gt;1. The CLI Contract&lt;/h2&gt;
          &lt;/div&gt;
          
          &lt;div className="bg-slate-900 rounded-xl p-6 text-slate-200 font-mono text-sm mb-8 shadow-xl"&gt;
            &lt;div className="flex gap-2 mb-4 border-b border-slate-700 pb-2"&gt;
              &lt;span className="text-red-400"&gt;●&lt;/span&gt;
              &lt;span className="text-yellow-400"&gt;●&lt;/span&gt;
              &lt;span className="text-green-400"&gt;●&lt;/span&gt;
            &lt;/div&gt;
            &lt;p className="mb-4 text-slate-400"&gt;# Standard invocation pattern&lt;/p&gt;
            &lt;p className="mb-2"&gt;
              &lt;span className="text-green-400"&gt;vm_cli&lt;/span&gt; \
            &lt;/p&gt;
            &lt;p className="pl-4 mb-2"&gt;
              &lt;span className="text-blue-400"&gt;--registry&lt;/span&gt; ./inputs/registry.json \
            &lt;/p&gt;
            &lt;p className="pl-4 mb-2"&gt;
              &lt;span className="text-blue-400"&gt;--tally&lt;/span&gt;    ./inputs/tally.json \
            &lt;/p&gt;
            &lt;p className="pl-4 mb-2"&gt;
              &lt;span className="text-blue-400"&gt;--params&lt;/span&gt;   ./inputs/params.json \
            &lt;/p&gt;
            &lt;p className="pl-4 mb-2"&gt;
              &lt;span className="text-blue-400"&gt;--out&lt;/span&gt;      ./outputs/run_01
            &lt;/p&gt;
          &lt;/div&gt;

          &lt;div className="grid md:grid-cols-3 gap-6"&gt;
            &lt;div className="border border-slate-200 p-5 rounded-lg"&gt;
              &lt;strong className="block text-slate-900 mb-2"&gt;Exit Code 0&lt;/strong&gt;
              &lt;p className="text-sm text-slate-600"&gt;Success. All artifacts emitted and hashes verified.&lt;/p&gt;
            &lt;/div&gt;
            &lt;div className="border border-slate-200 p-5 rounded-lg"&gt;
              &lt;strong className="block text-red-600 mb-2"&gt;Exit Code 2&lt;/strong&gt;
              &lt;p className="text-sm text-slate-600"&gt;Validation Error. Input schema or referential integrity failed.&lt;/p&gt;
            &lt;/div&gt;
            &lt;div className="border border-slate-200 p-5 rounded-lg"&gt;
              &lt;strong className="block text-red-600 mb-2"&gt;Exit Code 3&lt;/strong&gt;
              &lt;p className="text-sm text-slate-600"&gt;Verification Failure. Post-run hash check mismatch (critical).&lt;/p&gt;
            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/section&gt;

        {/* 2. REPORTING RULES (DOC 7) */}
        &lt;section className="mb-20"&gt;
          &lt;div className="flex items-center gap-3 mb-8 border-b border-slate-200 pb-4"&gt;
            &lt;FileText className="w-8 h-8 text-blue-600" /&gt;
            &lt;h2 className="text-2xl font-bold text-slate-900"&gt;2. Reporting Rules (Doc 7)&lt;/h2&gt;
          &lt;/div&gt;
          
          &lt;div className="grid md:grid-cols-2 gap-12"&gt;
            &lt;div&gt;
              &lt;h3 className="text-lg font-bold text-slate-900 mb-4"&gt;The "Read-Only" Principle&lt;/h3&gt;
              &lt;p className="text-slate-600 mb-6"&gt;
                The renderer consumes &lt;code&gt;result.json&lt;/code&gt; and &lt;code&gt;run_record.json&lt;/code&gt;. 
                It &lt;strong&gt;must not&lt;/strong&gt; re-calculate shares, margins, or winners. 
                Visual logic is strictly separated from business logic.
              &lt;/p&gt;
              
              &lt;ul className="space-y-4"&gt;
                &lt;li className="flex gap-3"&gt;
                  &lt;CheckCircle className="w-5 h-5 text-green-500 flex-shrink-0" /&gt;
                  &lt;span className="text-sm text-slate-700"&gt;&lt;strong&gt;Numeric Format:&lt;/strong&gt; Percentages show one decimal (e.g., 54.5%). Round half up. No locale-specific separators in raw data.&lt;/span&gt;
                &lt;/li&gt;
                &lt;li className="flex gap-3"&gt;
                  &lt;CheckCircle className="w-5 h-5 text-green-500 flex-shrink-0" /&gt;
                  &lt;span className="text-sm text-slate-700"&gt;&lt;strong&gt;Ordering:&lt;/strong&gt; Allocation tables typically follow Registry order, not vote count, to preserve neutrality.&lt;/span&gt;
                &lt;/li&gt;
                &lt;li className="flex gap-3"&gt;
                  &lt;CheckCircle className="w-5 h-5 text-green-500 flex-shrink-0" /&gt;
                  &lt;span className="text-sm text-slate-700"&gt;&lt;strong&gt;Presentation Toggles:&lt;/strong&gt; Variables 060-062 control labels and language but do &lt;em&gt;not&lt;/em&gt; affect the Formula ID (FID).&lt;/span&gt;
                &lt;/li&gt;
              &lt;/ul&gt;
            &lt;/div&gt;

            &lt;div className="bg-slate-50 p-6 rounded-xl border border-slate-200"&gt;
              &lt;h3 className="text-sm font-bold text-slate-500 uppercase tracking-wider mb-4 flex items-center gap-2"&gt;
                &lt;LayoutTemplate className="w-4 h-4" /&gt; Report Footer Template
              &lt;/h3&gt;
              &lt;div className="bg-white p-4 rounded border border-slate-200 text-xs text-slate-500 font-mono space-y-2"&gt;
                &lt;p&gt;Formula ID: &lt;span className="text-slate-900"&gt;a3f9...8b21&lt;/span&gt;&lt;/p&gt;
                &lt;p&gt;Engine Version: &lt;span className="text-slate-900"&gt;v1.2.0&lt;/span&gt;&lt;/p&gt;
                &lt;p&gt;Algorithm Variant: &lt;span className="text-slate-900"&gt;v1 (Standard)&lt;/span&gt;&lt;/p&gt;
                &lt;div className="h-px bg-slate-100 my-2"&gt;&lt;/div&gt;
                &lt;p&gt;Tie Policy: &lt;span className="text-slate-900"&gt;random&lt;/span&gt;&lt;/p&gt;
                &lt;p&gt;RNG Seed: &lt;span className="text-slate-900"&gt;424242&lt;/span&gt; (Event count: 1)&lt;/p&gt;
              &lt;/div&gt;
              &lt;p className="text-xs text-slate-400 mt-4 italic"&gt;
                *Required disclosure block on every official report page.
              &lt;/p&gt;
            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/section&gt;

        {/* 3. VERIFICATION */}
        &lt;section className="mb-20"&gt;
          &lt;div className="flex items-center gap-3 mb-8 border-b border-slate-200 pb-4"&gt;
            &lt;Code className="w-8 h-8 text-purple-600" /&gt;
            &lt;h2 className="text-2xl font-bold text-slate-900"&gt;3. Independent Verification&lt;/h2&gt;
          &lt;/div&gt;
          &lt;p className="text-slate-600 mb-8"&gt;
            Any third party can verify a run by re-executing the engine with the provided inputs. 
            Verification succeeds if the output hashes match exactly.
          &lt;/p&gt;
          
          &lt;div className="bg-purple-50 border border-purple-100 rounded-xl p-6"&gt;
            &lt;h3 className="font-bold text-purple-900 mb-4"&gt;Formula ID (FID) Audit&lt;/h3&gt;
            &lt;p className="text-sm text-purple-800 mb-4"&gt;
              The FID is a hash of the &lt;strong&gt;Normative Manifest&lt;/strong&gt; (the algorithm rules + included variables). 
              It proves that the logic wasn't secretly tweaked for a specific run.
            &lt;/p&gt;
            &lt;div className="grid md:grid-cols-2 gap-4"&gt;
              &lt;div className="bg-white p-3 rounded shadow-sm"&gt;
                &lt;strong className="block text-xs text-slate-500 uppercase"&gt;Included in FID&lt;/strong&gt;
                &lt;span className="text-sm font-mono text-slate-900"&gt;Thresholds, Frontier logic, Rounding rules, Tie Policy&lt;/span&gt;
              &lt;/div&gt;
              &lt;div className="bg-white p-3 rounded shadow-sm"&gt;
                &lt;strong className="block text-xs text-slate-500 uppercase"&gt;Excluded from FID&lt;/strong&gt;
                &lt;span className="text-sm font-mono text-slate-900"&gt;Visual labels, Language settings, Report layout options&lt;/span&gt;
              &lt;/div&gt;
            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/section&gt;

      &lt;/div&gt;
    &lt;/main&gt;
  );
}
    </file>
    <file path="technology/voting-machine/page.tsx" size="9388" lines="178" kind="source">
// app/technology/voting-machine/page.tsx
import Link from 'next/link';
import { 
  Cpu, 
  FileJson, 
  ShieldCheck, 
  Lock, 
  ArrowRight, 
  GitCommit,
  Network
} from 'lucide-react';

export const metadata = {
  title: 'VM-ENGINE – Deterministic Electoral Core',
  description: 'A pure-function component for byte-identical electoral simulation across platforms.',
};

export default function VotingMachineIndex() {
  return (
    &lt;main className="min-h-screen bg-white py-16 px-6"&gt;
      &lt;div className="max-w-5xl mx-auto"&gt;
        
        {/* HERO */}
        &lt;div className="mb-20 border-b border-gray-200 pb-10"&gt;
          &lt;div className="flex items-center gap-3 text-primary font-mono text-sm font-bold uppercase tracking-wider mb-4"&gt;
            &lt;Cpu className="w-5 h-5" /&gt;
            &lt;span&gt;Core Component&lt;/span&gt;
          &lt;/div&gt;
          &lt;h1 className="text-5xl md:text-6xl font-bold text-slate-900 mb-6"&gt;
            VM-ENGINE
          &lt;/h1&gt;
          &lt;p className="text-xl md:text-2xl text-slate-600 max-w-3xl leading-relaxed mb-8"&gt;
            A deterministic electoral simulation core. It is not a "black box" in the opaque sense, 
            but a &lt;strong&gt;pure function&lt;/strong&gt;: it accepts canonical inputs and produces 
            byte-identical outputs across any operating system or architecture.
          &lt;/p&gt;
          &lt;div className="flex flex-wrap gap-4"&gt;
            &lt;Link 
              href="/technology/voting-machine/specifications"
              className="inline-flex items-center gap-2 bg-slate-900 text-white px-6 py-3 rounded-lg font-bold hover:bg-primary transition-colors"
            &gt;
              View Specifications &lt;ArrowRight className="w-4 h-4" /&gt;
            &lt;/Link&gt;
            &lt;Link 
              href="/technology/voting-machine/integration"
              className="inline-flex items-center gap-2 border border-slate-300 text-slate-700 px-6 py-3 rounded-lg font-bold hover:border-slate-900 transition-colors"
            &gt;
              Integration Guide &lt;ArrowRight className="w-4 h-4" /&gt;
            &lt;/Link&gt;
          &lt;/div&gt;
        &lt;/div&gt;

        {/* THE PURE FUNCTION DIAGRAM */}
        &lt;section className="mb-20"&gt;
          &lt;h2 className="text-3xl font-bold text-slate-900 mb-8"&gt;The "Pure Function" Contract&lt;/h2&gt;
          &lt;div className="bg-slate-50 border border-slate-200 rounded-2xl p-8 md:p-12"&gt;
            &lt;div className="flex flex-col md:flex-row items-center justify-between gap-8 text-center md:text-left"&gt;
              
              {/* INPUTS */}
              &lt;div className="flex-1 space-y-4"&gt;
                &lt;div className="bg-white p-4 rounded-xl shadow-sm border border-slate-200"&gt;
                  &lt;div className="flex items-center gap-2 font-mono text-sm text-blue-600 mb-2"&gt;
                    &lt;FileJson className="w-4 h-4" /&gt; registry.json
                  &lt;/div&gt;
                  &lt;p className="text-xs text-slate-500"&gt;Universe of units &amp; options&lt;/p&gt;
                &lt;/div&gt;
                &lt;div className="bg-white p-4 rounded-xl shadow-sm border border-slate-200"&gt;
                  &lt;div className="flex items-center gap-2 font-mono text-sm text-blue-600 mb-2"&gt;
                    &lt;FileJson className="w-4 h-4" /&gt; tally.json
                  &lt;/div&gt;
                  &lt;p className="text-xs text-slate-500"&gt;Votes per unit/option&lt;/p&gt;
                &lt;/div&gt;
                &lt;div className="bg-white p-4 rounded-xl shadow-sm border border-slate-200"&gt;
                  &lt;div className="flex items-center gap-2 font-mono text-sm text-blue-600 mb-2"&gt;
                    &lt;FileJson className="w-4 h-4" /&gt; params.json
                  &lt;/div&gt;
                  &lt;p className="text-xs text-slate-500"&gt;Algorithm config &amp; variables&lt;/p&gt;
                &lt;/div&gt;
              &lt;/div&gt;

              {/* ENGINE */}
              &lt;div className="flex-shrink-0 flex flex-col items-center"&gt;
                &lt;ArrowRight className="w-8 h-8 text-slate-300 mb-2 rotate-90 md:rotate-0" /&gt;
                &lt;div className="w-32 h-32 bg-slate-900 rounded-full flex flex-col items-center justify-center text-white shadow-xl ring-4 ring-slate-100"&gt;
                  &lt;Cpu className="w-10 h-10 mb-2" /&gt;
                  &lt;span className="font-bold text-sm"&gt;VM-ENGINE&lt;/span&gt;
                &lt;/div&gt;
                &lt;ArrowRight className="w-8 h-8 text-slate-300 mt-2 rotate-90 md:rotate-0" /&gt;
              &lt;/div&gt;

              {/* OUTPUTS */}
              &lt;div className="flex-1 space-y-4"&gt;
                &lt;div className="bg-white p-4 rounded-xl shadow-sm border border-slate-200 border-l-4 border-l-green-500"&gt;
                  &lt;div className="flex items-center gap-2 font-mono text-sm text-green-700 mb-2"&gt;
                    &lt;FileJson className="w-4 h-4" /&gt; result.json
                  &lt;/div&gt;
                  &lt;p className="text-xs text-slate-500"&gt;Canonical outcome &amp; labels&lt;/p&gt;
                &lt;/div&gt;
                &lt;div className="bg-white p-4 rounded-xl shadow-sm border border-slate-200 border-l-4 border-l-purple-500"&gt;
                  &lt;div className="flex items-center gap-2 font-mono text-sm text-purple-700 mb-2"&gt;
                    &lt;ShieldCheck className="w-4 h-4" /&gt; run_record.json
                  &lt;/div&gt;
                  &lt;p className="text-xs text-slate-500"&gt;Cryptographic audit trail&lt;/p&gt;
                &lt;/div&gt;
              &lt;/div&gt;

            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/section&gt;

        {/* KEY GUARANTEES */}
        &lt;section className="grid md:grid-cols-2 gap-8 mb-20"&gt;
          &lt;div className="p-6 rounded-xl border border-slate-200 hover:border-slate-400 transition-colors"&gt;
            &lt;Lock className="w-8 h-8 text-slate-900 mb-4" /&gt;
            &lt;h3 className="text-xl font-bold mb-3"&gt;Byte-Identical Determinism&lt;/h3&gt;
            &lt;p className="text-slate-600"&gt;
              With identical inputs and seeds, the engine produces outputs that are bit-for-bit identical 
              on any machine. We achieve this by enforcing a strictly canonical JSON format with 
              sorted keys and stable array ordering.
            &lt;/p&gt;
          &lt;/div&gt;

          &lt;div className="p-6 rounded-xl border border-slate-200 hover:border-slate-400 transition-colors"&gt;
            &lt;Network className="w-8 h-8 text-slate-900 mb-4" /&gt;
            &lt;h3 className="text-xl font-bold mb-3"&gt;Offline &amp; Hermetic&lt;/h3&gt;
            &lt;p className="text-slate-600"&gt;
              The engine performs &lt;strong&gt;no network I/O&lt;/strong&gt; during official runs. It is a self-contained 
              binary that verifies its own input hashes and fails hard upon any spec violation.
            &lt;/p&gt;
          &lt;/div&gt;

          &lt;div className="p-6 rounded-xl border border-slate-200 hover:border-slate-400 transition-colors"&gt;
            &lt;GitCommit className="w-8 h-8 text-slate-900 mb-4" /&gt;
            &lt;h3 className="text-xl font-bold mb-3"&gt;Formula ID (FID)&lt;/h3&gt;
            &lt;p className="text-slate-600"&gt;
              We separate "outcomes" from "presentation". Outcome-affecting rules are hashed into a 
              &lt;strong&gt;Formula ID&lt;/strong&gt;. Changing a visual label does not change the FID; changing a 
              rounding rule does.
            &lt;/p&gt;
          &lt;/div&gt;

          &lt;div className="p-6 rounded-xl border border-slate-200 hover:border-slate-400 transition-colors"&gt;
            &lt;Cpu className="w-8 h-8 text-slate-900 mb-4" /&gt;
            &lt;h3 className="text-xl font-bold mb-3"&gt;Pinned Randomness&lt;/h3&gt;
            &lt;p className="text-slate-600"&gt;
              Tie-breaking is not arbitrary. When &lt;code&gt;random&lt;/code&gt; policy is selected, the engine uses a 
              frozen RNG profile seeded once per run. A k-way tie consumes exactly &lt;em&gt;k&lt;/em&gt; draws.
            &lt;/p&gt;
          &lt;/div&gt;
        &lt;/section&gt;

        {/* DEEP DIVE LINKS */}
        &lt;div className="grid md:grid-cols-2 gap-6"&gt;
          &lt;Link href="/technology/voting-machine/specifications" className="group block bg-slate-900 text-white p-8 rounded-2xl hover:bg-slate-800 transition-all"&gt;
            &lt;h3 className="text-2xl font-bold mb-2 group-hover:underline decoration-white underline-offset-4"&gt;Technical Specifications&lt;/h3&gt;
            &lt;p className="text-slate-400 mb-6"&gt;
              The rigorous engineering documentation. Data models, Algorithm Flow, Gates, and the Pipeline State Machine.
            &lt;/p&gt;
            &lt;div className="flex items-center text-sm font-bold"&gt;
              Read Specs &lt;ArrowRight className="w-4 h-4 ml-2" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;

          &lt;Link href="/technology/voting-machine/integration" className="group block bg-slate-50 border border-slate-200 p-8 rounded-2xl hover:border-slate-400 transition-all"&gt;
            &lt;h3 className="text-2xl font-bold text-slate-900 mb-2 group-hover:underline decoration-slate-900 underline-offset-4"&gt;Integration &amp; Reporting&lt;/h3&gt;
            &lt;p className="text-slate-600 mb-6"&gt;
              How to embed VM-ENGINE in a larger system. CLI contracts, read-only reporting templates, and audit verification.
            &lt;/p&gt;
            &lt;div className="flex items-center text-sm font-bold text-slate-900"&gt;
              View Guide &lt;ArrowRight className="w-4 h-4 ml-2" /&gt;
            &lt;/div&gt;
          &lt;/Link&gt;
        &lt;/div&gt;

      &lt;/div&gt;
    &lt;/main&gt;
  );
}
    </file>
    <file path="technology/voting-machine/specifications/page.tsx" size="10708" lines="230" kind="source">
// app/technology/voting-machine/specifications/page.tsx
import Link from 'next/link';
import { 
  Database, 
  GitBranch, 
  ShieldAlert, 
  Terminal, 
  ArrowLeft,
  FileJson,
  Layers
} from 'lucide-react';

export const metadata = {
  title: 'VM-ENGINE Specifications – Data Models &amp; Algorithms',
  description: 'Normative specifications for the VM-ENGINE: Data models, algorithmic step order, gating logic, and the canonical pipeline.',
};

export default function VMSpecs() {
  return (
    &lt;main className="min-h-screen bg-white py-16 px-6"&gt;
      &lt;div className="max-w-5xl mx-auto"&gt;
        
        {/* HEADER */}
        &lt;div className="mb-16"&gt;
          &lt;Link 
            href="/technology/voting-machine" 
            className="text-slate-500 hover:text-blue-600 font-medium flex items-center gap-2 mb-6 transition-colors"
          &gt;
            &lt;ArrowLeft className="w-4 h-4" /&gt; Back to VM-ENGINE Overview
          &lt;/Link&gt;
          &lt;h1 className="text-4xl font-bold text-slate-900 mb-6"&gt;
            Technical Specifications
          &lt;/h1&gt;
          &lt;p className="text-xl text-slate-600 leading-relaxed max-w-3xl"&gt;
            The machine operates on a strict set of normative documents (Docs 1–7). 
            Below is the condensed engineering reference for the Data Model, Algorithm, and Pipeline.
          &lt;/p&gt;
        &lt;/div&gt;

        {/* 1. DATA MODEL (DOC 1) */}
        &lt;section className="mb-20"&gt;
          &lt;div className="flex items-center gap-3 mb-8 border-b border-slate-200 pb-4"&gt;
            &lt;Database className="w-8 h-8 text-blue-600" /&gt;
            &lt;h2 className="text-2xl font-bold text-slate-900"&gt;1. Canonical Data Model&lt;/h2&gt;
          &lt;/div&gt;
          
          &lt;div className="grid md:grid-cols-2 gap-8"&gt;
            &lt;div className="space-y-6"&gt;
              &lt;div className="bg-slate-50 p-6 rounded-xl border border-slate-200"&gt;
                &lt;h3 className="font-bold text-slate-900 mb-2 flex items-center gap-2"&gt;
                  &lt;FileJson className="w-4 h-4" /&gt; Inputs (Consumed)
                &lt;/h3&gt;
                &lt;ul className="space-y-3 text-sm text-slate-700"&gt;
                  &lt;li&gt;
                    &lt;strong&gt;DivisionRegistry:&lt;/strong&gt; The stable universe of &lt;code&gt;units&lt;/code&gt; and &lt;code&gt;options&lt;/code&gt;. Defines the deterministic &lt;code&gt;order_index&lt;/code&gt; for every option.
                  &lt;/li&gt;
                  &lt;li&gt;
                    &lt;strong&gt;BallotTally:&lt;/strong&gt; Votes per unit/option. Must referentially align with the Registry.
                  &lt;/li&gt;
                  &lt;li&gt;
                    &lt;strong&gt;ParameterSet:&lt;/strong&gt; The configuration map. Must explicitly set all &lt;strong&gt;Included&lt;/strong&gt; VM-VARs (outcome-affecting variables).
                  &lt;/li&gt;
                &lt;/ul&gt;
              &lt;/div&gt;
            &lt;/div&gt;

            &lt;div className="space-y-6"&gt;
              &lt;div className="bg-slate-50 p-6 rounded-xl border border-slate-200"&gt;
                &lt;h3 className="font-bold text-slate-900 mb-2 flex items-center gap-2"&gt;
                  &lt;FileJson className="w-4 h-4" /&gt; Outputs (Produced)
                &lt;/h3&gt;
                &lt;ul className="space-y-3 text-sm text-slate-700"&gt;
                  &lt;li&gt;
                    &lt;strong&gt;Result:&lt;/strong&gt; The canonical outcome. Contains allocations, aggregates, and the &lt;code&gt;formula_id&lt;/code&gt; (FID).
                  &lt;/li&gt;
                  &lt;li&gt;
                    &lt;strong&gt;RunRecord:&lt;/strong&gt; The cryptographic audit trail. Contains input hashes, engine version, effective variables, and the &lt;code&gt;TieLog&lt;/code&gt;.
                  &lt;/li&gt;
                  &lt;li&gt;
                    &lt;strong&gt;FrontierMap (Optional):&lt;/strong&gt; Per-unit diagnostics for the frontier gating model (if enabled).
                  &lt;/li&gt;
                &lt;/ul&gt;
              &lt;/div&gt;
            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/section&gt;

        {/* 2. ALGORITHM FLOW (DOC 4A) */}
        &lt;section className="mb-20"&gt;
          &lt;div className="flex items-center gap-3 mb-8 border-b border-slate-200 pb-4"&gt;
            &lt;GitBranch className="w-8 h-8 text-purple-600" /&gt;
            &lt;h2 className="text-2xl font-bold text-slate-900"&gt;2. Algorithmic Step Order&lt;/h2&gt;
          &lt;/div&gt;
          
          &lt;div className="bg-white border border-slate-200 rounded-xl overflow-hidden shadow-sm"&gt;
            &lt;div className="p-6 bg-slate-50 border-b border-slate-200"&gt;
              &lt;p className="text-slate-600 text-sm"&gt;
                The engine must execute stages in this exact order to guarantee determinism. 
                Ordering of units is always by ascending &lt;code&gt;unit_id&lt;/code&gt;.
              &lt;/p&gt;
            &lt;/div&gt;
            
            &lt;div className="divide-y divide-slate-100"&gt;
              &lt;StepRow 
                step="S0" 
                title="Normalize &amp; Seed" 
                desc="Canonicalize inputs. Bind algorithm family constants. Initialize RNG seed (no draws yet)." 
              /&gt;
              &lt;StepRow 
                step="S1" 
                title="Per-Unit Tallies" 
                desc="Load votes. Compute raw shares and base metrics required for gating." 
              /&gt;
              &lt;StepRow 
                step="S2" 
                title="Gates (Sanity/Eligibility)" 
                desc="Apply Doc 4B gates. If a unit fails, mark Invalid and skip allocation. Record reasons." 
              /&gt;
              &lt;StepRow 
                step="S3" 
                title="Frontier Hook" 
                desc="If enabled (VM-VAR-040), apply band/cut logic. Emit diagnostics to FrontierMap." 
              /&gt;
              &lt;StepRow 
                step="S4" 
                title="Core Allocation" 
                desc="Compute allocations using the Algorithm Family rules (001-007). Deterministic; no RNG." 
              /&gt;
              &lt;StepRow 
                step="S5" 
                title="Tie Resolution" 
                desc="If ties exist: Apply policy (050). If 'random', consume exactly k draws from RNG (052) and log event." 
              /&gt;
              &lt;StepRow 
                step="S6" 
                title="Emit Artifacts" 
                desc="Build canonical Result and RunRecord. Compute FID. Self-verify all hashes before exit." 
              /&gt;
            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/section&gt;

        {/* 3. GATES &amp; INVALIDATION (DOC 4B) */}
        &lt;section className="mb-20"&gt;
          &lt;div className="flex items-center gap-3 mb-8 border-b border-slate-200 pb-4"&gt;
            &lt;ShieldAlert className="w-8 h-8 text-orange-600" /&gt;
            &lt;h2 className="text-2xl font-bold text-slate-900"&gt;3. Gates &amp; Edge Cases&lt;/h2&gt;
          &lt;/div&gt;
          
          &lt;div className="grid md:grid-cols-2 gap-8"&gt;
            &lt;div className="prose text-slate-600"&gt;
              &lt;p&gt;
                Before allocation, every unit must pass a series of &lt;strong&gt;Gates&lt;/strong&gt;. 
                If any gate fails, the unit is marked &lt;code&gt;Invalid&lt;/code&gt;, receives no allocation, 
                and the reason is recorded in the &lt;code&gt;RunRecord&lt;/code&gt;.
              &lt;/p&gt;
              &lt;ul className="list-disc pl-5 space-y-2 mt-4"&gt;
                &lt;li&gt;&lt;strong&gt;Sanity Gates:&lt;/strong&gt; Data plausibility (e.g., votes &amp;le; ballots).&lt;/li&gt;
                &lt;li&gt;&lt;strong&gt;Eligibility Gates:&lt;/strong&gt; Minimum turnout or share thresholds.&lt;/li&gt;
                &lt;li&gt;&lt;strong&gt;Validity Gates:&lt;/strong&gt; Integrity floors (VM-VAR-031).&lt;/li&gt;
              &lt;/ul&gt;
            &lt;/div&gt;
            
            &lt;div className="bg-orange-50 p-6 rounded-xl border border-orange-100"&gt;
              &lt;h4 className="font-bold text-orange-900 mb-3"&gt;The "Invalid" State&lt;/h4&gt;
              &lt;p className="text-sm text-orange-800 mb-4"&gt;
                There is no "provisional" allocation. A unit is either Valid or Invalid.
              &lt;/p&gt;
              &lt;div className="bg-white p-4 rounded border border-orange-200 font-mono text-xs text-slate-700"&gt;
                {`{
  "unit_id": "U-001",
  "label": "Invalid",
  "allocations": [],
  "reasons": ["VM-VAR-020:min_turnout"]
}`}
              &lt;/div&gt;
            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/section&gt;

        {/* 4. PIPELINE &amp; EXIT CODES (DOC 5) */}
        &lt;section className="mb-20"&gt;
          &lt;div className="flex items-center gap-3 mb-8 border-b border-slate-200 pb-4"&gt;
            &lt;Terminal className="w-8 h-8 text-slate-700" /&gt;
            &lt;h2 className="text-2xl font-bold text-slate-900"&gt;4. Pipeline Contracts&lt;/h2&gt;
          &lt;/div&gt;

          &lt;div className="grid md:grid-cols-3 gap-6"&gt;
            &lt;div className="border border-slate-200 p-6 rounded-xl"&gt;
              &lt;strong className="block text-lg text-slate-900 mb-2"&gt;Exit Code 2&lt;/strong&gt;
              &lt;span className="text-xs font-bold uppercase tracking-wider text-red-600"&gt;Validation Error&lt;/span&gt;
              &lt;p className="text-sm text-slate-600 mt-2"&gt;
                Input schema violation, referential integrity failure, or ordering precondition unmet.
              &lt;/p&gt;
            &lt;/div&gt;
            &lt;div className="border border-slate-200 p-6 rounded-xl"&gt;
              &lt;strong className="block text-lg text-slate-900 mb-2"&gt;Exit Code 3&lt;/strong&gt;
              &lt;span className="text-xs font-bold uppercase tracking-wider text-red-600"&gt;Verification Failure&lt;/span&gt;
              &lt;p className="text-sm text-slate-600 mt-2"&gt;
                Post-run self-check failed. The computed hash of an artifact does not match its embedded ID.
              &lt;/p&gt;
            &lt;/div&gt;
            &lt;div className="border border-slate-200 p-6 rounded-xl"&gt;
              &lt;strong className="block text-lg text-slate-900 mb-2"&gt;Exit Code 5&lt;/strong&gt;
              &lt;span className="text-xs font-bold uppercase tracking-wider text-red-600"&gt;Spec Violation&lt;/span&gt;
              &lt;p className="text-sm text-slate-600 mt-2"&gt;
                Internal determinism breach (e.g., RNG used when policy is not 'random').
              &lt;/p&gt;
            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/section&gt;

      &lt;/div&gt;
    &lt;/main&gt;
  );
}

function StepRow({ step, title, desc }: { step: string, title: string, desc: string }) {
  return (
    &lt;div className="flex gap-4 p-6 hover:bg-slate-50 transition-colors"&gt;
      &lt;div className="flex-shrink-0 w-10 h-10 bg-slate-200 rounded-full flex items-center justify-center font-bold text-slate-600 text-sm"&gt;
        {step}
      &lt;/div&gt;
      &lt;div&gt;
        &lt;h4 className="font-bold text-slate-900"&gt;{title}&lt;/h4&gt;
        &lt;p className="text-sm text-slate-600 mt-1"&gt;{desc}&lt;/p&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  );
}
    </file>
  </files>
</volume>
