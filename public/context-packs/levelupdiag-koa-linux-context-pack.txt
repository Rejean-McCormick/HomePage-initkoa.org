# INITKOA CONTEXT PACK

repository: Rejean-McCormick/LevelUpDiag-Koa-Linux
source_commit: db00cf04c684dcb7eb802ad366bb7323cb694f84
source_mode: git
working_tree_markdown: clean
working_tree_selected: clean
selection_mode: markdown
wiki_source_commit: none
wiki_working_tree_markdown: none
policy_version: 2026-09-18.3
repo_files: 12
wiki_files: 0
source_files: 12
included_files: 12
excluded_files: 0
duplicate_files: 0
content_bytes: 40659
authority_counts: {"reference":12}
content_role_counts: {"knowledge":12}
generated_at: 2026-09-22T15:18:14-04:00
files: 12
content_sha256: d492cff039cd318e02f11dff480e7828a2cdd5244215f3369add075fee838420

================================================================================================
FILE INDEX
================================================================================================

01. [reference] [knowledge] CODE_SNAPSHOT_MANIFEST.md | bytes=2853 | sha256=936bd5b29334c6c720d46e2e612ae4f1bd5367ca365732d813fdee315efbe2a8
02. [reference] [knowledge] docs/ARCHITECTURE.md | bytes=3298 | sha256=112fea3900b83380d5d5e8552e61328380b1ec6306212971e9b9314e294d5415
03. [reference] [knowledge] docs/KOALI_SEQUENCE.md | bytes=1654 | sha256=6b2d8834c6edcb814db841f476cb82e9f834b30d723412da49ff463154e99dfa
04. [reference] [knowledge] docs/KOALI_SYSTEM_TESTS.md | bytes=1514 | sha256=447c3d4d1a586f10464722395928e49821f4d4fbd73fea9c5d68b9f52dbfb6a9
05. [reference] [knowledge] docs/MIGRATION_FROM_V1.md | bytes=939 | sha256=f322541d47fea81a45bb2b11fc1371d289f7dbc129adf130c6f7c44ad575365c
06. [reference] [knowledge] docs/README.md | bytes=659 | sha256=6d165d77710d285f103422ac9b3d2ae37b1274b360795c8e4cf6a09d6fc7ae3f
07. [reference] [knowledge] docs/RESULT_MODEL.md | bytes=1641 | sha256=9b4497138697abd4c398361e4f7a6f9f94bc975f585f5b465c58981c2d813de7
08. [reference] [knowledge] docs/SECURITY_MODEL.md | bytes=797 | sha256=b8cf498f59c451104f4d8bdc5dba9524477b95100313ce214bca861732f806a3
09. [reference] [knowledge] docs/STORE_TESTS.md | bytes=5151 | sha256=994e25f981796411c6d1c6f4e2cd1d9d5669464f06d50d2c28d615c249e0eddb
10. [reference] [knowledge] docs/UI_MODULARITY.md | bytes=2668 | sha256=0ef0d3068065a42f96b6383ec9182a0cb2ff211553eea07aa23642baaf04910b
11. [reference] [knowledge] README.md | bytes=15163 | sha256=a3e15147bfd92b073ef9268391a9de84477cdae32bbca56f5f0245fbd84a5d75
12. [reference] [knowledge] UPDATE_20260919.md | bytes=4322 | sha256=2b8fa977ee4ef1f93a8230184f115489ca289feec79c74e8df27f5e1c82add3a


================================================================================================
FILE: CODE_SNAPSHOT_MANIFEST.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: 936bd5b29334c6c720d46e2e612ae4f1bd5367ca365732d813fdee315efbe2a8
CONTENT_BYTES: 2853
================================================================================================

# Code snapshot

- generated_at: 2026-09-19T07:51:28.756911
- repository: LevelUpDiag-Koali
- archive_layout: repository-relative paths

## Snapshot files
- `.gitignore` (80 bytes)
- `.smartignore` (14 bytes)
- `docs/ARCHITECTURE.md` (3298 bytes)
- `docs/KOALI_SEQUENCE.md` (1654 bytes)
- `docs/KOALI_SYSTEM_TESTS.md` (1514 bytes)
- `docs/MIGRATION_FROM_V1.md` (939 bytes)
- `docs/README.md` (659 bytes)
- `docs/RESULT_MODEL.md` (1641 bytes)
- `docs/SECURITY_MODEL.md` (797 bytes)
- `docs/STORE_TESTS.md` (5151 bytes)
- `docs/UI_MODULARITY.md` (2668 bytes)
- `levels/__init__.py` (0 bytes)
- `levels/koali_support.py` (20629 bytes)
- `levels/koali_system_support.py` (26854 bytes)
- `levels/n00_diagnostic_integrity.py` (4587 bytes)
- `levels/n01_environment.py` (4531 bytes)
- `levels/n02_repository.py` (4058 bytes)
- `levels/n03_documentation.py` (264 bytes)
- `levels/n04_contracts.py` (268 bytes)
- `levels/n05_components.py` (275 bytes)
- `levels/n06_integrations.py` (391 bytes)
- `levels/n07_profiles.py` (538 bytes)
- `levels/n08_security.py` (340 bytes)
- `levels/n09_offline.py` (337 bytes)
- `levels/n10_system.py` (334 bytes)
- `levels/n11_delivery.py` (3418 bytes)
- `levels/n12_koali_system.py` (139 bytes)
- `levels/n13_store.py` (118 bytes)
- `levels/store_support.py` (12155 bytes)
- `levelupdiag.config.json` (4700 bytes)
- `levelupdiag.config.local.example.json` (1069 bytes)
- `levelupdiag.py` (310 bytes)
- `LEVELUPDIAG_CONSOLE.pyw` (16166 bytes)
- `levelupdiag_core/__init__.py` (134 bytes)
- `levelupdiag_core/cli.py` (5333 bytes)
- `levelupdiag_core/commands.py` (2025 bytes)
- `levelupdiag_core/config.py` (1788 bytes)
- `levelupdiag_core/execution_backend.py` (7532 bytes)
- `levelupdiag_core/manifest.py` (2130 bytes)
- `levelupdiag_core/modes.py` (1534 bytes)
- `levelupdiag_core/report.py` (2162 bytes)
- `levelupdiag_core/runner.py` (13669 bytes)
- `levelupdiag_core/util.py` (1479 bytes)
- `levelupdiag_core/vcs.py` (844 bytes)
- `levelupdiag_core/verdicts.py` (2401 bytes)
- `levelupdiag_core/worker.py` (1680 bytes)
- `levelupdiag_manifest.json` (14445 bytes)
- `pyproject.toml` (281 bytes)
- `README.md` (13484 bytes)
- `RUN_KOALI_SYSTEM_DIAG.cmd` (104 bytes)
- `RUN_LEVELUPDIAG.bat` (197 bytes)
- `RUN_LEVELUPDIAG.sh` (187 bytes)
- `schemas/campaign-summary.schema.json` (1097 bytes)
- `schemas/report.schema.json` (778 bytes)
- `scripts/run_level.py` (1365 bytes)
- `tests/__init__.py` (0 bytes)
- `tests/test_commands.py` (588 bytes)
- `tests/test_delivery.py` (558 bytes)
- `tests/test_execution_backend.py` (6017 bytes)
- `tests/test_koali_support.py` (1468 bytes)
- `tests/test_koali_system.py` (8396 bytes)
- `tests/test_manifest.py` (3119 bytes)
- `tests/test_modes.py` (1850 bytes)
- `tests/test_stabilization.py` (2153 bytes)
- `tests/test_store.py` (6668 bytes)
- `tests/test_ui_modularity.py` (4303 bytes)


================================================================================================
FILE: docs/ARCHITECTURE.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: 112fea3900b83380d5d5e8552e61328380b1ec6306212971e9b9314e294d5415
CONTENT_BYTES: 3298
================================================================================================

# Architecture — Debug-first policy

LevelUpDiag-Koali separates **observation**, **conformance**, and **release provenance**.

The tool code lives in this directory and generated evidence lives under `.levelupdiag/` in the target. Commands execute with `shell=false`, bounded/redacted output, explicit timeouts, and process isolation.

## DEBUG

DEBUG is the default execution policy. It does not snapshot or compare Git state globally. Dependency verdicts do not suppress downstream diagnostic levels. A local `BLOCKED`, `FAIL`, `CONFIG_ERROR`, or repository formality remains evidence for that level while independent diagnostics continue.

The default `debug` campaign excludes N02 Repository/Structure, N03 Documentation, and N11 Delivery.

## VALIDATION

VALIDATION includes formal conformance levels. Hard prerequisite states (`BLOCKED`, `ERROR`, `INFRA_ERROR`, `CONFIG_ERROR`) can block declared dependants. Git cleanliness is still not a global prerequisite.

## RELEASE

RELEASE enables tracked Git-state protection and strict dependency semantics. This is the only default mode where before/after Git tracked-state comparison is part of campaign integrity.

## Dependency topology

Repository conformance is no longer a functional prerequisite. N04 depends on N01 rather than N02; N05 depends on N01/N04; runtime gates depend on N01/N04/N05. This prevents repository formalities from entering a functional debug selection through transitive dependencies.


## Modular product UI boundary

LevelUpDiag is an observer of Koali product-UI modularity, not the owner of that architecture. N06 can consume an explicit local `ui_modularity` policy and test product availability, product-owned standalone markers, public integrated-contribution markers, and explicitly forbidden private cross-product UI dependency tokens.

The check is intentionally configuration-driven so LevelUpDiag does not hard-code product frameworks or manifest names. Optional product absence is a valid state and reports `PASS` for the removal observation; required product absence can report `BLOCKED`. The core `stabilization` campaign does not select N06, so UI-product lifecycle remains outside core stabilization.

## Desktop execution backend

The desktop console separates the Windows UI host from the Linux qualification runtime. `desktop_console.execution_backend = "auto"` means WSL on Windows and native execution elsewhere. WSL launch uses argument arrays (`wsl.exe --exec`), not a shell command string. If WSL is unavailable, AUTO fails explicitly rather than silently reinterpreting Linux toolchain absence as target failure.

The `audit-total` campaign selects N00-N10 under DEBUG semantics so every independent level can produce evidence in one pass. It is diagnostic evidence, not a conformance or delivery claim.


## Windows desktop / WSL tool PATH

The Windows desktop console executes Linux qualification through WSL. Because `wsl.exe --exec` may expose a narrower PATH than an interactive WSL terminal, the backend probes WSL `HOME` and `PATH` using the selected Python interpreter, prepends the configured `desktop_console.wsl_path_prepend` entries (default: `~/.local/bin`, `~/.cargo/bin`), and launches LevelUpDiag through `/usr/bin/env`. No login shell or shell command string is required.


================================================================================================
FILE: docs/KOALI_SEQUENCE.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: 6b2d8834c6edcb814db841f476cb82e9f834b30d723412da49ff463154e99dfa
CONTENT_BYTES: 1654
================================================================================================

# Koali sequence — LevelUpDiag 2.5

## Primary path: live Koali product system

Start Koali with its managed launcher, wait for the Shell to become available, then run:

```text
RUN_KOALI.cmd
→ LevelUpDiag koali-system
→ N00 Diagnostic Integrity
→ N12 Koali Product System
→ N13 Store and Third-Party Applications
```

N12 consumes the runtime evidence produced by Koali itself:

```text
.koali-dev/state/ecosystem-status.json
.koali-dev/state/surface-runtime.json
.koali-dev/state/active-state.json
        ↓
owner contracts + surface projection + module health
        ↓
/api/shell-state + independent loopback probes
```

The expected KS4.4 policy is:

- Konnaxion, Orgo and Médiathèque kOA are Shell-critical and must be ready.
- SemantiK Architect remains linked/projected but its current runtime outage is accepted and must not degrade the global Shell.
- Kristal, kOA Linux, Koali Control Panel and kOA Digital Ecosystem remain source-only discoveries.
- Koali must not expose repository paths, commands, PIDs or other orchestration-private fields through the public Shell state.
- Hosted applications keep their own internal navigation; Koali must not recreate a redundant product sidebar.

## Legacy path: kOA core stabilization

The historical campaigns remain available unchanged for the underlying kOA-Linux target:

```text
stabilization
stabilization-runtime
audit-total
validation
release
```

These continue to use N01-N11 and the WSL/native toolchain rules documented by the kOA-Linux qualification flow.

N13 checks koa-linux store sources by default; Linux session checks are opt-in. See [Store tests](STORE_TESTS.md).


================================================================================================
FILE: docs/KOALI_SYSTEM_TESTS.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: 447c3d4d1a586f10464722395928e49821f4d4fbd73fea9c5d68b9f52dbfb6a9
CONTENT_BYTES: 1514
================================================================================================

# N12 — Koali Product System

N12 is a read-only live-system diagnostic for the KS4.x Koali composition layer.

It verifies:

1. the Koali repository and managed launcher are present;
2. `ecosystem-status.json`, `surface-runtime.json`, and `active-state.json` exist and are fresh;
3. every expected application remains in its owner repository with a discoverable `koali.integration.json`;
4. Konnaxion, Orgo, and Médiathèque are ready;
5. SemantiK Architect may be unready while remaining linked and visible;
6. every product has a runtime registration and resolved local embed target;
7. owner application manifests expose routes but no redundant Koali sidebar;
8. presentation authority remains presentation-only and does not grant capabilities or perform direct domain writes;
9. the global Shell is `ready` and `online` when all Shell-critical products are ready;
10. `/api/shell-state` matches the canonical active projection and does not expose orchestration-private fields;
11. required runtime probes are independently rechecked over loopback only.

N12 refuses non-loopback HTTP targets. It does not turn `allow_network=false` into general network access.

## Changes in 2.6

Optional runtime outages now report WARN, not PASS. Disabled HTTP checks and
products without independently testable required HTTP probes report PARTIAL.
Loopback restrictions apply to redirects too, and environment HTTP proxies are
disabled for these probes. `koali-system` also runs N13; see [Store tests](STORE_TESTS.md).


================================================================================================
FILE: docs/MIGRATION_FROM_V1.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: f322541d47fea81a45bb2b11fc1371d289f7dbc129adf130c6f7c44ad575365c
CONTENT_BYTES: 939
================================================================================================

# Migration notes — v2.1 debug-first policy

Version 2.1 retains N00-N11 and the existing Koali validators but changes orchestration semantics.

Key changes:

1. `debug` is the default campaign and mode.
2. N02 Repository/Structure and N03 Documentation are removed from the primary debug campaign.
3. N04-N10 no longer depend transitively on N02.
4. DEBUG has no dependency-blocking verdict set: independent evidence continues to be collected.
5. DEBUG does not snapshot or compare tracked Git state.
6. VALIDATION keeps conformance checks and strict dependency semantics without global Git cleanliness enforcement.
7. RELEASE keeps strict tracked-state protection and delivery/finalization checks.
8. Campaign summaries separate diagnostic execution, testability, and conformance.

Historical campaign names remain available for compatibility, but new automation should use `debug`, `structure`, `validation`, or `release` explicitly.


================================================================================================
FILE: docs/README.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: 6d165d77710d285f103422ac9b3d2ae37b1274b360795c8e4cf6a09d6fc7ae3f
CONTENT_BYTES: 659
================================================================================================

# LevelUpDiag-Koali documentation

- `ARCHITECTURE.md` — DEBUG / VALIDATION / RELEASE separation and dependency topology.
- `KOALI_SEQUENCE.md` — live Koali N12 path plus legacy kOA campaigns.
- `RESULT_MODEL.md` — local verdicts and campaign aggregation.
- `SECURITY_MODEL.md` — bounded execution and mode-scoped Git protection.
- `MIGRATION_FROM_V1.md` — migration to the v2.1 debug-first model.
- `UI_MODULARITY.md` — optional N06 diagnostics for standalone/integrated product UI boundaries.

- `KOALI_SYSTEM_TESTS.md` — exact N12 live-system checks and KS4.4 readiness policy.

- [N13 — Store and third-party applications](STORE_TESTS.md)


================================================================================================
FILE: docs/RESULT_MODEL.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: 9b4497138697abd4c398361e4f7a6f9f94bc975f585f5b465c58981c2d813de7
CONTENT_BYTES: 1641
================================================================================================

# Result model

Level verdicts remain:

`PASS`, `WARN`, `FAIL`, `SKIP`, `BLOCKED`, `PARTIAL`, `ERROR`, `INFRA_ERROR`, `CONFIG_ERROR`.

Their meaning is local to a level. `BLOCKED` means that level could not establish its intended evidence because a prerequisite was unavailable. It does not mean every independent diagnostic must stop.

Campaign summaries add:

- `diagnostic_mode`: `debug`, `validation`, or `release`;
- `execution_verdict`: whether the diagnostic machinery completed without `ERROR`/`INFRA_ERROR`;
- `testability`: `COMPLETE` or `PARTIAL`;
- `conformance_verdict`: `NOT_EVALUATED` in DEBUG, otherwise the strict campaign verdict.

## DEBUG aggregation

- any `ERROR`/`INFRA_ERROR` -> campaign `ERROR`;
- any other non-PASS local verdict -> campaign `WARN`;
- otherwise -> `PASS`.

Thus target failures and missing prerequisites remain visible without turning routine development diagnosis into a global `BLOCKED` state.

## VALIDATION / RELEASE aggregation

Strict evidence semantics are preserved. `FAIL` remains failure, required unavailable evidence can block completion, and configuration/diagnostic errors are not accepted as success.


## Optional product removal evidence

An explicitly configured UI product with `required: false` can be absent and still produce a local `PASS` observation in the N06 UI-modularity diagnostic. This represents the intended independent-removal property, not a claim that the product was tested. A configured required product that is absent reports `BLOCKED`; missing configured markers on an available product report `FAIL`; invalid diagnostic configuration reports `CONFIG_ERROR`.


================================================================================================
FILE: docs/SECURITY_MODEL.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: b8cf498f59c451104f4d8bdc5dba9524477b95100313ce214bca861732f806a3
CONTENT_BYTES: 797
================================================================================================

# Security model

LevelUpDiag-Koali remains read-oriented and bounded:

- commands execute as argument arrays with `shell=false`;
- stdout/stderr capture is bounded and redacted;
- target mutation is disabled by default;
- network use is disabled unless explicitly enabled;
- referenced gate reports are accepted only from bounded paths inside the target repository;
- worker timeouts remain explicit.

Git tracked-state protection is now **mode-scoped**:

- DEBUG: disabled; Git provenance is not a prerequisite for diagnosis;
- VALIDATION: disabled globally; N02 can still observe repository state when selected;
- RELEASE: enabled and tracked-state mutation during diagnostics is an error.

This keeps release provenance strict without making Git formalities part of ordinary system debugging.


================================================================================================
FILE: docs/STORE_TESTS.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: 994e25f981796411c6d1c6f4e2cd1d9d5669464f06d50d2c28d615c249e0eddb
CONTENT_BYTES: 5151
================================================================================================

# N13 — Store et applications tierces (2.6)

N13 cible **koa-linux**, où se trouve `host/store/`. Ce n'est pas un dépôt de
store supplémentaire. Il accompagne N12 dans `koali-system` et
`koali-system-debug`, avec une campagne `store` indépendante et un bouton STORE.

## Trois preuves différentes

| Couverture | Exécution | Ce que cela prouve |
|---|---|---|
| Sources | Windows ou Linux | Catalogue exact, syntaxe Python, raccourcis, politiques, paquets requis et projection dans l'image |
| Session Linux | Dans la session graphique cible | Outils présents, greffon Flatpak détecté, bus/portail, raccourcis installés correspondant au script source, dépôt et inventaire Flatpak |
| Fonctionnement réel | Essai explicite dans une VM jetable | Ouverture des fenêtres, installation, utilisation et reprise après panne |

Les tests de sources n'importent ni n'exécutent le code du dépôt cible.
La présence du fichier de tests est un contrôle distinct de leur exécution.
N13 ne télécharge rien, ne configure pas de dépôt et n'installe aucun logiciel.
Il ne certifie ni les signatures du dépôt, ni le fonctionnement du greffon à
partir de sa seule présence, ni le fonctionnement des applications installées.

## Lancer depuis le dépôt LevelUpDiag-Koali

```sh
python levelupdiag.py run store
```

Le chemin par défaut est `../koa-linux`. Pour le modifier, fusionner dans
`levelupdiag.config.local.json` (sans remplacer ses autres paramètres) :

```json
{
  "store": {
    "root": "../koa-linux",
    "runtime_checks": false
  }
}
```

Sous Linux, mettre `runtime_checks` à `true` et lancer le diagnostic **dans la
session utilisateur graphique de la VM**, sans sudo. Le bouton Windows reste
sur Python natif ; il ne valide pas implicitement une VM ni une session WSL.
Si des contrôles Linux sont demandés sous Windows, N13 rapporte `BLOCKED`.

Le module autonome permet de tester cette mise à jour même sans le dépôt complet
de LevelUpDiag (se placer dans `overlays/LevelUpDiag-Koali` pour cette archive) :

```sh
python -m levels.store_support --root /chemin/koa-linux
python -m levels.store_support --root /chemin/koa-linux --runtime
```

Il imprime un rapport JSON. Son code de sortie vaut 1 en présence de FAIL,
BLOCKED ou erreur de configuration ; 0 indique seulement l'absence de ces erreurs,
pas une qualification de bout en bout. Les points non exécutés restent PARTIAL.
Le moteur historique agrège les verdicts selon le mode de campagne.

## Tests du code de l'installateur

Depuis le dépôt **koa-linux** corrigé, exécuter explicitement :

```sh
python tests/system/test_third_party_store.py
```

Cette suite utilise des simulations et couvre annulation, sélection fermée,
dépôt inattendu, applications déjà présentes, échecs partiels et portée utilisateur.
Elle ne prouve pas qu'une installation réelle aboutit. N13 ne l'exécute pas
implicitement pendant une inspection en lecture seule.

## Scénario d'acceptation dans une VM jetable

Préparer un instantané et un compte utilisateur graphique ; installer les prérequis
système via l'adaptateur de distribution. Utiliser un dépôt koa-linux contenant
la livraison du store. Consigner la version de l'image, le commit, la date,
l'architecture et les versions des logiciels. Ne pas utiliser le compte principal.

1. Exécuter N13 avec `--runtime`, conserver le JSON. Un dépôt absent avant le
   premier lancement est un WARN attendu ; une URL différente est un FAIL.
2. Ouvrir « Installer les essentiels », annuler, puis comparer `flatpak remotes
   --user` et `flatpak list --user --app` à l'état initial : aucun ajout.
3. Ouvrir « Store — applications tierces », accepter la configuration. Vérifier
   la fenêtre GNOME Logiciels, le catalogue Flathub et la recherche de Firefox.
4. Sélectionner une seule application manquante (par exemple PeaZip), installer,
   vérifier son apparition dans `flatpak list --user --app`, son ouverture et
   l'accès à un fichier via le portail. Les autres applications restent absentes.
5. Relancer la même sélection : application déjà présente, aucune réinstallation.
6. Installer le reste de la présélection ; vérifier une page web Firefox, un média
   VLC avec audio, le démarrage de Thunderbird, une archive PeaZip, un fichier
   Notepad Next, un document LibreOffice et une base de test KeePassXC.
   Ne pas ajouter de vrais comptes ni de mots de passe personnels.
7. Sur un second instantané, couper le réseau pendant un téléchargement : échec
   visible, journal utile, pas de faux succès. Rétablir le réseau et relancer :
   les applications installées sont ignorées, les manquantes sont réessayées.
8. Lancer deux raccourcis d'installation simultanément : une seule opération.
9. Relancer N13. Conserver le rapport, le journal et les résultats par application.
   Restaurer l'instantané pour nettoyer l'essai.

Cette version n'ingère pas de déclaration manuelle de réussite pour transformer
le verdict en PASS : `end_to_end_validated` reste false. Le procès-verbal de VM
constitue une preuve séparée. Cela évite un faux PASS global.


================================================================================================
FILE: docs/UI_MODULARITY.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: 0ef0d3068065a42f96b6383ec9182a0cb2ff211553eea07aa23642baaf04910b
CONTENT_BYTES: 2668
================================================================================================

# Modular product UI diagnostics

LevelUpDiag-Koali treats modular product UI behavior as an **integration diagnostic**, not as a definition of Koali user-interface architecture.

## Authority boundary

Koali canonical documentation and each product's admitted public contract define what counts as its standalone entry point, integrated contribution, commands, routes, surfaces, inspector declarations, or other UI assets. LevelUpDiag only evaluates explicit diagnostic configuration.

It MUST NOT:

- make kOA Spaces a mandatory runtime for a product UI;
- make Konnaxion, Orgo, Kristal, or another product a mandatory UI dependency of another product;
- invent a universal product-manifest filename;
- copy product business semantics into diagnostic code;
- treat an intentionally removed optional product as a Koali core failure.

## Configuration

The optional `ui_modularity` object is read from the normal LevelUpDiag configuration merge.

```json
{
  "ui_modularity": {
    "enabled": true,
    "base_root": "..",
    "products": [
      {
        "id": "product-id",
        "root": "product-repository",
        "required": false,
        "standalone_markers": ["path/declared/by/product"],
        "integration_markers": ["path/declared/by/integration-contract"],
        "scan_files": ["package-or-source-file"],
        "forbidden_private_ui_tokens": ["private-import-token"]
      }
    ]
  }
}
```

`base_root` is resolved relative to the LevelUpDiag tool root. Product roots can be absolute or relative to that base. Marker and scan paths are product-relative and cannot escape the configured product repository.

## Verdicts

- optional product absent: `PASS` — removal is accepted;
- required product absent: `BLOCKED`;
- available product with configured markers present: `PASS`;
- configured marker missing: `FAIL`;
- configured forbidden private UI token found: `FAIL`;
- invalid paths or malformed configuration: `CONFIG_ERROR`;
- unreadable configured scan input: `INFRA_ERROR`.

A `PASS` for an absent optional product means only that the removal state is permitted. It is not positive functional evidence for that product.

## Campaign placement

The check runs from N06 Integrations. The default `stabilization` campaign remains N00/N01/N04/N05 and therefore does not depend on UI products. Broader `debug`, `validation`, and `release` selections can include N06 according to their existing policies.

## Security and boundedness

The check is read-only. It reads only explicit marker paths and explicit scan files. Each scan file is bounded to 512 KiB of text and only reports configured token/path metadata, not arbitrary source content.


================================================================================================
FILE: README.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: a3e15147bfd92b073ef9268391a9de84477cdae32bbca56f5f0245fbd84a5d75
CONTENT_BYTES: 15163
================================================================================================

# LevelUpDiag-Koali v2.5

LevelUpDiag-Koali 2.5 is the diagnostic suite for the **live Koali product system**, while preserving the historical kOA-Linux stabilization and release campaigns.

Its primary default is now `koali-system`: it observes the Koali shell that is actually running on the Windows host, reads the canonical `.koali-dev/state` projection, validates linked owner contracts without copying application repositories, independently probes required runtimes, and verifies that optional modules do not incorrectly degrade the global Shell.

Current KS4.4 product policy:

```text
Konnaxion          shell-critical / must be ready
Orgo               shell-critical / must be ready
Médiathèque kOA    shell-critical / must be ready
SemantiK Architect linked + projected / runtime outage currently accepted
Kristal / kOA Linux / Control Panel / Digital Ecosystem   source-only
```

Run it after starting Koali with `RUN_KOALI.cmd`:

```powershell
python levelupdiag.py run koali-system
# or
RUN_KOALI_SYSTEM_DIAG.cmd
```

The desktop console automatically uses **native Windows Python** for `koali-system`, because it must inspect the Windows-hosted Koali process/state and loopback endpoints. Legacy kOA-Linux campaigns keep the existing WSL-aware backend policy.


## Legacy kOA core stabilization scope

The legacy core campaign remains `stabilization`:

```text
N00 Diagnostic Integrity
N01 Environment
N04 Contracts
N05 Components
```

This scope deliberately defers independent subsystems while the Koali host/build/runtime environment is being stabilized:

```text
Konnaxion          placeholder_until_integration
Ariane             deferred_until_koali_integration_test
Orgo               draft_not_admitted
Semantik Architect deferred_not_admitted
```

This does **not** weaken or rewrite the canonical `sovereign-linux-node` profile. Full-target VALIDATION and RELEASE remain strict and can still report those subsystem authorities as unresolved. The `stabilization-runtime` campaign adds N08/N09/N10 when a bootable image/runtime context becomes available, while retaining DEBUG non-blocking campaign semantics.

## Repository layout

The specialization remains a sibling of the kOA-Linux repository:

```text
C:\mycode\kOA-Linux\
├── koa-linux\
└── LevelUpDiag-Koali\
```

The default target is `../koa-linux`.

Local developer utilities such as `GitSink.bat` are not a DEBUG gate. If an underlying kOA conformance validator reports a local utility or generated build residue, that individual level may report `FAIL/WARN`, but DEBUG continues collecting independent functional/runtime evidence.

## Modes

### DEBUG — default

Use while developing, loading, testing, or diagnosing kOA-Linux.

- no tracked-Git-state protection;
- no dependency verdict blocks another level;
- repository/documentation/release formalities are excluded from the default campaign;
- `FAIL`, `BLOCKED`, `CONFIG_ERROR`, `SKIP`, and `PARTIAL` remain visible at their local level but make the campaign `WARN`, not globally `BLOCKED`;
- `ERROR`/`INFRA_ERROR` still mean the diagnostic execution itself had a problem.

### VALIDATION

Use to check repository/documentation/conformance in addition to functional gates.
Dependencies can be blocked by `BLOCKED`, `ERROR`, `INFRA_ERROR`, or `CONFIG_ERROR`, but a clean Git worktree is not imposed globally.

### RELEASE

Use for strict finalization. Tracked Git state is snapshotted before and after the campaign; mutation by diagnostics is an error.

## Normal execution

```bash
python levelupdiag.py doctor
python levelupdiag.py list
python levelupdiag.py run stabilization
```

Explicit modes are supported:

```bash
python levelupdiag.py run N08,N09,N10 --mode debug
python levelupdiag.py run validation --mode validation
python levelupdiag.py run release --mode release
```

Windows launcher default:

```bat
RUN_LEVELUPDIAG.bat
```

Linux/macOS launcher default:

```bash
./RUN_LEVELUPDIAG.sh
```

Both now default to the `koali-system` campaign.

## Console

`LEVELUPDIAG_CONSOLE.pyw` exposes three policies and an execution backend. With `desktop_console.execution_backend = "auto"`, Windows uses WSL automatically; Linux/macOS use the native interpreter. The Windows UI does not silently fall back to native Python if WSL is unavailable.

- **DEBUG — non bloquant**
- **VALIDATION — conformité**
- **RELEASE — strict**

Presets:

- **KOALI SYSTEM**: N00 + N12, strict live product-system validation.
- **KOALI DEBUG**: N00 + N12, same evidence with non-blocking DEBUG aggregation.
- **STABILISATION kOA**: N00, N01, N04, N05.
- **AUDIT TOTAL**: legacy N00-N10 in DEBUG non-blocking mode.
- **VALIDATION kOA**: legacy N00-N10.
- **RELEASE**: legacy N00-N11.

N02 Repository and N03 Documentation are intentionally absent from DEBUG PRINCIPAL.

## Level sequence

```text
N00 Diagnostic Integrity
N01 Environment
N02 Repository / Structure
N03 Documentation
N04 Contracts
N05 Components
N06 Integrations
N07 Profiles
N08 Security Runtime
N09 Offline Runtime
N10 System Runtime
N11 Delivery
N12 Koali Product System
```

All levels remain serial for stable evidence ordering.

Functional levels no longer depend transitively on N02. Selecting `N08,N09,N10` adds functional prerequisites N00, N01, N04 and N05, but not repository/documentation formalities.

## Campaigns

- `koali-system` — default live Koali qualification, N00 + N12 in VALIDATION mode.
- `koali-system-debug` — non-blocking live Koali diagnosis, N00 + N12 in DEBUG mode.
- `stabilization` — primary pre-subsystem Koali core stabilization, mode DEBUG.
- `stabilization-runtime` — core runtime qualification when image/QEMU context exists, mode DEBUG.
- `debug` — broader development diagnosis including integration/profile/runtime levels, mode DEBUG.
- `audit-total` — N00-N10 in one non-blocking DEBUG pass, scope `full_target_diagnostic`.
- `structure` — repository/documentation checks, mode VALIDATION.
- `validation` — N00-N10, mode VALIDATION.
- `release` — N00-N11, mode RELEASE.

Compatibility aliases remain: `preflight`, `core`, `standard`, `delivery`, `full`.

## Modular product UI diagnostics

LevelUpDiag does **not** define the Koali UX, shell implementation, product manifest filename, or product framework. Those remain owned by Koali documentation and the individual products.

N06 can optionally diagnose consequences of the Koali modular-UI contract when `ui_modularity.enabled` is set in local configuration. The diagnostic is read-only and configuration-driven:

- an optional product repository may be absent and that absence is accepted as an independent-removal state;
- a required product repository that is unavailable reports `BLOCKED`;
- configured standalone markers can verify that a product retains its own runnable UI entry point or equivalent product-owned surface;
- configured integration markers can verify that the product exposes its declared integrated contribution;
- configured dependency scan files can reject explicit private cross-product UI dependency tokens;
- LevelUpDiag does not invent or hard-code Orgo, Konnaxion, Kristal, Spaces, or other product UI file names.

Example local policy:

```json
{
  "ui_modularity": {
    "enabled": true,
    "base_root": "..",
    "products": [
      {
        "id": "example-product",
        "root": "example-product",
        "required": false,
        "standalone_markers": ["<product-owned standalone marker>"],
        "integration_markers": ["<public Koali integration marker>"],
        "scan_files": ["<dependency manifest or source file>"],
        "forbidden_private_ui_tokens": ["<private cross-product UI import token>"]
      }
    ]
  }
}
```

The example deliberately uses placeholders: concrete marker paths belong to each product's admitted contract, not to LevelUpDiag. This check is outside the `stabilization` campaign and therefore cannot turn a missing optional UI product into a Koali core-stabilization prerequisite.

## Result model

A campaign summary now separates:

```text
verdict
execution_verdict
system testability
conformance_verdict
```

In DEBUG, conformance is reported as `NOT_EVALUATED` by the campaign summary. Local level findings are still preserved exactly.

Example:

```text
LevelUpDiag debug [DEBUG] - WARN
Diagnostic execution: PASS
System testability: PARTIAL
Conformance: NOT_EVALUATED
```

That means the diagnostic completed and found unavailable/failed local checks; it does **not** mean the whole debug session is blocked.

## Git policy

DEBUG does not inspect Git globally before/after a run. N02 performs Git observations only when N02 is explicitly selected.
A dirty worktree in N02 is advisory in DEBUG.

RELEASE retains tracked-state mutation protection because release/finalization provenance is a separate concern.

## kOA-specific validation commands

Default commands are still the canonical public kOA-Linux vectors:

```text
Documentation  uv run --frozen python docs/tools/validate_docs.py
Contracts      uv run --frozen python ci/scripts/run-contracts.py
Components     uv run --frozen python ci/scripts/run-components.py
Profiles       uv run --frozen pytest -q tests/profiles
Security       uv run --frozen python ci/scripts/run-security.py
Offline        uv run --frozen python ci/scripts/run-offline.py
System         uv run --frozen python ci/scripts/run-system-tests.py
```

Koali gate markers such as `security: blocked`, `offline: blocked`, and `system: blocked` remain local level verdicts. In DEBUG they do not prevent independent levels from running.

## Evidence

Evidence remains under the target repository:

```text
.levelupdiag/
├── runs/<run-id>/
│   ├── effective_config.json
│   ├── summary.json
│   ├── summary.txt
│   └── levels/Nxx/result.json
└── latest/
```

Commands use `shell=false`; output is bounded and redacted; target mutation remains disabled by configuration.

## Version 2.5.0

2.5.0 adds N12 **Koali Product System**. It validates the KS4.x multi-repository composition layer from its canonical runtime evidence instead of treating integrated applications as copied dependencies. N12 checks live state freshness, owner-contract discovery, runtime readiness, surface registrations, active Space projection, Shell-critical versus optional health semantics, public `/api/shell-state` privacy, and independent loopback probes. SemantiK Architect may remain unavailable without failing Koali while its catalog entry has `affectsShellState=false`.

The `koali-system` desktop campaign is forced to the native Windows backend; legacy kOA-Linux campaigns retain WSL-aware execution.

## Version 2.4.1

2.4.1 fixes the WSL environment inherited by the Windows desktop console. A direct `wsl.exe --exec` launch can expose a narrower `PATH` than an interactive WSL session, which made user-installed `uv`, Cargo, Rust and rustup disappear even though the run was correctly executing under WSL.

The WSL backend now probes `HOME` and `PATH` with the selected WSL Python interpreter, prepends the configurable `desktop_console.wsl_path_prepend` entries (default: `~/.local/bin` and `~/.cargo/bin`), and launches LevelUpDiag through `/usr/bin/env`. The Windows subprocess still uses `shell=false`; no login shell or profile-script dependency is introduced.

## Version 2.4.0

2.4.0 made desktop execution environment-aware. On Windows, the console `auto` backend launches LevelUpDiag through `wsl.exe --exec` and resolves the script path with `wslpath`, preventing Windows Python from producing misleading Linux-toolchain failures. It also added **AUDIT TOTAL**, a named N00-N10 DEBUG campaign for complete one-pass evidence collection, and aligned `levelupdiag_manifest.json` suite version with the package/UI version.

## Version 2.3.0

2.3.0 keeps the 2.2 core-stabilization policy and adds opt-in N06 diagnostics for modular product user interfaces. The new check validates only explicitly configured product roots, standalone/integration markers, and private-dependency tokens; it does not define the Koali UI architecture itself. Optional product absence is accepted as a valid independent-removal state.

It also fixes the desktop console's **DEBUG COMPLET** preset so it selects the `debug` campaign rather than `stabilization`.

## Version 2.2.0

2.2.0 changed the default workflow from broad debug to explicit **Koali core stabilization before subsystem integration**. It also requires the core Rust/native toolchain in N01 (`git`, `cargo`, `rustc`, `cc`), adds qualification-scope metadata to campaign summaries, and provides compatibility aliases plus `scripts/run_level.py` for older Koali Control Panel integrations.

## 2.6 — Store coverage and honest partial results

In version 2.6, `koali-system` ran N00 + N12 + N13. Version 2.7 separates the Store again. The **STORE** button
and `python levelupdiag.py run store` run N00 + N13 independently. N13 targets
`../koa-linux`, not the Spaces repository. Linux session probes are opt-in with
`store.runtime_checks=true`; Windows source checks do not certify the Linux store.
No packages are installed by this diagnostic. See [N13 setup and VM acceptance](docs/STORE_TESTS.md).

N12 reports accepted optional outages as WARN, disabled/missing live checks as
PARTIAL, and refuses redirects away from loopback. HTTP proxy environment variables
are ignored for local probes. Version 2.7 restores the Windows-only N00 + N12 campaign.


## Version 2.7 — Independent diagnostic scopes

The console now provides **AUDIT SANS VM** (`audit-local`) and **AUDIT QEMU**
(`audit-qemu`). Select the preset, then **LANCER SÉLECTION**. **AUDIT TOTAL**
keeps both scopes and documentation: missing VM prerequisites remain visible,
while available local tests still execute. **KOALI SYSTEM** remains native Windows,
N00 + N12. **STORE** remains an independent N00 + N13 campaign; its PARTIAL result
honestly means no end-to-end installation has been validated.

WSL probes and launches explicitly select `desktop_console.wsl_distro`
(default `Ubuntu-24.04`) and pass `--target` from `wsl_target_root`
(default `~/work/koa-linux`). On Rejean's configured distro this expands to
`/home/rejean/work/koa-linux`. The execution banner shows the distro and target.
The log button follows the report emitted by the completed run, including WSL paths.

Install this update together with the kOA-Linux gate-scope update. Use the Control
Panel's development preparation to refresh the imported Linux workspace after
copying the updated sources. Preserve existing `levelupdiag.config.local.json`;
check that it does not override `commands.offline` or `commands.system` with empty
arrays, or override the WSL distro/workspace with stale values.

`audit-local` covers configured source tests, including Store source tests in N10.
It does not validate a running Linux desktop. `audit-qemu` retains its prerequisite
levels and selects VM tests in N08–N10. RELEASE always selects the full runtime
scope. Scope and omitted commands are recorded in kOA gate reports.

See `UPDATE_20260919.md` for verification results and remaining image prerequisites.


================================================================================================
FILE: UPDATE_20260919.md
AUTHORITY: reference
CONTENT_ROLE: knowledge
CONTENT_SHA256: 2b8fa977ee4ef1f93a8230184f115489ca289feec79c74e8df27f5e1c82add3a
CONTENT_BYTES: 4322
================================================================================================

# Mise à jour du 19 septembre 2026

## Installation

Copier les fichiers de chaque archive par-dessus son dépôt existant : koa-linux
et LevelUpDiag-Koali. Ne pas supprimer le dépôt ou les fichiers absents du snapshot.
Conserver les réglages locaux et les ressources non exportées, notamment les SVG.
Fermer puis rouvrir la console LevelUpDiag après copie. Dans le Control Panel,
relancer la préparation du développement pour réimporter le checkout kOA modifié
vers Ubuntu-24.04, /home/rejean/work/koa-linux.

Dans LevelUpDiag : KOALI SYSTEM puis LANCER SÉLECTION pour Windows natif ;
AUDIT SANS VM puis LANCER SÉLECTION pour les sources Linux ; AUDIT TOTAL pour
collecter aussi la documentation et les blocages VM. AUDIT QEMU cible les essais
VM après les niveaux prérequis. STORE reste la campagne indépendante du Store.
La ligne Backend doit afficher Ubuntu-24.04 et /home/rejean/work/koa-linux pour
les audits Linux. Adapter desktop_console.wsl_distro et wsl_target_root dans
levelupdiag.config.local.json si nécessaire. Un ancien override commands.offline
ou commands.system égal à [] désactive encore ces essais : retirer cet override
pour utiliser les commandes du fichier de base.

## Changements

- kOA : séparation --scope all/local/qemu dans les gates sécurité, hors-ligne,
  système ; les groupes pytest locaux sont exécutés même sans image VM.
- Les échecs restent dans le verdict ; ils n'interrompent plus les commandes suivantes.
- Rapports avec scope, full_gate et omitted_commands. Une VM non exécutée est
  BLOCKED et n'est jamais comptée comme un test réussi. Le mode par défaut reste all.
- N10 couvre aussi les sources du Store, le workspace natif, les paquets,
  la matérialisation rootfs et la construction d'image.
- docs/CODE_SNAPSHOT_MANIFEST.md est un inventaire d'export, exclu exactement
  du corpus normatif. Les autres documents restent contrôlés. Index régénérés.
- LevelUpDiag 2.7 : campagnes audit-local/audit-qemu, sélection conservée dans
  l'interface, distribution WSL explicite pour chaque sonde, cible Linux explicite,
  timeout des sondes, bouton des logs suivant le rapport de l'exécution.
- Koali System N00/N12 reste sous Windows ; N13 reste dans STORE avec une
  couverture PARTIAL tant que l'installation réelle n'est pas vérifiée.
- RELEASE force la portée runtime all ; aucun prérequis produit n'est inventé.

## Vérification effectuée ici

- LevelUpDiag : 57 tests unittest réussis, dont routage WSL simulé et sélection UI.
- kOA scripts : 3 tests de régression réussis (partition sans perte, poursuite
  après échec documentaire et VM absente, exclusion exacte du manifeste).
- Suite kOA étendue : 1 075 réussis, 1 échec existant du snapshot :
  integrations/koa-spaces/tests/test_boundary.py::test_only_declared_bundle_files_exist.
  Les neuf SVG attendus dans integrations/koa-spaces/interface/icons/ sont absents
  de l'archive d'entrée. Aucun assouplissement de ce contrôle ni icône inventée.
- Scripts réels : sécurité all = 5 contrôles statiques + 34 tests locaux réussis,
  puis BLOCKED pour QEMU absent ; système local = 64 tests réussis ; hors-ligne
  local = réussi. Les rapports conservent explicitement la couverture manquante.
- Environnement de vérification : Linux, Python 3.12.14, pytest 9.1.1 et
  jsonschema 4.26.0 installés isolément. Ce n'est pas le frozen environnement
  Python 3.13/pytest 9.0.3 du poste Windows/WSL. Aucune VM ni installation
  Flatpak interactive n'a été exécutée ici.

## Image QEMU : prérequis réels encore absents

packaging/system/image.toml référence generated/assembly/B-0092/bundle.json,
qui n'est pas fourni, et des entrypoints générés. Les projections
resolved-plan/effective-profile et l'image système générée sont également absentes.
Le pipeline build.image existant demande un rootfs, un noyau, un initramfs,
du matériel de boot, un rootfs de récupération et leurs identités/provenances.
Ses tests de construction passent, mais les données binaires et les bundles admis
ne peuvent pas être déduits d'un snapshot de code. activation_ready reste false.
Les regex de session, de confinement et de navigation doivent provenir du runtime
réel de l'image. QEMU restera NOT READY tant que ces prérequis ne sont pas produits.
