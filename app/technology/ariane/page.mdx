import Link from 'next/link';
import { Card } from '@/components/Card'; // Assuming you have the Card component we fixed earlier

export const metadata = {
  title: "Ariane",
  description: "Semantic infrastructure for treating user interfaces as data.",
};

export default function ArianePage() {
  return (
    <main className="min-h-screen bg-white text-slate-900 selection:bg-primary selection:text-white pb-24">
      
      {/* HERO SECTION */}
      <section className="max-w-4xl mx-auto px-6 pt-24 pb-12 border-b border-gray-100">
        <h1 className="text-5xl font-serif font-medium mb-6 text-slate-900">
          Ariane
        </h1>
        <p className="text-2xl font-light text-slate-600 mb-8 leading-relaxed">
          Semantic infrastructure for treating user interfaces as data.
        </p>
        
        <div className="prose prose-slate prose-lg text-slate-600 leading-relaxed">
          <p>
            Ariane defines a universal graph model of software UIs—screens, controls, and the actions that connect them—so that external systems (such as AI agents or automation tools) can query this graph and use it as a reference when guiding users through software.
          </p>
          <p>
            Ariane itself is not a help overlay or assistant. It is the underlying map.
          </p>
        </div>

        {/* BLUE ALERT BOX */}
        <div className="mt-8 bg-blue-50 border-l-4 border-blue-500 p-6 rounded-r-sm">
          <p className="font-bold text-blue-700 mb-1 uppercase tracking-wide text-xs">Core Idea</p>
          <p className="text-blue-800 font-medium text-lg">
            “UI as data” — represent any interface as a graph of states and transitions, independent of platform, styling, or branding.
          </p>
        </div>
      </section>

      <div className="max-w-4xl mx-auto px-6 py-16 space-y-20">

        {/* OBJECTIVES */}
        <section>
          <h2 className="text-3xl font-serif font-medium mb-6 text-slate-900">Objectives</h2>
          <ul className="grid gap-3 list-disc pl-5 text-slate-700 leading-relaxed">
            <li>Model software user interfaces as a machine-readable graph.</li>
            <li>Make procedural knowledge (“how to do X in this tool”) accessible as data, not buried in tutorials.</li>
            <li>Provide a stable reference layer for agents and tools that need to act inside existing software.</li>
            <li>Support both <strong>automated</strong> and <strong>human-guided</strong> mapping of real applications.</li>
            <li>Remain platform-agnostic: web, desktop, mobile, and other environments.</li>
          </ul>
        </section>

        {/* COMPONENTS */}
        <section>
          <h2 className="text-3xl font-serif font-medium mb-10 text-slate-900">Components</h2>
          <div className="grid md:grid-cols-2 gap-8">
            
            {/* Theseus */}
            <Card title="Theseus (Exploration Engine)">
              <p className="mb-4">
                The exploratory engine that inspects real software to extracts a graph of <strong>States</strong> (screens) and <strong>Transitions</strong> (actions).
              </p>
              <p className="mb-4">
                It operates through platform-specific drivers to normalize accessibility APIs into a common internal representation.
              </p>
              <ul className="list-disc pl-4 space-y-1 mb-4 text-slate-500">
                <li>Automated exploration</li>
                <li>Human-guided recording</li>
              </ul>
              <span className="text-xs font-mono text-slate-400 border border-slate-200 px-2 py-1 rounded">
                Docs: Drivers, State ID, Engine (Coming Soon)
              </span>
            </Card>

            {/* Atlas */}
            <Card title="Atlas (UI Graph & Ontology)">
              <p className="mb-4">
                The storage and semantic layer that persists the UI graph. It provides the core schema for elements and actions.
              </p>
              <p className="mb-4">
                Includes an ontology vocabulary for semantic intents (e.g., “Save”, “Export”) and metadata for tracking provenance (`auto` vs `human`).
              </p>
              <span className="text-xs font-mono text-slate-400 border border-slate-200 px-2 py-1 rounded">
                Docs: Graph Model, Schema, Vocabulary (Coming Soon)
              </span>
            </Card>
          </div>
        </section>

        {/* INTENDED CONSUMERS */}
        <section>
          <h2 className="text-3xl font-serif font-medium mb-6 text-slate-900">Intended Consumers</h2>
          <p className="text-slate-600 mb-6">
            Ariane is designed as a data source. Typical consumers include:
          </p>
          <ul className="space-y-4 text-slate-700">
            <li className="flex gap-3">
              <span className="text-primary font-bold">1.</span>
              <span><strong>AI Agents:</strong> Need to plan and execute steps inside existing software.</span>
            </li>
            <li className="flex gap-3">
              <span className="text-primary font-bold">2.</span>
              <span><strong>Automation Tools:</strong> Require a declarative description of workflows.</span>
            </li>
            <li className="flex gap-3">
              <span className="text-primary font-bold">3.</span>
              <span><strong>Analysis Tools:</strong> Reason about UI complexity, accessibility, or consistency.</span>
            </li>
            <li className="flex gap-3">
              <span className="text-primary font-bold">4.</span>
              <span><strong>Human Assistants:</strong> Provide step-by-step textual guidance to operators using Atlas as the reference.</span>
            </li>
          </ul>
        </section>

        {/* CONCEPTUAL FLOW */}
        <section className="bg-slate-50 p-8 rounded-sm border border-slate-100">
          <h2 className="text-2xl font-serif font-medium mb-6 text-slate-900">Conceptual Flow</h2>
          <div className="space-y-6">
            <div>
              <h4 className="font-bold text-slate-900 uppercase tracking-widest text-xs mb-1">Step 1: Exploration</h4>
              <p className="text-sm text-slate-600">Theseus runs against a target application, observing the UI (automatically or human-guided).</p>
            </div>
            <div>
              <h4 className="font-bold text-slate-900 uppercase tracking-widest text-xs mb-1">Step 2: Extraction</h4>
              <p className="text-sm text-slate-600">Identifies UI states, fingerprints them, and records transitions with metadata.</p>
            </div>
            <div>
              <h4 className="font-bold text-slate-900 uppercase tracking-widest text-xs mb-1">Step 3: Storage</h4>
              <p className="text-sm text-slate-600">The resulting graph (states + transitions + semantics) is written into Atlas.</p>
            </div>
            <div>
              <h4 className="font-bold text-slate-900 uppercase tracking-widest text-xs mb-1">Step 4: Consumption</h4>
              <p className="text-sm text-slate-600">External systems query Atlas to recognize states, determine valid next actions, or compute paths.</p>
            </div>
          </div>
        </section>

        {/* NAVIGATION / FOOTER */}
        <section className="border-t border-gray-100 pt-10">
          <h3 className="text-sm font-bold uppercase tracking-widest text-slate-400 mb-6">Related Documentation</h3>
          <div className="grid sm:grid-cols-2 gap-4 text-sm">
            <span className="text-slate-400">Background: UI as Data (Coming Soon)</span>
            <span className="text-slate-400">Hybrid Mapping Patterns (Coming Soon)</span>
            <span className="text-slate-400">Glossary of Terms (Coming Soon)</span>
          </div>
        </section>

      </div>
    </main>
  );
}